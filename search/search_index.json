{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Substrax","text":"<p>Shared training and hardware infrastructure for the Avitai JAX stack. Substrax sits at the bottom of the dependency chain \u2014 <code>substrax \u2192 calibrax \u2192 datarax \u2192 artifex \u2192 opifex</code> \u2014 and holds the code those packages used to carry separately.</p> Subpackage What it owns <code>substrax.runtime</code> JAX process settings applied before or after jax is imported, XLA flags merged by name, test-run device emulation, entry-point logging <code>substrax.artifacts</code> Output directories from an argument, <code>AVITAI_OUTPUT_DIR</code> or a per-run temporary directory, never the working tree <code>substrax.testing</code> Opt-in test infrastructure: fresh-interpreter runs with a chosen JAX configuration, and a pytest plugin with device markers and jax configuration isolation <code>substrax.devices</code> Device information and device placement <code>substrax.mesh</code> Device meshes, mesh rules, partition-spec helpers, sharding strategies <code>substrax.spmd</code> Data-parallel placement, <code>spmd_train_step</code>, gradient reduction, collectives <code>substrax.checkpoint</code> The <code>CheckpointStore</code> protocol and its Orbax implementation <code>substrax.callbacks</code> Training callbacks, <code>BestMetricTracker</code>, <code>EarlyStopping</code> <code>substrax.tracking</code> Step-wise experiment tracking backends <p>Each subpackage has an API page.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>uv add substrax\n</code></pre> <p>Python 3.12 or later, <code>jax&gt;=0.11.1</code>, <code>flax&gt;=0.12.9</code>, <code>orbax-checkpoint&gt;=0.11.33</code>.</p>"},{"location":"api/artifacts/","title":"Artifacts","text":"<p><code>substrax.artifacts</code> decides where a run writes its figures, tables and other outputs, and never defaults into the working tree.</p> <pre><code>import argparse\nfrom pathlib import Path\n\nfrom substrax.artifacts import resolve_output_dir\n\n\ndef main() -&gt; None:\n    parser = argparse.ArgumentParser()\n    parser.add_argument(\"--output-dir\", type=Path, default=None)\n    args = parser.parse_args()\n\n    location = resolve_output_dir(\"fno_darcy\", explicit=args.output_dir)\n    figure_path = location.path / \"prediction.png\"\n</code></pre> <p><code>resolve_output_dir</code> takes the first of these that applies and creates the directory:</p> Order Directory <code>source</code> 1 <code>explicit</code>, resolved against the working directory when it is relative <code>\"argument\"</code> 2 <code>$AVITAI_OUTPUT_DIR/&lt;name&gt;</code>; the variable must hold an absolute path <code>\"environment\"</code> 3 <code>&lt;name&gt;</code> inside a directory created once per process under the system temporary directory <code>\"run_default\"</code> <p>Writing figures into the documentation is an explicit choice: <code>python examples/neural-operators/fno_darcy.py --output-dir docs/assets/examples/fno_darcy</code>. A test run or a CI job sets <code>AVITAI_OUTPUT_DIR</code> instead, so running an example never rewrites tracked files.</p> <p>The per-run directory is created with <code>tempfile.mkdtemp</code>, so its name cannot be claimed by another user of a shared temporary directory and only its owner can read it. Every default output in one process shares it. <code>name</code> must be a relative path that stays inside the output directory. Importing the package creates nothing.</p>"},{"location":"api/artifacts/#substrax.artifacts","title":"<code>substrax.artifacts</code>","text":"<p>Output directories that never default into the working tree.</p> <p><code>resolve_output_dir</code> picks the directory a run writes to: an explicit argument, then <code>AVITAI_OUTPUT_DIR</code>, then a directory created once per process under the system temporary directory. Importing this package creates nothing.</p>"},{"location":"api/artifacts/#substrax.artifacts.OutputLocation","title":"<code>OutputLocation(*, path, source)</code>  <code>dataclass</code>","text":"<p>A resolved output directory and where the choice came from.</p> <p>Attributes:</p> Name Type Description <code>path</code> <code>Path</code> <p>The absolute directory, which exists.</p> <code>source</code> <code>Literal['argument', 'environment', 'run_default']</code> <p><code>\"argument\"</code> for a directory the caller passed, <code>\"environment\"</code> for one under <code>AVITAI_OUTPUT_DIR</code>, <code>\"run_default\"</code> for one under the per-run directory.</p>"},{"location":"api/artifacts/#substrax.artifacts.resolve_output_dir","title":"<code>resolve_output_dir(name, *, explicit=None, env=None)</code>","text":"<p>Return the directory a run writes the output called <code>name</code> to, creating it.</p> <p>The directory is <code>explicit</code> when given, with a relative path resolved against the working directory. Otherwise it is <code>$AVITAI_OUTPUT_DIR/&lt;name&gt;</code>, and otherwise <code>&lt;name&gt;</code> inside a directory created once per process under the system temporary directory. It never defaults into the working tree, so running an example or a test cannot overwrite tracked files, and writing into <code>docs/assets</code> is always an explicit choice.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The output's relative path, such as <code>\"fno_darcy\"</code>.</p> required <code>explicit</code> <code>Path | None</code> <p>A directory chosen by the caller, typically from an <code>--output-dir</code> option.</p> <code>None</code> <code>env</code> <code>Mapping[str, str] | None</code> <p>The environment <code>AVITAI_OUTPUT_DIR</code> is read from; <code>os.environ</code> when <code>None</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>OutputLocation</code> <p>The created directory and the source it was chosen from.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>name</code> is empty, absolute or leaves the output directory, or <code>AVITAI_OUTPUT_DIR</code> holds a relative path.</p>"},{"location":"api/callbacks/","title":"Callbacks","text":"<p><code>substrax.callbacks</code> holds the training-callback protocol and metric-driven early stopping, in two shapes:</p> <ul> <li><code>EarlyStopping</code> for a loop that feeds it one validation value per epoch and asks   <code>should_stop</code>;</li> <li><code>EarlyStoppingCallback</code> for a trainer that drives <code>TrainingCallback</code> hooks and reads   the metric from the epoch's logs.</li> </ul> <p>Both keep their counters in a <code>BestMetricTracker</code>.</p> <pre><code>from substrax.callbacks import EarlyStopping, EarlyStoppingCallback, EarlyStoppingConfig\n\nstopper = EarlyStopping(patience=3, min_delta=1e-3)\nfor epoch in range(epochs):\n    stopper.update(validate())\n    if stopper.should_stop:\n        break\n\ncallback = EarlyStoppingCallback(EarlyStoppingConfig(monitor=\"val_loss\", patience=10))\n</code></pre> <p>Learning-rate decay on a plateau is not here: use <code>optax.contrib.reduce_on_plateau</code>.</p>"},{"location":"api/callbacks/#substrax.callbacks","title":"<code>substrax.callbacks</code>","text":"<p>Training callbacks, best-metric tracking and early stopping.</p>"},{"location":"api/callbacks/#substrax.callbacks.BaseCallback","title":"<code>BaseCallback</code>","text":"<p>No-op implementation of every hook; subclasses override what they need.</p>"},{"location":"api/callbacks/#substrax.callbacks.BaseCallback.on_train_begin","title":"<code>on_train_begin(_trainer)</code>","text":"<p>Called once before the first epoch.</p>"},{"location":"api/callbacks/#substrax.callbacks.BaseCallback.on_train_end","title":"<code>on_train_end(_trainer)</code>","text":"<p>Called once after the last epoch.</p>"},{"location":"api/callbacks/#substrax.callbacks.BaseCallback.on_epoch_begin","title":"<code>on_epoch_begin(_trainer, _epoch)</code>","text":"<p>Called before each epoch.</p>"},{"location":"api/callbacks/#substrax.callbacks.BaseCallback.on_epoch_end","title":"<code>on_epoch_end(_trainer, _epoch, _logs)</code>","text":"<p>Called after each epoch with that epoch's metrics.</p>"},{"location":"api/callbacks/#substrax.callbacks.BaseCallback.on_batch_begin","title":"<code>on_batch_begin(_trainer, _batch)</code>","text":"<p>Called before each batch.</p>"},{"location":"api/callbacks/#substrax.callbacks.BaseCallback.on_batch_end","title":"<code>on_batch_end(_trainer, _batch, _logs)</code>","text":"<p>Called after each batch with that batch's metrics.</p>"},{"location":"api/callbacks/#substrax.callbacks.BaseCallback.on_validation_begin","title":"<code>on_validation_begin(_trainer)</code>","text":"<p>Called before a validation pass.</p>"},{"location":"api/callbacks/#substrax.callbacks.BaseCallback.on_validation_end","title":"<code>on_validation_end(_trainer, _logs)</code>","text":"<p>Called after a validation pass with its metrics.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList","title":"<code>CallbackList(callbacks=None)</code>","text":"<p>Dispatches every hook to its callbacks in registration order.</p> <p>Wrap an initial set of callbacks.</p> <p>Parameters:</p> Name Type Description Default <code>callbacks</code> <code>Iterable[TrainingCallback] | None</code> <p>The callbacks to start with; each receives every hook, in this order.</p> <code>None</code>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.add","title":"<code>add(callback)</code>","text":"<p>Append a callback.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.remove","title":"<code>remove(callback)</code>","text":"<p>Remove a callback; raises <code>ValueError</code> if it was never added.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.__len__","title":"<code>__len__()</code>","text":"<p>The number of callbacks.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.__iter__","title":"<code>__iter__()</code>","text":"<p>Iterate over the callbacks in dispatch order.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.on_train_begin","title":"<code>on_train_begin(trainer)</code>","text":"<p>Dispatch <code>on_train_begin</code>.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.on_train_end","title":"<code>on_train_end(trainer)</code>","text":"<p>Dispatch <code>on_train_end</code>.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.on_epoch_begin","title":"<code>on_epoch_begin(trainer, epoch)</code>","text":"<p>Dispatch <code>on_epoch_begin</code>.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.on_epoch_end","title":"<code>on_epoch_end(trainer, epoch, logs)</code>","text":"<p>Dispatch <code>on_epoch_end</code>.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.on_batch_begin","title":"<code>on_batch_begin(trainer, batch)</code>","text":"<p>Dispatch <code>on_batch_begin</code>.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.on_batch_end","title":"<code>on_batch_end(trainer, batch, logs)</code>","text":"<p>Dispatch <code>on_batch_end</code>.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.on_validation_begin","title":"<code>on_validation_begin(trainer)</code>","text":"<p>Dispatch <code>on_validation_begin</code>.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.on_validation_end","title":"<code>on_validation_end(trainer, logs)</code>","text":"<p>Dispatch <code>on_validation_end</code>.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainerLike","title":"<code>TrainerLike</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>What a callback may ask of the trainer that drives it.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainerLike.model","title":"<code>model</code>  <code>property</code>","text":"<p>The model being trained.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainingCallback","title":"<code>TrainingCallback</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>Lifecycle hooks a training loop fires; implement any subset via <code>BaseCallback</code>.</p> <p>Parameters are positional-only so implementations may name them as they like.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainingCallback.on_train_begin","title":"<code>on_train_begin(trainer)</code>","text":"<p>Called once before the first epoch.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainingCallback.on_train_end","title":"<code>on_train_end(trainer)</code>","text":"<p>Called once after the last epoch.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainingCallback.on_epoch_begin","title":"<code>on_epoch_begin(trainer, epoch)</code>","text":"<p>Called before each epoch.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainingCallback.on_epoch_end","title":"<code>on_epoch_end(trainer, epoch, logs)</code>","text":"<p>Called after each epoch with that epoch's metrics.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainingCallback.on_batch_begin","title":"<code>on_batch_begin(trainer, batch)</code>","text":"<p>Called before each batch.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainingCallback.on_batch_end","title":"<code>on_batch_end(trainer, batch, logs)</code>","text":"<p>Called after each batch with that batch's metrics.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainingCallback.on_validation_begin","title":"<code>on_validation_begin(trainer)</code>","text":"<p>Called before a validation pass.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainingCallback.on_validation_end","title":"<code>on_validation_end(trainer, logs)</code>","text":"<p>Called after a validation pass with its metrics.</p>"},{"location":"api/callbacks/#substrax.callbacks.EarlyStoppingCallback","title":"<code>EarlyStoppingCallback(config)</code>","text":"<p>               Bases: <code>BaseCallback</code></p> <p>Stop training when a monitored metric stops improving.</p> <p>Initialize early stopping callback.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>EarlyStoppingConfig</code> <p>Early stopping configuration.</p> required"},{"location":"api/callbacks/#substrax.callbacks.EarlyStoppingCallback.wait_count","title":"<code>wait_count</code>  <code>property</code>","text":"<p>Epochs since the last improvement.</p>"},{"location":"api/callbacks/#substrax.callbacks.EarlyStoppingCallback.best_score","title":"<code>best_score</code>  <code>property</code>","text":"<p>The best monitored value so far, or <code>None</code> before the metric first appears.</p>"},{"location":"api/callbacks/#substrax.callbacks.EarlyStoppingCallback.stopped_epoch","title":"<code>stopped_epoch</code>  <code>property</code>","text":"<p>The epoch at which stopping was decided, if it has been.</p>"},{"location":"api/callbacks/#substrax.callbacks.EarlyStoppingCallback.should_stop","title":"<code>should_stop</code>  <code>property</code>","text":"<p>Whether training should stop.</p>"},{"location":"api/callbacks/#substrax.callbacks.EarlyStoppingCallback.on_epoch_end","title":"<code>on_epoch_end(_trainer, epoch, logs)</code>","text":"<p>Read the monitored metric, if present, and decide whether to stop.</p> <p>Parameters:</p> Name Type Description Default <code>_trainer</code> <code>TrainerLike</code> <p>The trainer instance (unused).</p> required <code>epoch</code> <code>int</code> <p>Current epoch number.</p> required <code>logs</code> <code>dict[str, Any]</code> <p>Dictionary of metrics from this epoch.</p> required"},{"location":"api/callbacks/#substrax.callbacks.EarlyStoppingConfig","title":"<code>EarlyStoppingConfig(*, monitor='val_loss', min_delta=0.0, patience=10, mode='min', check_finite=True, stopping_threshold=None, divergence_threshold=None)</code>  <code>dataclass</code>","text":"<p>Configuration for early stopping.</p> <p>Attributes:</p> Name Type Description <code>monitor</code> <code>str</code> <p>Metric name to monitor (e.g., \"val_loss\", \"accuracy\").</p> <code>min_delta</code> <code>float</code> <p>Minimum change to qualify as an improvement.</p> <code>patience</code> <code>int</code> <p>Number of epochs with no improvement before stopping.</p> <code>mode</code> <code>Literal['min', 'max']</code> <p>\"min\" if lower is better, \"max\" if higher is better.</p> <code>check_finite</code> <code>bool</code> <p>If True, stop when metric becomes NaN or Inf.</p> <code>stopping_threshold</code> <code>float | None</code> <p>Stop immediately when metric reaches this value.</p> <code>divergence_threshold</code> <code>float | None</code> <p>Stop if metric exceeds this value (min mode only).</p>"},{"location":"api/callbacks/#substrax.callbacks.BestMetricTracker","title":"<code>BestMetricTracker(*, mode, min_delta)</code>","text":"<p>Shared best-so-far + stagnation bookkeeping for the plateau callbacks.</p> <p>Tracks the best monitored value and the number of consecutive updates without a <code>min_delta</code> improvement, in either <code>\"min\"</code> or <code>\"max\"</code> mode.</p> <p>Initialise the tracker.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>PlateauMode | str</code> <p><code>\"min\"</code> (lower is better) or <code>\"max\"</code> (higher is better).</p> required <code>min_delta</code> <code>float</code> <p>Minimum absolute change counted as an improvement.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>min_delta</code> is negative.</p>"},{"location":"api/callbacks/#substrax.callbacks.BestMetricTracker.mode","title":"<code>mode</code>  <code>property</code>","text":"<p>The improvement direction.</p>"},{"location":"api/callbacks/#substrax.callbacks.BestMetricTracker.best","title":"<code>best</code>  <code>property</code>","text":"<p>The best monitored value seen so far.</p>"},{"location":"api/callbacks/#substrax.callbacks.BestMetricTracker.num_bad_epochs","title":"<code>num_bad_epochs</code>  <code>property</code>","text":"<p>Consecutive updates without a <code>min_delta</code> improvement.</p>"},{"location":"api/callbacks/#substrax.callbacks.BestMetricTracker.register","title":"<code>register(value)</code>","text":"<p>Record <code>value</code>; return <code>True</code> if it improves on the best so far.</p> <p>Updates the best value and resets the stagnation counter on an improvement; otherwise increments the stagnation counter.</p>"},{"location":"api/callbacks/#substrax.callbacks.EarlyStopping","title":"<code>EarlyStopping(*, patience, min_delta=0.0, mode=PlateauMode.MIN)</code>","text":"<p>               Bases: <code>BestMetricTracker</code></p> <p>Signal to stop once a monitored metric stops improving.</p> <p>Initialise the stopper.</p> <p>Parameters:</p> Name Type Description Default <code>patience</code> <code>int</code> <p>Epochs without a <code>min_delta</code> improvement before stopping.</p> required <code>min_delta</code> <code>float</code> <p>Minimum absolute change counted as an improvement.</p> <code>0.0</code> <code>mode</code> <code>PlateauMode | str</code> <p><code>\"min\"</code> (lower is better) or <code>\"max\"</code> (higher is better).</p> <code>MIN</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>patience</code> is not positive.</p>"},{"location":"api/callbacks/#substrax.callbacks.EarlyStopping.should_stop","title":"<code>should_stop</code>  <code>property</code>","text":"<p>Whether the metric has stagnated for <code>patience</code> epochs.</p>"},{"location":"api/callbacks/#substrax.callbacks.EarlyStopping.update","title":"<code>update(value)</code>","text":"<p>Record the latest metric; return <code>True</code> if it improved on the best.</p>"},{"location":"api/callbacks/#substrax.callbacks.PlateauMode","title":"<code>PlateauMode</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Whether a monitored metric improves by decreasing or increasing.</p>"},{"location":"api/checkpoint/","title":"Checkpoint","text":"<p><code>substrax.checkpoint</code> is one checkpoint store over Orbax: step-addressed, pickle-free, with retention and best-step selection delegated to <code>orbax.checkpoint.CheckpointManager</code>.</p> <pre><code>from substrax.checkpoint import OrbaxCheckpointStore\n\nwith OrbaxCheckpointStore(\"checkpoints\", max_to_keep=5) as store:\n    store.save(model, step=100, loss=0.42)\n    model, metadata = store.restore(model)          # the latest step\n    best = store.best_step(metric=\"loss\")\n</code></pre> <p>The payload is written with <code>PyTreeSave</code>, which carries arrays, typed PRNG keys and plain-Python leaves (ints, strings, booleans, lists) alike, and metadata with <code>JsonSave</code>, so restoring a checkpoint never executes code. Consumers depend on the <code>CheckpointStore</code> protocol; the Orbax class is the one implementation.</p>"},{"location":"api/checkpoint/#substrax.checkpoint","title":"<code>substrax.checkpoint</code>","text":"<p>The checkpoint store protocol and its Orbax implementation.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointStore","title":"<code>CheckpointStore</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>Step-int addressed checkpoint store contract.</p> <p>Implementations persist a model payload plus JSON metadata under an integer <code>step</code> and restore it back. This is the single abstraction the training layer depends on; concrete backends live in the infrastructure layer.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointStore.save","title":"<code>save(model, step, loss=None, *, physics_metadata=None, additional_metadata=None)</code>","text":"<p>Persist <code>model</code> at <code>step</code> and return the checkpoint path.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointStore.restore","title":"<code>restore(target_model=None, step=None, *, return_original_on_missing=True, restrict_to_nnx_module=False)</code>","text":"<p>Restore the payload + metadata saved at <code>step</code>.</p> <p>With a target, arrays are restored onto the target's device placement. Without one the payload comes back as it was stored.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointStore.list_steps","title":"<code>list_steps()</code>","text":"<p>Return all available checkpoint steps.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointStore.latest_step","title":"<code>latest_step()</code>","text":"<p>Return the most recent checkpoint step, or <code>None</code>.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointStore.best_step","title":"<code>best_step(metric='loss', *, minimize=True)</code>","text":"<p>Return the step optimizing <code>metric</code>, or <code>None</code>.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointStore.delete","title":"<code>delete(step)</code>","text":"<p>Delete the checkpoint at <code>step</code>; return success.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointStore.close","title":"<code>close()</code>","text":"<p>Release backend resources.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore","title":"<code>OrbaxCheckpointStore(checkpoint_dir, max_to_keep=5, create=True)</code>","text":"<p>Orbax-backed implementation of :class:<code>CheckpointStore</code>.</p> <p>Wraps :class:<code>orbax.checkpoint.CheckpointManager</code> with step-int addressing. A single checkpoint bundles the payload pytree (<code>PyTreeSave</code>) and a JSON metadata sidecar (<code>JsonSave</code>) carrying the step, timestamp, model type, the loss when one is given, physics metadata, and any caller extras. Old checkpoints are pruned by Orbax according to <code>max_to_keep</code>.</p> <p>The store doubles as a context manager so backend resources are released deterministically::</p> <pre><code>with OrbaxCheckpointStore(path) as store:\n    store.save(model, step=0, loss=loss)\n</code></pre> <p>Initialize the store.</p> <p>Parameters:</p> Name Type Description Default <code>checkpoint_dir</code> <code>str | Path</code> <p>Directory in which checkpoints are stored.</p> required <code>max_to_keep</code> <code>int | None</code> <p>Maximum number of checkpoints Orbax retains; <code>None</code> keeps every checkpoint.</p> <code>5</code> <code>create</code> <code>bool</code> <p>Whether to create <code>checkpoint_dir</code> if it is missing.</p> <code>True</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>checkpoint_dir</code> is empty or whitespace-only.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.__enter__","title":"<code>__enter__()</code>","text":"<p>Enter the context manager.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.__exit__","title":"<code>__exit__(*_exc_info)</code>","text":"<p>Close the backend on context exit.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.save","title":"<code>save(model, step, loss=None, *, physics_metadata=None, additional_metadata=None)</code>","text":"<p>Persist a checkpoint with complete model state and metadata.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>ModelLike</code> <p>Model payload \u2014 an <code>nnx.Module</code>, a Flax <code>TrainState</code>, or a plain state <code>dict</code> (arrays, typed PRNG keys and plain-Python leaves alike).</p> required <code>step</code> <code>int</code> <p>Non-negative training step number (the checkpoint key).</p> required <code>loss</code> <code>float | None</code> <p>Current loss value, recorded in metadata when given; a payload with no training loss, such as iterator state, omits it.</p> <code>None</code> <code>physics_metadata</code> <code>dict[str, Any] | None</code> <p>Optional physics-specific metadata.</p> <code>None</code> <code>additional_metadata</code> <code>dict[str, Any] | None</code> <p>Optional extra metadata merged into the top-level metadata record.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>Filesystem path of the saved checkpoint.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If <code>model</code> is not a supported type.</p> <code>ValueError</code> <p>If <code>step</code> is negative.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.restore","title":"<code>restore(target_model=None, step=None, *, return_original_on_missing=True, restrict_to_nnx_module=False)</code>","text":"<p>Restore model state and metadata for <code>step</code>.</p> <p>Parameters:</p> Name Type Description Default <code>target_model</code> <code>ModelLike | None</code> <p>Model to restore arrays into; each array lands on the device placement and dtype of its target leaf, whatever topology the checkpoint was written on. <code>None</code> returns the payload as it was stored: a pytree of arrays and plain leaves, in which a module's Variables are <code>{\"value\": ...}</code> nodes, placed as they were at save time.</p> <code>None</code> <code>step</code> <code>int | None</code> <p>Step to restore.</p> <code>None</code> <code>return_original_on_missing</code> <code>bool</code> <p>If <code>True</code>, return <code>target_model</code> unchanged when the checkpoint is missing; otherwise <code>None</code>.</p> <code>True</code> <code>restrict_to_nnx_module</code> <code>bool</code> <p>If <code>True</code>, coerce a non-<code>nnx.Module</code> result to <code>None</code>.</p> <code>False</code> <p>Returns:</p> Type Description <code>ModelLike | None</code> <p>Tuple of <code>(model_or_none, metadata)</code>. <code>metadata</code> is empty when</p> <code>dict[str, Any]</code> <p>the checkpoint is missing.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>target_model</code>'s tree does not match the checkpoint.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.create_train_state","title":"<code>create_train_state(model, optimizer, step=0, **kwargs)</code>","text":"<p>Create a Flax <code>TrainState</code> for complete-state checkpointing.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>NNX module to wrap.</p> required <code>optimizer</code> <code>Any</code> <p>An optax optimizer used as the <code>TrainState</code> <code>tx</code>.</p> required <code>step</code> <code>int</code> <p>Initial step number.</p> <code>0</code> <code>**kwargs</code> <code>Any</code> <p>Extra arguments forwarded to <code>TrainState.create</code>.</p> <code>{}</code> <p>Returns:</p> Type Description <code>TrainState</code> <p>A <code>TrainState</code> carrying the model parameters and optimizer.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If <code>model</code> is not an <code>nnx.Module</code>.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.save_train_state","title":"<code>save_train_state(state, step, loss=None, *, physics_metadata=None, additional_metadata=None)</code>","text":"<p>Persist a <code>TrainState</code> checkpoint (see :meth:<code>save</code>).</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.restore_train_state","title":"<code>restore_train_state(target_state, step=None)</code>","text":"<p>Restore a <code>TrainState</code> checkpoint, falling back to the target.</p> <p>Parameters:</p> Name Type Description Default <code>target_state</code> <code>TrainState</code> <p><code>TrainState</code> whose structure guides restoration.</p> required <code>step</code> <code>int | None</code> <p>Step to restore.</p> <code>None</code> <p>Returns:</p> Type Description <code>tuple[TrainState, dict[str, Any]]</code> <p>Tuple of <code>(train_state, metadata)</code>.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.list_steps","title":"<code>list_steps()</code>","text":"<p>Return all available checkpoint steps.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.latest_step","title":"<code>latest_step()</code>","text":"<p>Return the most recent checkpoint step, or <code>None</code>.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.best_step","title":"<code>best_step(metric='loss', *, minimize=True)</code>","text":"<p>Return the checkpoint step optimizing <code>metric</code>.</p> <p>The metric is read from each checkpoint's JSON metadata. <code>\"loss\"</code> reads the top-level loss; any other name is looked up in metadata.</p> <p>Parameters:</p> Name Type Description Default <code>metric</code> <code>str</code> <p>Metadata key to optimize (<code>\"loss\"</code> by default).</p> <code>'loss'</code> <code>minimize</code> <code>bool</code> <p>Whether the best value is the minimum (<code>True</code>) or maximum (<code>False</code>).</p> <code>True</code> <p>Returns:</p> Type Description <code>int | None</code> <p>The optimizing step, or <code>None</code> if no checkpoints exist.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.delete","title":"<code>delete(step)</code>","text":"<p>Delete the checkpoint at <code>step</code>.</p> <p>Parameters:</p> Name Type Description Default <code>step</code> <code>int</code> <p>Step number to delete.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> on success, <code>False</code> if deletion failed.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.close","title":"<code>close()</code>","text":"<p>Close the underlying Orbax manager and release resources.</p>"},{"location":"api/devices/","title":"Devices","text":"<p><code>substrax.devices</code> answers two questions: what hardware is this process running on, and where should a batch go.</p> <pre><code>from substrax.devices import DevicePlacement, detect_devices, get_batch_size_recommendation\n\ninfo = detect_devices()          # platform, kind, count, memory, name\nplacement = DevicePlacement()    # defaults to jax.devices()\nbatch = placement.place_on_device(batch)\nrecommendation = get_batch_size_recommendation()  # for the detected hardware\n</code></pre> <p><code>DeviceInfo</code> is a frozen dataclass; <code>detect_devices()</code> reads it from <code>jax.devices()</code> and never imports an accelerator plugin itself. <code>DevicePlacement</code> carries the batch-size recommendation table per <code>HardwareType</code>.</p>"},{"location":"api/devices/#substrax.devices","title":"<code>substrax.devices</code>","text":"<p>Device information and device placement.</p>"},{"location":"api/devices/#substrax.devices.DeviceInfo","title":"<code>DeviceInfo(*, platform, kind, count, device_kinds)</code>  <code>dataclass</code>","text":"<p>Snapshot of the devices visible to the current JAX process.</p> <p>Attributes:</p> Name Type Description <code>platform</code> <code>str</code> <p>The default backend name exactly as JAX reports it.</p> <code>kind</code> <code>DeviceKind</code> <p>The accelerator class of that backend.</p> <code>count</code> <code>int</code> <p>The number of visible devices.</p> <code>device_kinds</code> <code>tuple[str, ...]</code> <p>One <code>device_kind</code> string per visible device, in JAX order.</p>"},{"location":"api/devices/#substrax.devices.DeviceInfo.has_accelerator","title":"<code>has_accelerator</code>  <code>property</code>","text":"<p>Whether the default backend is anything other than the CPU.</p>"},{"location":"api/devices/#substrax.devices.DeviceKind","title":"<code>DeviceKind</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>The accelerator class behind a JAX backend name.</p>"},{"location":"api/devices/#substrax.devices.DeviceKind.from_platform","title":"<code>from_platform(platform)</code>  <code>classmethod</code>","text":"<p>Map a backend name as <code>jax.default_backend()</code> reports it onto a kind.</p> <p>Parameters:</p> Name Type Description Default <code>platform</code> <code>str</code> <p>The backend name, in any case (<code>\"gpu\"</code>, <code>\"cuda\"</code>, <code>\"METAL\"</code>).</p> required <p>Returns:</p> Type Description <code>DeviceKind</code> <p>The matching kind, or <code>OTHER</code> for a backend this package has not seen.</p>"},{"location":"api/devices/#substrax.devices.BatchSizeRecommendation","title":"<code>BatchSizeRecommendation(min_batch_size, optimal_batch_size, critical_batch_size, max_memory_batch_size=None, notes='')</code>  <code>dataclass</code>","text":"<p>Hardware-specific batch size recommendations.</p> <p>Attributes:</p> Name Type Description <code>min_batch_size</code> <code>int</code> <p>Minimum batch size for reasonable efficiency.</p> <code>optimal_batch_size</code> <code>int</code> <p>Optimal batch size for peak throughput.</p> <code>critical_batch_size</code> <code>int</code> <p>Critical batch size for reaching roofline (per JAX guide).</p> <code>max_memory_batch_size</code> <code>int | None</code> <p>Maximum batch size before OOM (estimate).</p> <code>notes</code> <code>str</code> <p>Additional notes about the recommendation.</p>"},{"location":"api/devices/#substrax.devices.DevicePlacement","title":"<code>DevicePlacement(default_device=None)</code>","text":"<p>Utility class for explicit device placement of JAX arrays.</p> <p>This class provides methods for placing arrays on specific devices, distributing batches across devices using sharding, and providing hardware-aware batch size recommendations.</p> Example <pre><code>placement = DevicePlacement()\ndata = jnp.ones((4, 8))\ngpu_data = placement.place_on_device(data, jax.devices(\"gpu\")[0])  # Place on first GPU\nrec = placement.get_batch_size_recommendation()  # Get batch size recommendation\nprint(f\"Optimal batch: {rec.optimal_batch_size}\")\n</code></pre> <p>Initialize DevicePlacement.</p> <p>Parameters:</p> Name Type Description Default <code>default_device</code> <code>Device | None</code> <p>Default device to use when none is specified. If None, lazily resolves to jax.devices()[0] on first access.</p> <code>None</code>"},{"location":"api/devices/#substrax.devices.DevicePlacement.default_device","title":"<code>default_device</code>  <code>property</code>","text":"<p>Get the default device, lazily resolving on first access.</p>"},{"location":"api/devices/#substrax.devices.DevicePlacement.hardware_type","title":"<code>hardware_type</code>  <code>property</code>","text":"<p>Get the detected hardware type, lazily resolving on first access.</p>"},{"location":"api/devices/#substrax.devices.DevicePlacement.num_devices","title":"<code>num_devices</code>  <code>property</code>","text":"<p>Get the number of available devices.</p>"},{"location":"api/devices/#substrax.devices.DevicePlacement.place_on_device","title":"<code>place_on_device(data, device=None)</code>","text":"<p>Place data on a specific device.</p> <p>This uses jax.device_put for explicit device placement, ensuring data is transferred to the target device.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>PyTree</code> <p>PyTree of JAX arrays to place on device.</p> required <code>device</code> <code>Device | None</code> <p>Target device. If None, uses the default device.</p> <code>None</code> <p>Returns:</p> Type Description <code>PyTree</code> <p>PyTree with arrays placed on the specified device.</p> Example <pre><code>data = {\"images\": jnp.ones((4, 28, 28, 3))}\ngpu_data = placement.place_on_device(data, jax.devices(\"gpu\")[0])\n</code></pre>"},{"location":"api/devices/#substrax.devices.DevicePlacement.replicate_across_devices","title":"<code>replicate_across_devices(data, devices=None)</code>","text":"<p>Replicate data across all specified devices.</p> <p>Creates a copy of the data on each device, useful for broadcasting model weights or constants.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>PyTree</code> <p>PyTree of JAX arrays to replicate.</p> required <code>devices</code> <code>list[Device] | None</code> <p>List of devices to replicate to. If None, uses all devices.</p> <code>None</code> <p>Returns:</p> Type Description <code>PyTree</code> <p>PyTree with arrays replicated across devices.</p>"},{"location":"api/devices/#substrax.devices.DevicePlacement.shard_batch_dim","title":"<code>shard_batch_dim(data, mesh, batch_axis=0, mesh_axis='data')</code>","text":"<p>Shard data along the batch dimension.</p> <p>This is the most common sharding pattern for data-parallel training, where each device processes a slice of the batch.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>PyTree</code> <p>PyTree of JAX arrays to shard.</p> required <code>mesh</code> <code>Mesh</code> <p>Device mesh to shard across.</p> required <code>batch_axis</code> <code>int</code> <p>The axis index representing the batch dimension.</p> <code>0</code> <code>mesh_axis</code> <code>str</code> <p>The mesh axis name to shard along.</p> <code>'data'</code> <p>Returns:</p> Type Description <code>PyTree</code> <p>PyTree with arrays sharded along the batch dimension.</p>"},{"location":"api/devices/#substrax.devices.DevicePlacement.get_batch_size_recommendation","title":"<code>get_batch_size_recommendation(hardware_type=None)</code>","text":"<p>Get batch size recommendation for the current hardware.</p> <p>Parameters:</p> Name Type Description Default <code>hardware_type</code> <code>HardwareType | None</code> <p>Override hardware type. If None, uses detected type.</p> <code>None</code> <p>Returns:</p> Type Description <code>BatchSizeRecommendation</code> <p>BatchSizeRecommendation with hardware-specific values.</p>"},{"location":"api/devices/#substrax.devices.DevicePlacement.validate_batch_size","title":"<code>validate_batch_size(batch_size, warn_suboptimal=True)</code>","text":"<p>Validate batch size against hardware recommendations.</p> <p>Parameters:</p> Name Type Description Default <code>batch_size</code> <code>int</code> <p>The batch size to validate.</p> required <code>warn_suboptimal</code> <code>bool</code> <p>Whether to warn for suboptimal (but valid) sizes.</p> <code>True</code> <p>Returns:</p> Type Description <code>tuple[bool, str]</code> <p>Tuple of (is_valid, message).</p>"},{"location":"api/devices/#substrax.devices.DevicePlacement.get_device_info","title":"<code>get_device_info()</code>","text":"<p>Get information about available devices.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dictionary containing device information.</p>"},{"location":"api/devices/#substrax.devices.HardwareType","title":"<code>HardwareType</code>","text":"<p>               Bases: <code>Enum</code></p> <p>Enumeration of supported hardware types.</p>"},{"location":"api/devices/#substrax.devices.detect_devices","title":"<code>detect_devices()</code>","text":"<p>Probe the JAX runtime once and return what it reports.</p> <p>Errors from the runtime (no backend, uninitialised plugins) propagate; a process that cannot see its devices should not be told it is running on a CPU.</p> <p>Returns:</p> Type Description <code>DeviceInfo</code> <p>The device snapshot.</p>"},{"location":"api/devices/#substrax.devices.get_batch_size_recommendation","title":"<code>get_batch_size_recommendation(hardware_type=None)</code>","text":"<p>Get batch size recommendation for current or specified hardware.</p> <p>Parameters:</p> Name Type Description Default <code>hardware_type</code> <code>HardwareType | None</code> <p>Hardware type to get recommendation for.</p> <code>None</code> <p>Returns:</p> Type Description <code>BatchSizeRecommendation</code> <p>BatchSizeRecommendation with hardware-specific values.</p>"},{"location":"api/devices/#substrax.devices.place_on_device","title":"<code>place_on_device(data, device=None)</code>","text":"<p>Convenience function for placing data on a device.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>PyTree</code> <p>PyTree of JAX arrays.</p> required <code>device</code> <code>Device | None</code> <p>Target device. If None, uses first available device.</p> <code>None</code> <p>Returns:</p> Type Description <code>PyTree</code> <p>PyTree with arrays on the specified device.</p>"},{"location":"api/mesh/","title":"Mesh","text":"<p><code>substrax.mesh</code> builds device meshes and describes how arrays are laid out on them.</p> <pre><code>from substrax.mesh import DeviceMeshManager, FSDPStrategy, ShardingConfig, fsdp_rules\n\nmesh = DeviceMeshManager.create_device_mesh({\"data\": 2, \"model\": 4})\nrules = fsdp_rules(data_axis=\"data\", model_axis=\"model\")\nconfig = ShardingConfig(data_parallel_size=2, tensor_parallel_size=4, fsdp_enabled=True)\nstrategy = FSDPStrategy(\"model\", mesh_axis=1, min_weight_size=config.fsdp_min_weight_size)\n</code></pre> <p>Three layers, from low to high:</p> Layer Names Role Meshes <code>DeviceMeshManager</code> <code>jax.make_mesh</code> over the visible devices, plus mesh introspection Rules <code>MeshRules</code>, <code>data_parallel_rules</code>, <code>fsdp_rules</code>, <code>create_named_sharding</code>, <code>partition_spec_for_names</code> Map logical axis names to mesh axes and build <code>NamedSharding</code>s Strategies <code>ShardingConfig</code>, <code>ParallelismConfig</code>, <code>ShardingStrategy</code> and its data-parallel, FSDP, tensor-parallel, pipeline-parallel and multi-dimensional implementations Decide the partition spec of every parameter in a model"},{"location":"api/mesh/#substrax.mesh","title":"<code>substrax.mesh</code>","text":"<p>Device meshes, mesh rules, partition-spec helpers and sharding strategies.</p>"},{"location":"api/mesh/#substrax.mesh.DeviceMeshManager","title":"<code>DeviceMeshManager</code>","text":"<p>Manager for creating and configuring JAX device meshes.</p> <p>This class provides utilities for creating device meshes for different distributed training configurations, including data-parallel, model-parallel, and hybrid approaches.</p>"},{"location":"api/mesh/#substrax.mesh.DeviceMeshManager.create_device_mesh","title":"<code>create_device_mesh(mesh_shape, devices=None, *, axis_types=None)</code>  <code>staticmethod</code>","text":"<p>Create a JAX device mesh with the specified shape.</p> <p>Every axis is <code>AxisType.Auto</code> unless <code>axis_types</code> says otherwise: the data-parallel helpers in :mod:<code>substrax.spmd</code> leave sharding inference to XLA (the gradient all-reduce of a batch-sharded step is inferred from the input sharding). <code>jax.make_mesh</code> defaults to <code>Explicit</code> axes from jax 0.11, under which a contraction over a sharded batch axis, the backward pass of any layer, raises unless every such op names its <code>out_sharding</code>.</p> <p>Parameters:</p> Name Type Description Default <code>mesh_shape</code> <code>dict[str, int] | list[tuple[str, int]]</code> <p>The shape of the mesh, specified either as a dictionary mapping axis names to sizes, or as a list of (name, size) tuples.</p> required <code>devices</code> <code>list[Any] | None</code> <p>Optional list of devices to use. If None, uses all available devices.</p> <code>None</code> <code>axis_types</code> <code>Sequence[AxisType] | None</code> <p>One <code>AxisType</code> per axis; <code>Auto</code> for every axis when None.</p> <code>None</code> <p>Returns:</p> Type Description <code>Mesh</code> <p>A JAX device mesh.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the mesh shape is incompatible with the number of devices, or <code>axis_types</code> does not name one type per axis.</p>"},{"location":"api/mesh/#substrax.mesh.DeviceMeshManager.create_data_parallel_mesh","title":"<code>create_data_parallel_mesh(num_devices=None)</code>  <code>staticmethod</code>","text":"<p>Create a data-parallel device mesh.</p> <p>Parameters:</p> Name Type Description Default <code>num_devices</code> <code>int | None</code> <p>Optional number of devices to use. If None, uses all available devices.</p> <code>None</code> <p>Returns:</p> Type Description <code>Mesh</code> <p>A JAX device mesh configured for data-parallel training.</p>"},{"location":"api/mesh/#substrax.mesh.DeviceMeshManager.create_model_parallel_mesh","title":"<code>create_model_parallel_mesh(num_devices)</code>  <code>staticmethod</code>","text":"<p>Create a model-parallel device mesh.</p> <p>Parameters:</p> Name Type Description Default <code>num_devices</code> <code>int</code> <p>Number of devices to use for model parallelism.</p> required <p>Returns:</p> Type Description <code>Mesh</code> <p>A JAX device mesh configured for model-parallel training.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If there aren't enough devices available.</p>"},{"location":"api/mesh/#substrax.mesh.DeviceMeshManager.create_hybrid_mesh","title":"<code>create_hybrid_mesh(data_parallel_size, model_parallel_size)</code>  <code>staticmethod</code>","text":"<p>Create a hybrid data-parallel and model-parallel device mesh.</p> <p>Parameters:</p> Name Type Description Default <code>data_parallel_size</code> <code>int</code> <p>Number of devices to use for data parallelism.</p> required <code>model_parallel_size</code> <code>int</code> <p>Number of devices to use for model parallelism.</p> required <p>Returns:</p> Type Description <code>Mesh</code> <p>A JAX device mesh configured for hybrid parallel training.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If there aren't enough devices available.</p>"},{"location":"api/mesh/#substrax.mesh.DeviceMeshManager.get_mesh_info","title":"<code>get_mesh_info(mesh)</code>  <code>staticmethod</code>","text":"<p>Get information about a device mesh.</p> <p>Parameters:</p> Name Type Description Default <code>mesh</code> <code>Mesh</code> <p>The device mesh to inspect.</p> required <p>Returns:</p> Type Description <code>dict[str, int | dict[str, int]]</code> <p>A dictionary containing information about the mesh, including the</p> <code>dict[str, int | dict[str, int]]</code> <p>number of devices and the size of each axis.</p>"},{"location":"api/mesh/#substrax.mesh.MeshRules","title":"<code>MeshRules(*, data=None, embed=None, mlp=None, heads=None)</code>  <code>dataclass</code>","text":"<p>Logical-to-physical mesh axis mapping for SPMD sharding.</p> <p>Maps logical dimension names to physical mesh axis names. Following the pattern from Flax NNX FSDP examples.</p> <p>Attributes:</p> Name Type Description <code>data</code> <code>str | None</code> <p>Mesh axis for the batch/data dimension.</p> <code>embed</code> <code>str | None</code> <p>Mesh axis for embedding dimensions.</p> <code>mlp</code> <code>str | None</code> <p>Mesh axis for MLP hidden dimensions.</p> <code>heads</code> <code>str | None</code> <p>Mesh axis for attention head dimensions.</p>"},{"location":"api/mesh/#substrax.mesh.MeshRules.__call__","title":"<code>__call__(*keys)</code>","text":"<p>Look up mesh axes for the given logical dimension names.</p> <p>Parameters:</p> Name Type Description Default <code>*keys</code> <code>str</code> <p>Logical dimension names (must be attributes of MeshRules).</p> <code>()</code> <p>Returns:</p> Type Description <code>tuple[str | None, ...]</code> <p>Tuple of mesh axis names (or None for unmapped dimensions).</p>"},{"location":"api/mesh/#substrax.mesh.DataParallelStrategy","title":"<code>DataParallelStrategy(axis_name, mesh_axis)</code>","text":"<p>               Bases: <code>ShardingStrategy</code></p> <p>Data parallel sharding strategy.</p> <p>Shards the batch dimension across devices while replicating model parameters and computation.</p>"},{"location":"api/mesh/#substrax.mesh.DataParallelStrategy.get_partition_spec","title":"<code>get_partition_spec(tensor_shape)</code>","text":"<p>Get partition spec for data parallel sharding.</p> <p>Only shards the batch dimension, leaves others replicated.</p>"},{"location":"api/mesh/#substrax.mesh.DataParallelStrategy.apply_sharding","title":"<code>apply_sharding(array, mesh)</code>","text":"<p>Apply data parallel sharding to array.</p>"},{"location":"api/mesh/#substrax.mesh.FSDPStrategy","title":"<code>FSDPStrategy(axis_name, mesh_axis, min_weight_size=1024)</code>","text":"<p>               Bases: <code>ShardingStrategy</code></p> <p>Fully Sharded Data Parallel strategy.</p> <p>Shards model parameters across devices to reduce memory usage while maintaining training efficiency.</p> <p>Initialize FSDP strategy.</p> <p>Parameters:</p> Name Type Description Default <code>axis_name</code> <code>str</code> <p>Name of the mesh axis</p> required <code>mesh_axis</code> <code>int</code> <p>Index of the mesh axis</p> required <code>min_weight_size</code> <code>int</code> <p>Minimum first dimension size to enable sharding</p> <code>1024</code>"},{"location":"api/mesh/#substrax.mesh.FSDPStrategy.should_shard_weight","title":"<code>should_shard_weight(weight)</code>","text":"<p>Determine if a weight should be sharded based on its size.</p> <p>Parameters:</p> Name Type Description Default <code>weight</code> <code>Array</code> <p>Weight array to check</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if weight should be sharded, False otherwise</p>"},{"location":"api/mesh/#substrax.mesh.FSDPStrategy.get_partition_spec","title":"<code>get_partition_spec(tensor_shape)</code>","text":"<p>Get partition spec for FSDP sharding.</p> <p>Shards along the first dimension of weight tensors.</p>"},{"location":"api/mesh/#substrax.mesh.FSDPStrategy.get_gradient_partition_spec","title":"<code>get_gradient_partition_spec(tensor_shape)</code>","text":"<p>Get partition spec for gradient sharding (same as weights).</p>"},{"location":"api/mesh/#substrax.mesh.FSDPStrategy.apply_sharding","title":"<code>apply_sharding(array, mesh)</code>","text":"<p>Apply FSDP sharding to array.</p>"},{"location":"api/mesh/#substrax.mesh.MultiDimensionalStrategy","title":"<code>MultiDimensionalStrategy(strategies, config)</code>","text":"<p>Multi-dimensional parallelism strategy combining multiple approaches.</p> <p>Combines different sharding strategies (data, tensor, FSDP, pipeline) to achieve optimal performance for large-scale training.</p> <p>Initialize multi-dimensional strategy.</p> <p>Parameters:</p> Name Type Description Default <code>strategies</code> <code>Mapping[str, ShardingStrategy]</code> <p>Dictionary mapping strategy names to strategy instances</p> required <code>config</code> <code>ParallelismConfig</code> <p>Sharding configuration for the multi-dimensional strategy</p> required"},{"location":"api/mesh/#substrax.mesh.MultiDimensionalStrategy.get_combined_partition_spec","title":"<code>get_combined_partition_spec(tensor_name, tensor_shape)</code>","text":"<p>Get combined partition spec from all strategies.</p> <p>Parameters:</p> Name Type Description Default <code>tensor_name</code> <code>str</code> <p>Name/type of the tensor</p> required <code>tensor_shape</code> <code>tuple[str, ...]</code> <p>Shape dimension names of the tensor</p> required <p>Returns:</p> Type Description <code>PartitionSpec</code> <p>Combined PartitionSpec</p>"},{"location":"api/mesh/#substrax.mesh.MultiDimensionalStrategy.resolve_sharding_conflicts","title":"<code>resolve_sharding_conflicts(tensor_name, proposed_specs)</code>","text":"<p>Resolve conflicts between multiple proposed partition specs.</p> <p>Parameters:</p> Name Type Description Default <code>tensor_name</code> <code>str</code> <p>Name of the tensor</p> required <code>proposed_specs</code> <code>dict[str, PartitionSpec]</code> <p>Dictionary of strategy names to proposed specs</p> required <p>Returns:</p> Type Description <code>PartitionSpec</code> <p>Resolved PartitionSpec</p>"},{"location":"api/mesh/#substrax.mesh.ParallelismConfig","title":"<code>ParallelismConfig(mesh_shape, mesh_axis_names, sharding_config)</code>  <code>dataclass</code>","text":"<p>Complete parallelism configuration including mesh topology.</p> <p>Combines sharding configuration with device mesh setup.</p>"},{"location":"api/mesh/#substrax.mesh.ParallelismConfig.is_valid","title":"<code>is_valid()</code>","text":"<p>Validate that mesh shape matches sharding configuration.</p>"},{"location":"api/mesh/#substrax.mesh.ParallelismConfig.from_sharding_config","title":"<code>from_sharding_config(config)</code>  <code>classmethod</code>","text":"<p>Create parallelism config from sharding configuration.</p>"},{"location":"api/mesh/#substrax.mesh.PipelineParallelStrategy","title":"<code>PipelineParallelStrategy(axis_name, mesh_axis, num_stages)</code>","text":"<p>               Bases: <code>ShardingStrategy</code></p> <p>Pipeline parallel sharding strategy.</p> <p>Distributes model layers across devices to enable pipeline parallelism for very large models that don't fit on single devices.</p> <p>Initialize pipeline parallel strategy.</p> <p>Parameters:</p> Name Type Description Default <code>axis_name</code> <code>str</code> <p>Name of the mesh axis</p> required <code>mesh_axis</code> <code>int</code> <p>Index of the mesh axis</p> required <code>num_stages</code> <code>int</code> <p>Number of pipeline stages</p> required"},{"location":"api/mesh/#substrax.mesh.PipelineParallelStrategy.assign_layers_to_stages","title":"<code>assign_layers_to_stages(num_layers)</code>","text":"<p>Assign layers to pipeline stages.</p> <p>Parameters:</p> Name Type Description Default <code>num_layers</code> <code>int</code> <p>Total number of layers in the model</p> required <p>Returns:</p> Type Description <code>list[int]</code> <p>list of layer counts per stage</p>"},{"location":"api/mesh/#substrax.mesh.PipelineParallelStrategy.get_partition_spec","title":"<code>get_partition_spec(tensor_shape)</code>","text":"<p>Get partition spec for pipeline parallel sharding.</p> <p>Pipeline parallelism doesn't shard individual tensors, but rather assigns entire layers to different devices.</p>"},{"location":"api/mesh/#substrax.mesh.PipelineParallelStrategy.get_forward_communication_pattern","title":"<code>get_forward_communication_pattern()</code>","text":"<p>Get communication pattern for forward pass.</p> <p>Returns:</p> Type Description <code>list[tuple[int, int]]</code> <p>list of (source_stage, dest_stage) pairs</p>"},{"location":"api/mesh/#substrax.mesh.PipelineParallelStrategy.get_backward_communication_pattern","title":"<code>get_backward_communication_pattern()</code>","text":"<p>Get communication pattern for backward pass.</p> <p>Returns:</p> Type Description <code>list[tuple[int, int]]</code> <p>list of (source_stage, dest_stage) pairs</p>"},{"location":"api/mesh/#substrax.mesh.PipelineParallelStrategy.apply_sharding","title":"<code>apply_sharding(array, mesh)</code>","text":"<p>Apply pipeline parallel sharding to array.</p> <p>Pipeline parallelism handles layer assignment rather than tensor sharding.</p>"},{"location":"api/mesh/#substrax.mesh.ShardingConfig","title":"<code>ShardingConfig(data_parallel_size=1, tensor_parallel_size=1, pipeline_parallel_size=1, fsdp_enabled=False, fsdp_min_weight_size=1024)</code>  <code>dataclass</code>","text":"<p>Configuration for multi-dimensional parallelism setup.</p> <p>Defines the parallelism dimensions and FSDP settings for a model.</p>"},{"location":"api/mesh/#substrax.mesh.ShardingConfig.get_total_device_count","title":"<code>get_total_device_count()</code>","text":"<p>Calculate total devices needed for this configuration.</p>"},{"location":"api/mesh/#substrax.mesh.ShardingConfig.from_device_count","title":"<code>from_device_count(device_count)</code>  <code>classmethod</code>","text":"<p>Create optimal sharding config for given device count.</p> <p>Uses heuristics to balance different parallelism dimensions.</p>"},{"location":"api/mesh/#substrax.mesh.ShardingStrategy","title":"<code>ShardingStrategy(axis_name, mesh_axis)</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for sharding strategies.</p> <p>Defines the interface that all sharding strategies must implement for consistent handling of different parallelism types.</p> <p>Initialize sharding strategy.</p> <p>Parameters:</p> Name Type Description Default <code>axis_name</code> <code>str</code> <p>Name of the mesh axis for this strategy</p> required <code>mesh_axis</code> <code>int</code> <p>Index of the mesh axis</p> required"},{"location":"api/mesh/#substrax.mesh.ShardingStrategy.get_partition_spec","title":"<code>get_partition_spec(tensor_shape)</code>  <code>abstractmethod</code>","text":"<p>Get partition specification for a tensor with given shape names.</p> <p>Parameters:</p> Name Type Description Default <code>tensor_shape</code> <code>tuple[str, ...]</code> <p>Tuple of dimension names for the tensor</p> required <p>Returns:</p> Type Description <code>PartitionSpec</code> <p>PartitionSpec defining how to shard the tensor</p>"},{"location":"api/mesh/#substrax.mesh.ShardingStrategy.apply_sharding","title":"<code>apply_sharding(array, mesh)</code>  <code>abstractmethod</code>","text":"<p>Apply sharding to an array using the given mesh.</p> <p>Parameters:</p> Name Type Description Default <code>array</code> <code>Array</code> <p>JAX array to shard</p> required <code>mesh</code> <code>Mesh</code> <p>Device mesh for sharding</p> required <p>Returns:</p> Type Description <code>Array</code> <p>Sharded array</p>"},{"location":"api/mesh/#substrax.mesh.ShardingStrategy.get_sharding_constraints","title":"<code>get_sharding_constraints()</code>","text":"<p>Get sharding constraints for this strategy.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dictionary of sharding constraints</p>"},{"location":"api/mesh/#substrax.mesh.TensorParallelStrategy","title":"<code>TensorParallelStrategy(axis_name, mesh_axis, shard_dimension=None)</code>","text":"<p>               Bases: <code>ShardingStrategy</code></p> <p>Tensor parallel sharding strategy.</p> <p>Shards model computation across devices by splitting tensors along specific dimensions (typically features).</p> <p>Initialize tensor parallel strategy.</p> <p>Parameters:</p> Name Type Description Default <code>axis_name</code> <code>str</code> <p>Name of the mesh axis</p> required <code>mesh_axis</code> <code>int</code> <p>Index of the mesh axis</p> required <code>shard_dimension</code> <code>str | None</code> <p>Preferred dimension to shard ('in_features' or 'out_features')</p> <code>None</code>"},{"location":"api/mesh/#substrax.mesh.TensorParallelStrategy.get_partition_spec","title":"<code>get_partition_spec(tensor_shape)</code>","text":"<p>Get partition spec for tensor parallel sharding.</p>"},{"location":"api/mesh/#substrax.mesh.TensorParallelStrategy.get_linear_weight_spec","title":"<code>get_linear_weight_spec()</code>","text":"<p>Get partition spec for linear layer weights.</p>"},{"location":"api/mesh/#substrax.mesh.TensorParallelStrategy.get_attention_qkv_spec","title":"<code>get_attention_qkv_spec()</code>","text":"<p>Get partition spec for attention QKV projections.</p>"},{"location":"api/mesh/#substrax.mesh.TensorParallelStrategy.get_attention_output_spec","title":"<code>get_attention_output_spec()</code>","text":"<p>Get partition spec for attention output projection.</p>"},{"location":"api/mesh/#substrax.mesh.TensorParallelStrategy.apply_sharding","title":"<code>apply_sharding(array, mesh)</code>","text":"<p>Apply tensor parallel sharding to array.</p>"},{"location":"api/mesh/#substrax.mesh.create_named_sharding","title":"<code>create_named_sharding(mesh, *axis_names)</code>","text":"<p>Create a NamedSharding from a mesh and axis names.</p> <p>Parameters:</p> Name Type Description Default <code>mesh</code> <code>Mesh</code> <p>The device mesh.</p> required <code>*axis_names</code> <code>str | None</code> <p>Axis names for each dimension. Use None for replicated dimensions. If no axes are provided, creates a fully replicated sharding.</p> <code>()</code> <p>Returns:</p> Type Description <code>NamedSharding</code> <p>A JAX NamedSharding object.</p>"},{"location":"api/mesh/#substrax.mesh.data_parallel_rules","title":"<code>data_parallel_rules(data_axis='data')</code>","text":"<p>Create MeshRules for pure data-parallel training.</p> <p>Shards only the batch dimension across devices.</p> <p>Parameters:</p> Name Type Description Default <code>data_axis</code> <code>str</code> <p>Name of the mesh axis for data parallelism.</p> <code>'data'</code> <p>Returns:</p> Type Description <code>MeshRules</code> <p>MeshRules with only the data axis mapped.</p>"},{"location":"api/mesh/#substrax.mesh.fsdp_rules","title":"<code>fsdp_rules(data_axis='data', model_axis='model')</code>","text":"<p>Create MeshRules for Fully Sharded Data Parallel (FSDP) training.</p> <p>Shards the batch dimension across the data axis and model weight dimensions across the model axis.</p> <p>Parameters:</p> Name Type Description Default <code>data_axis</code> <code>str</code> <p>Name of the mesh axis for data parallelism.</p> <code>'data'</code> <code>model_axis</code> <code>str</code> <p>Name of the mesh axis for model parallelism.</p> <code>'model'</code> <p>Returns:</p> Type Description <code>MeshRules</code> <p>MeshRules with data and model axes mapped.</p>"},{"location":"api/mesh/#substrax.mesh.partition_spec_for_names","title":"<code>partition_spec_for_names(rules, *logical_names)</code>","text":"<p>Apply MeshRules to create a PartitionSpec for a tensor.</p> <p>Maps logical dimension names through the rules to produce physical mesh axis assignments.</p> <p>Parameters:</p> Name Type Description Default <code>rules</code> <code>MeshRules</code> <p>The MeshRules mapping to apply.</p> required <code>*logical_names</code> <code>str</code> <p>Logical dimension names for each tensor axis.</p> <code>()</code> <p>Returns:</p> Type Description <code>PartitionSpec</code> <p>A PartitionSpec with physical mesh axis assignments.</p>"},{"location":"api/rng/","title":"Rng","text":"<p><code>substrax.rng</code> takes keys from an explicit owner and derives streams from a seed by name. Nothing in it falls back to a default seed: two callers never share a key they did not ask for.</p> <pre><code>from flax import nnx\n\nfrom substrax.rng import fold_in_name, key_from, rngs_from_seed, split_key\n\nrngs = rngs_from_seed(42, streams=(\"params\", \"dropout\", \"sample\"))\n\n# A sampler accepts an Rngs with a `sample` stream, or a key given directly.\nkey = key_from(rngs, streams=(\"sample\", \"default\"), context=\"sampling\")\nper_walker = split_key(key, 1024)\n\n# A sub-component derives its own key from a name, the same in every interpreter.\nnoise_key = fold_in_name(key, \"observation_noise\")\n</code></pre> Function Behaviour <code>key_from(rng, *, streams, context)</code> With an <code>nnx.Rngs</code>, calls the first of <code>streams</code> it holds, in the order given; a typed key or a raw <code>uint32</code> key of shape <code>(2,)</code> is returned as given. A missing stream raises <code>MissingRngStreamError</code> naming <code>context</code> and the streams; <code>None</code>, a Python integer or any other array raises <code>TypeError</code> <code>rngs_from_seed(seed, streams=(\"default\",))</code> One stream per name, each the seed's key folded by the name, so adding or reordering streams leaves the others unchanged <code>split_key(key, num)</code> <code>jax.random.split</code> with <code>num</code> at least one; asking for no keys is a defect <code>fold_in_name(key, name)</code> Folds in the name's BLAKE2b digest truncated to 32 bits, stable across interpreters whatever <code>PYTHONHASHSEED</code> is <p><code>rngs_from_seed</code> differs from splitting the seed key into one part per stream, which every package used to do: there, appending a stream moved every existing stream's key, so a recorded run stopped reproducing when a model gained a <code>dropout</code> stream.</p> <p>The pytest plugin (<code>substrax.testing.pytest_plugin</code>) provides <code>rng_key</code>, a typed key from seed 42, and <code>rngs</code>, an <code>nnx.Rngs</code> from the same seed with <code>default</code>, <code>params</code>, <code>dropout</code> and <code>sample</code> streams.</p>"},{"location":"api/rng/#substrax.rng","title":"<code>substrax.rng</code>","text":"<p>Keys from an explicit owner, streams derived from a seed by name, splits and named folds.</p> <p><code>key_from</code> takes a key from an <code>nnx.Rngs</code> stream or a key given directly and never falls back to a default seed; <code>rngs_from_seed</code> derives each stream from the seed and the stream's name, so streams are independent of each other; <code>split_key</code> and <code>fold_in_name</code> are the two derivations every package needs, with a digest that is stable across interpreters.</p>"},{"location":"api/rng/#substrax.rng.MissingRngStreamError","title":"<code>MissingRngStreamError(context, streams)</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>An <code>nnx.Rngs</code> holds none of the streams a call accepts.</p> <p>Attributes:</p> Name Type Description <code>context</code> <code>str</code> <p>What needed the key, as the caller named it.</p> <code>streams</code> <code>tuple[str, ...]</code> <p>The stream names the caller accepts, in order of preference.</p> <p>Name what needed the key and the streams it accepts.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>str</code> <p>What needed the key, as the caller named it.</p> required <code>streams</code> <code>Sequence[str]</code> <p>The stream names the caller accepts, in order of preference.</p> required"},{"location":"api/rng/#substrax.rng.fold_in_name","title":"<code>fold_in_name(key, name)</code>","text":"<p>Fold a name into <code>key</code> through a stable digest.</p> <p>The name's BLAKE2b digest, truncated to 32 bits, is folded in, so the derived key is the same in every interpreter, whatever <code>PYTHONHASHSEED</code> is; Python's <code>hash</code> is salted per process and is never used.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Array</code> <p>The key to derive from.</p> required <code>name</code> <code>str</code> <p>A non-empty name, such as a stream's.</p> required <p>Returns:</p> Type Description <code>Array</code> <p>The derived key.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>name</code> is empty.</p>"},{"location":"api/rng/#substrax.rng.key_from","title":"<code>key_from(rng, *, streams, context)</code>","text":"<p>Return a key from an explicit owner: an <code>nnx.Rngs</code> stream or a key given directly.</p> <p>With an <code>nnx.Rngs</code>, the first of <code>streams</code> it holds is called, in the order given. A typed key (<code>jax.random.key</code>) or a raw <code>uint32</code> key of shape <code>(2,)</code> is returned as given. There is no fallback: a missing stream, <code>None</code>, a Python integer or any other array raises, so two callers never share a key drawn from a default seed.</p> <p>Parameters:</p> Name Type Description Default <code>rng</code> <code>Array | Rngs</code> <p>The key's owner.</p> required <code>streams</code> <code>Sequence[str]</code> <p>Stream names accepted, most preferred first.</p> required <code>context</code> <code>str</code> <p>What needs the key, named in the errors (<code>\"sampling\"</code>, <code>\"dropout\"</code>).</p> required <p>Returns:</p> Type Description <code>Array</code> <p>The key.</p> <p>Raises:</p> Type Description <code>MissingRngStreamError</code> <p>If <code>rng</code> is an <code>nnx.Rngs</code> holding none of <code>streams</code>.</p> <code>TypeError</code> <p>If <code>rng</code> is neither an <code>nnx.Rngs</code> nor a single key.</p>"},{"location":"api/rng/#substrax.rng.rngs_from_seed","title":"<code>rngs_from_seed(seed, streams=('default',))</code>","text":"<p>Build an <code>nnx.Rngs</code> whose every stream is the seed's key folded by the stream's name.</p> <p>Each stream depends on the seed and its own name only, so adding or reordering streams leaves the others' keys unchanged, which a split of the seed key into <code>len(streams)</code> parts does not.</p> <p>Parameters:</p> Name Type Description Default <code>seed</code> <code>int</code> <p>A non-negative integer.</p> required <code>streams</code> <code>Sequence[str]</code> <p>Distinct stream names; at least one.</p> <code>('default',)</code> <p>Returns:</p> Type Description <code>Rngs</code> <p>The <code>nnx.Rngs</code> with one stream per name.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If <code>seed</code> is not an integer.</p> <code>ValueError</code> <p>If <code>seed</code> is negative, or <code>streams</code> is empty or repeats a name.</p>"},{"location":"api/rng/#substrax.rng.split_key","title":"<code>split_key(key, num)</code>","text":"<p>Split <code>key</code> into <code>num</code> keys along a new leading axis.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Array</code> <p>The key to split.</p> required <code>num</code> <code>int</code> <p>How many keys to return; at least one, so a caller asking for none is a defect.</p> required <p>Returns:</p> Type Description <code>Array</code> <p>Keys of shape <code>(num,)</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>num</code> is below one.</p>"},{"location":"api/runtime/","title":"Runtime","text":"<p><code>substrax.runtime</code> declares the settings a JAX process starts with and applies them at the point where they still take effect.</p> <pre><code>import os\nimport subprocess\nimport sys\n\nfrom substrax.runtime import JaxRuntime, apply_runtime, runtime_environment\n\nruntime = JaxRuntime(platforms=(\"cpu\",), cpu_devices=8, enable_x64=True)\n\n# A child process on eight emulated CPU devices.\nenv = {**os.environ, **runtime_environment(runtime, os.environ)}\nsubprocess.run([sys.executable, \"train.py\"], env=env, check=True)\n\n# This process, before it imports jax.\napply_runtime(runtime)\n\nimport jax\n\nassert jax.device_count() == 8\n</code></pre> <p><code>runtime_environment</code> renders each field as the variable jax or XLA reads when it starts:</p> Field Variable <code>platforms</code> <code>JAX_PLATFORMS</code> <code>cpu_devices</code> <code>JAX_NUM_CPU_DEVICES</code>, which jax prefers over <code>--xla_force_host_platform_device_count</code> <code>enable_x64</code> <code>JAX_ENABLE_X64</code> <code>matmul_precision</code> <code>JAX_DEFAULT_MATMUL_PRECISION</code> <code>compilation_cache_dir</code> <code>JAX_COMPILATION_CACHE_DIR</code> <code>xla_flags</code> <code>XLA_FLAGS</code>, merged by flag name <code>preallocate</code> <code>XLA_PYTHON_CLIENT_PREALLOCATE</code> <code>memory_fraction</code> <code>XLA_CLIENT_MEM_FRACTION</code> <p>A requested XLA flag that is already set to a different value raises <code>XlaFlagConflictError</code>, so a device count or GPU flag exported by the caller is never silently replaced. A memory fraction raises while the deprecated <code>XLA_PYTHON_CLIENT_MEM_FRACTION</code> is still set, because jax refuses both at once. <code>JaxRuntime</code> applies no per-backend flag presets: <code>xla_flags</code> is empty unless a run names the flags it needs.</p> <p>Once jax is imported, <code>apply_runtime</code> applies the CPU device count, 64-bit types, matmul precision and compilation cache directory through <code>jax.config</code>; jax refuses a new device count after its first operation, and that refusal is raised as <code>RuntimeConfigurationError</code>. Platforms, XLA flags and accelerator memory settings are read only when jax starts its backends, and jax gives no public signal of whether that has happened, so after the import they raise instead of being written where they may change nothing.</p> <p>A test suite chooses its backend and emulated CPU devices before anything imports jax:</p> <pre><code># conftest.py\nimport importlib.util\nimport os\n\nfrom substrax.runtime import resolve_test_runtime, runtime_environment\n\nruntime = resolve_test_runtime(\n    os.environ,\n    prefix=\"DATARAX_TEST_\",\n    cuda_plugin_available=importlib.util.find_spec(\"jax_cuda12_plugin\") is not None,\n)\nos.environ.update(runtime_environment(runtime, os.environ))\n</code></pre> <p>Tests run on eight emulated CPU devices unless <code>DATARAX_TEST_JAX_PLATFORMS</code> asks for an accelerator or <code>DATARAX_TEST_DEVICE_COUNT</code> names another count; an inherited <code>JAX_PLATFORMS</code> does not move them onto a GPU.</p> <p>Entry points configure logging inside <code>main()</code>, never at import:</p> <pre><code>from substrax.runtime import configure_entry_point_logging\n\n\ndef main() -&gt; None:\n    configure_entry_point_logging()\n</code></pre> <p>A second call replaces only the handler the first one installed, so it needs no <code>force=True</code> and leaves pytest's log capture in place. Importing <code>substrax.runtime</code> imports no jax and changes no process state.</p>"},{"location":"api/runtime/#substrax.runtime","title":"<code>substrax.runtime</code>","text":"<p>JAX process configuration, declared once and applied before or after jax is imported.</p> <p><code>JaxRuntime</code> describes the settings. <code>runtime_environment</code> renders them as the environment of a process that has not imported jax yet (a child process, or a conftest before its first <code>import jax</code>), <code>apply_runtime</code> applies them to the current process, and <code>resolve_test_runtime</code> chooses the backend and emulated CPU devices of a test run. <code>configure_entry_point_logging</code> sets up the root logger from an entry point. Importing this package imports no jax and changes no process state.</p>"},{"location":"api/runtime/#substrax.runtime.JaxRuntime","title":"<code>JaxRuntime(*, platforms=None, cpu_devices=None, enable_x64=None, matmul_precision=None, compilation_cache_dir=None, xla_flags=(), preallocate=None, memory_fraction=None)</code>  <code>dataclass</code>","text":"<p>Declarative JAX process settings; <code>None</code> leaves the process's current value alone.</p> <p>:func:<code>~substrax.runtime.runtime_environment</code> renders the settings as the environment variables jax and XLA read when a process starts, and :func:<code>~substrax.runtime.apply_runtime</code> applies them to the current process.</p> <p>Attributes:</p> Name Type Description <code>platforms</code> <code>tuple[str, ...] | None</code> <p>The backends jax initialises, the first being the default (<code>JAX_PLATFORMS</code>).</p> <code>cpu_devices</code> <code>int | None</code> <p>The number of CPU devices (<code>JAX_NUM_CPU_DEVICES</code>). jax prefers it over the <code>--xla_force_host_platform_device_count</code> flag.</p> <code>enable_x64</code> <code>bool | None</code> <p>Whether 64-bit types are enabled (<code>JAX_ENABLE_X64</code>).</p> <code>matmul_precision</code> <code>MatmulPrecision | None</code> <p>The default matrix-multiplication precision (<code>JAX_DEFAULT_MATMUL_PRECISION</code>).</p> <code>compilation_cache_dir</code> <code>Path | None</code> <p>The absolute directory of the persistent compilation cache (<code>JAX_COMPILATION_CACHE_DIR</code>).</p> <code>xla_flags</code> <code>tuple[str, ...]</code> <p>Flags merged into <code>XLA_FLAGS</code> by name, each <code>--name</code> or <code>--name=value</code>.</p> <code>preallocate</code> <code>bool | None</code> <p>Whether the accelerator client preallocates its memory (<code>XLA_PYTHON_CLIENT_PREALLOCATE</code>).</p> <code>memory_fraction</code> <code>float | None</code> <p>The fraction of accelerator memory the client may use, in <code>(0, 1]</code> (<code>XLA_CLIENT_MEM_FRACTION</code>).</p>"},{"location":"api/runtime/#substrax.runtime.JaxRuntime.__post_init__","title":"<code>__post_init__()</code>","text":"<p>Validate every field.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If a field holds a value of the wrong type.</p> <code>ValueError</code> <p>If a field holds a value outside its domain.</p> <code>XlaFlagConflictError</code> <p>If one requested flag is given two different values.</p>"},{"location":"api/runtime/#substrax.runtime.RuntimeConfigurationError","title":"<code>RuntimeConfigurationError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>A JAX process setting cannot be applied, or conflicts with one already in place.</p>"},{"location":"api/runtime/#substrax.runtime.XlaFlagConflictError","title":"<code>XlaFlagConflictError(flag, existing, requested)</code>","text":"<p>               Bases: <code>RuntimeConfigurationError</code></p> <p>One XLA flag was given two different values.</p> <p>Attributes:</p> Name Type Description <code>flag</code> <code>str</code> <p>The flag name, with its leading dashes.</p> <code>existing</code> <code>str | None</code> <p>The value already in place; <code>None</code> for a flag given without a value.</p> <code>requested</code> <code>str | None</code> <p>The conflicting value; <code>None</code> for a flag given without a value.</p> <p>Record the flag and both of its values.</p> <p>Parameters:</p> Name Type Description Default <code>flag</code> <code>str</code> <p>The flag name, with its leading dashes.</p> required <code>existing</code> <code>str | None</code> <p>The value already in place.</p> required <code>requested</code> <code>str | None</code> <p>The conflicting value.</p> required"},{"location":"api/runtime/#substrax.runtime.apply_runtime","title":"<code>apply_runtime(runtime, *, environ=None)</code>","text":"<p>Apply <code>runtime</code> to the current process.</p> <p>Before jax is imported, every field is written to <code>environ</code> as the variables jax and XLA read at start-up (see :func:<code>~substrax.runtime.runtime_environment</code>). Once jax is imported, the CPU device count, 64-bit types, matmul precision and compilation cache directory go through <code>jax.config.update</code>; jax refuses a new device count once its backends have started. Platforms, XLA flags and client memory settings are read only when the backends start, and jax offers no public signal of whether that has happened, so once jax is imported they raise instead of being written where they might change nothing.</p> <p>Parameters:</p> Name Type Description Default <code>runtime</code> <code>JaxRuntime</code> <p>The settings to apply.</p> required <code>environ</code> <code>MutableMapping[str, str] | None</code> <p>The environment written before jax is imported; <code>os.environ</code> when <code>None</code>.</p> <code>None</code> <p>Raises:</p> Type Description <code>RuntimeConfigurationError</code> <p>If jax is imported and <code>runtime</code> sets a platform, an XLA flag or a client memory setting, or jax refuses the CPU device count.</p> <code>XlaFlagConflictError</code> <p>If a requested flag has a different value in the current <code>XLA_FLAGS</code>.</p>"},{"location":"api/runtime/#substrax.runtime.configure_entry_point_logging","title":"<code>configure_entry_point_logging(level=logging.INFO, *, stream=None, fmt='%(message)s')</code>","text":"<p>Send log records to <code>stream</code> without replacing any other handler.</p> <p>Call it inside <code>main()</code>, never at import. A second call replaces only the handler the first one installed and leaves every other handler, pytest's log capture included, in place, so it needs no <code>force=True</code>.</p> <p>Parameters:</p> Name Type Description Default <code>level</code> <code>int</code> <p>The level set on the root logger.</p> <code>INFO</code> <code>stream</code> <code>TextIO | None</code> <p>Where records are written; standard error when <code>None</code>.</p> <code>None</code> <code>fmt</code> <code>str</code> <p>The <code>logging.Formatter</code> format string.</p> <code>'%(message)s'</code>"},{"location":"api/runtime/#substrax.runtime.resolve_test_runtime","title":"<code>resolve_test_runtime(env, *, prefix, cuda_plugin_available, default_cpu_devices=8)</code>","text":"<p>Choose the backend and the emulated CPU devices of a test run.</p> <p>Tests run on the CPU unless <code>{prefix}JAX_PLATFORMS</code> names an accelerator, which is then used without CPU emulation. An inherited <code>JAX_PLATFORMS</code>, such as the <code>cuda,cpu</code> a developer shell exports, does not move tests onto an accelerator, so a local run matches CI. The CPU device count is <code>{prefix}DEVICE_COUNT</code> when set (<code>0</code> for no emulation), otherwise the count <code>JAX_NUM_CPU_DEVICES</code> or the XLA device-count flag already names, otherwise <code>default_cpu_devices</code>.</p> <p>Parameters:</p> Name Type Description Default <code>env</code> <code>Mapping[str, str]</code> <p>The process environment.</p> required <code>prefix</code> <code>str</code> <p>The prefix of the repository's test variables, such as <code>\"DATARAX_TEST_\"</code>.</p> required <code>cuda_plugin_available</code> <code>bool</code> <p>Whether a JAX CUDA plugin is installed.</p> required <code>default_cpu_devices</code> <code>int</code> <p>The emulated device count when nothing else names one.</p> <code>8</code> <p>Returns:</p> Type Description <code>JaxRuntime</code> <p>The runtime of the test process.</p> <p>Raises:</p> Type Description <code>RuntimeConfigurationError</code> <p>If CUDA is requested and no JAX CUDA plugin is installed.</p> <code>ValueError</code> <p>If <code>{prefix}DEVICE_COUNT</code> is not a non-negative integer.</p>"},{"location":"api/runtime/#substrax.runtime.runtime_environment","title":"<code>runtime_environment(runtime, base)</code>","text":"<p>Return the environment variables that apply <code>runtime</code> to a process not yet running jax.</p> <p>Only the variables <code>runtime</code> sets are returned, and <code>base</code> is read, never changed. A field replaces the variable <code>base</code> holds, except <code>xla_flags</code>, which merges into <code>base</code>'s <code>XLA_FLAGS</code> by flag name. Pass the result to a child process on top of its environment, or write it to <code>os.environ</code> before importing jax.</p> <p>Parameters:</p> Name Type Description Default <code>runtime</code> <code>JaxRuntime</code> <p>The settings to render.</p> required <code>base</code> <code>Mapping[str, str]</code> <p>The environment the variables will be added to.</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>Variable names mapped to their values.</p> <p>Raises:</p> Type Description <code>RuntimeConfigurationError</code> <p>If <code>runtime</code> sets a memory fraction and <code>base</code> still holds the deprecated <code>XLA_PYTHON_CLIENT_MEM_FRACTION</code>, which jax refuses beside the current name.</p> <code>XlaFlagConflictError</code> <p>If a requested flag has a different value in <code>base</code>'s <code>XLA_FLAGS</code>.</p>"},{"location":"api/runtime/#substrax.runtime.merge_xla_flags","title":"<code>merge_xla_flags(existing, requested)</code>","text":"<p>Merge requested flags into an <code>XLA_FLAGS</code> value by flag name.</p> <p>Flags already present keep their position, and new flags follow in the order requested. A requested flag already set to the same value changes nothing, and one set to a different value raises, so a value already in the environment is never silently replaced.</p> <p>Parameters:</p> Name Type Description Default <code>existing</code> <code>str</code> <p>The current <code>XLA_FLAGS</code> value.</p> required <code>requested</code> <code>Iterable[str]</code> <p>Flags to add, each <code>--name</code> or <code>--name=value</code>.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The merged <code>XLA_FLAGS</code> value, with flags separated by single spaces.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If a token is not a flag.</p> <code>XlaFlagConflictError</code> <p>If a requested flag has a different value from an existing one.</p>"},{"location":"api/runtime/#substrax.runtime.parse_xla_flags","title":"<code>parse_xla_flags(value)</code>","text":"<p>Parse an <code>XLA_FLAGS</code> value into flag names mapped to their values, in order.</p> <p>Flags are separated by whitespace. <code>--name=value</code> maps <code>--name</code> to <code>value</code>, and a bare <code>--name</code> maps it to <code>None</code>. A flag given twice with the same value counts once.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>str</code> <p>The <code>XLA_FLAGS</code> string.</p> required <p>Returns:</p> Type Description <code>dict[str, str | None]</code> <p>Each flag name, with its leading dashes, mapped to its value.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If a token is not a flag.</p> <code>XlaFlagConflictError</code> <p>If one flag is given two different values.</p>"},{"location":"api/spmd/","title":"SPMD","text":"<p><code>substrax.spmd</code> is the data-parallel training path on top of <code>substrax.mesh</code>: place a batch on the data axis, run one step, reduce metrics across devices.</p> <pre><code>import jax\nfrom flax import nnx\n\nfrom substrax.mesh import DeviceMeshManager\nfrom substrax.spmd import create_data_parallel_sharding, place_batch_on_shards, spmd_train_step\n\nmesh = DeviceMeshManager.create_device_mesh({\"data\": jax.device_count()})\nsharding = create_data_parallel_sharding(mesh)\n\n\n@nnx.jit\ndef train_step(model, optimizer, batch):\n    return spmd_train_step(model, optimizer, loss_fn, batch)\n\n\nwith jax.set_mesh(mesh):\n    loss = train_step(model, optimizer, place_batch_on_shards(batch, sharding))\n</code></pre> <p><code>spmd_train_step</code> differentiates with <code>nnx.value_and_grad</code> and updates the <code>nnx.Optimizer</code>; XLA inserts the gradient all-reduce when the parameters are sharded under <code>jax.set_mesh</code>. The collectives (<code>reduce_mean</code>, <code>reduce_sum</code>, <code>all_gather</code>, <code>collect_from_devices</code> and friends) work on metric dictionaries.</p>"},{"location":"api/spmd/#substrax.spmd","title":"<code>substrax.spmd</code>","text":"<p>Data-parallel placement, training steps, gradient reduction and collectives.</p>"},{"location":"api/spmd/#substrax.spmd.all_gather","title":"<code>all_gather(metrics, axis_name='batch')</code>","text":"<p>Gather metrics from all devices.</p> <p>Only valid inside a pmap or shard_map context.</p> <p>Parameters:</p> Name Type Description Default <code>metrics</code> <code>dict[str, Any]</code> <p>The metrics to gather.</p> required <code>axis_name</code> <code>str</code> <p>The name of the axis to gather across.</p> <code>'batch'</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary of gathered metrics.</p>"},{"location":"api/spmd/#substrax.spmd.collect_from_devices","title":"<code>collect_from_devices(metrics)</code>","text":"<p>Collect metrics from all devices.</p> <p>Call outside of a pmapped function to split per-device values from the leading device axis.</p> <p>Parameters:</p> Name Type Description Default <code>metrics</code> <code>dict[str, Any]</code> <p>The metrics from all devices, with the first dimension corresponding to the device axis.</p> required <p>Returns:</p> Type Description <code>dict[str, list[Any] | Any]</code> <p>A dictionary of metrics, with array values split into per-device lists.</p>"},{"location":"api/spmd/#substrax.spmd.reduce_custom","title":"<code>reduce_custom(metrics, reduce_fn=None)</code>","text":"<p>Apply custom reduction operations to metrics.</p> <p>Uses standard JAX operations. Works in SPMD contexts.</p> <p>Parameters:</p> Name Type Description Default <code>metrics</code> <code>dict[str, Any]</code> <p>The metrics to reduce.</p> required <code>reduce_fn</code> <code>dict[str, str | None] | None</code> <p>A dictionary mapping metric names to reduction operations. Each operation should be one of {\"mean\", \"sum\", \"max\", \"min\"}. If None, defaults to \"mean\" for all metrics.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary of reduced metrics.</p>"},{"location":"api/spmd/#substrax.spmd.reduce_max","title":"<code>reduce_max(metrics)</code>","text":"<p>Compute the maximum of metrics using standard JAX operations.</p> <p>Works with global arrays in SPMD contexts (nnx.jit + mesh).</p> <p>Parameters:</p> Name Type Description Default <code>metrics</code> <code>dict[str, Any]</code> <p>The metrics to reduce.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary of maximum metrics.</p>"},{"location":"api/spmd/#substrax.spmd.reduce_mean","title":"<code>reduce_mean(metrics)</code>","text":"<p>Compute the mean of metrics using standard JAX operations.</p> <p>Works with global arrays in SPMD contexts (nnx.jit + mesh).</p> <p>Parameters:</p> Name Type Description Default <code>metrics</code> <code>dict[str, Any]</code> <p>The metrics to reduce.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary of mean-reduced metrics.</p>"},{"location":"api/spmd/#substrax.spmd.reduce_mean_collective","title":"<code>reduce_mean_collective(metrics, axis_name='batch')</code>","text":"<p>Compute the mean of metrics using collective operations.</p> <p>Only valid inside a pmap or shard_map context.</p> <p>Parameters:</p> Name Type Description Default <code>metrics</code> <code>dict[str, Any]</code> <p>The metrics to reduce.</p> required <code>axis_name</code> <code>str</code> <p>The name of the axis to reduce across.</p> <code>'batch'</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary of mean metrics.</p>"},{"location":"api/spmd/#substrax.spmd.reduce_min","title":"<code>reduce_min(metrics)</code>","text":"<p>Compute the minimum of metrics using standard JAX operations.</p> <p>Works with global arrays in SPMD contexts (nnx.jit + mesh).</p> <p>Parameters:</p> Name Type Description Default <code>metrics</code> <code>dict[str, Any]</code> <p>The metrics to reduce.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary of minimum metrics.</p>"},{"location":"api/spmd/#substrax.spmd.reduce_sum","title":"<code>reduce_sum(metrics)</code>","text":"<p>Compute the sum of metrics using standard JAX operations.</p> <p>Works with global arrays in SPMD contexts (nnx.jit + mesh).</p> <p>Parameters:</p> Name Type Description Default <code>metrics</code> <code>dict[str, Any]</code> <p>The metrics to reduce.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary of summed metrics.</p>"},{"location":"api/spmd/#substrax.spmd.reduce_sum_collective","title":"<code>reduce_sum_collective(metrics, axis_name='batch')</code>","text":"<p>Compute the sum of metrics using collective operations.</p> <p>Only valid inside a pmap or shard_map context.</p> <p>Parameters:</p> Name Type Description Default <code>metrics</code> <code>dict[str, Any]</code> <p>The metrics to reduce.</p> required <code>axis_name</code> <code>str</code> <p>The name of the axis to reduce across.</p> <code>'batch'</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary of summed metrics.</p>"},{"location":"api/spmd/#substrax.spmd.create_data_parallel_sharding","title":"<code>create_data_parallel_sharding(mesh, data_axis='data')</code>","text":"<p>Create the sharding that splits a batch's leading axis over a mesh axis.</p> <p>Parameters:</p> Name Type Description Default <code>mesh</code> <code>Mesh</code> <p>The device mesh to use for sharding.</p> required <code>data_axis</code> <code>str</code> <p>The name of the mesh axis to use for data parallelism.</p> <code>'data'</code> <p>Returns:</p> Type Description <code>NamedSharding</code> <p>A <code>NamedSharding</code> over <code>mesh</code> with spec <code>PartitionSpec(data_axis)</code>.</p>"},{"location":"api/spmd/#substrax.spmd.place_batch_on_shards","title":"<code>place_batch_on_shards(batch, sharding)</code>","text":"<p>Turn this process's batch into global arrays on <code>sharding</code>.</p> <p>Array leaves, whether <code>jax.Array</code> or a NumPy array as a host data loader yields them, go to <code>jax.make_array_from_process_local_data</code> in one call. On a single process that is one batched <code>jax.device_put</code>. On several processes each process passes the slice of the global batch it loaded, and jax stitches the slices into one global array. Any other leaf, such as a string, is returned as it is. Call it on the host batches a loader yields, outside <code>jax.jit</code>: inside a traced function the result is not placed on <code>sharding</code>.</p> <p>Parameters:</p> Name Type Description Default <code>batch</code> <code>PyTree</code> <p>The batch this process loaded.</p> required <code>sharding</code> <code>Sharding</code> <p>The sharding of the global batch.</p> required <p>Returns:</p> Type Description <code>PyTree</code> <p>The batch with every array leaf replaced by the global array on <code>sharding</code>.</p>"},{"location":"api/spmd/#substrax.spmd.place_nnx_state_on_shards","title":"<code>place_nnx_state_on_shards(state, mesh, filter_sharding)</code>","text":"<p>Shard a Flax NNX state tree using current NNX sharding helpers.</p>"},{"location":"api/spmd/#substrax.spmd.reduce_gradient_tree","title":"<code>reduce_gradient_tree(gradients, reduce_type='mean')</code>","text":"<p>Reduce gradients using standard JAX operations on global arrays.</p> <p>Works in SPMD contexts (inside nnx.jit with mesh). The XLA compiler handles cross-device communication automatically.</p> <p>Parameters:</p> Name Type Description Default <code>gradients</code> <code>Any</code> <p>The gradients to reduce (global sharded arrays).</p> required <code>reduce_type</code> <code>str</code> <p>The type of reduction (\"mean\" or \"sum\").</p> <code>'mean'</code> <p>Returns:</p> Type Description <code>Any</code> <p>The reduced gradients.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If reduce_type is not \"mean\" or \"sum\".</p>"},{"location":"api/spmd/#substrax.spmd.spmd_train_step","title":"<code>spmd_train_step(model, optimizer, loss_fn, batch)</code>","text":"<p>Execute a data-parallel training step using SPMD.</p> <p>Uses nnx.value_and_grad for automatic differentiation. The XLA compiler handles gradient AllReduce automatically when model parameters are sharded across devices via jax.set_mesh or explicit NamedSharding.</p> <p>This function should be called inside an @nnx.jit decorated function with a mesh context active.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The NNX model to train.</p> required <code>optimizer</code> <code>Optimizer[Any]</code> <p>The NNX optimizer wrapping the model.</p> required <code>loss_fn</code> <code>Callable[[Module, PyTree], Array]</code> <p>Function (model, batch) -&gt; loss scalar.</p> required <code>batch</code> <code>PyTree</code> <p>The training batch (should be pre-sharded).</p> required <p>Returns:</p> Type Description <code>Array</code> <p>The loss value.</p> Example <p>mesh = DeviceMeshManager.create_device_mesh({\"data\": 4}) rules = data_parallel_rules()</p> <p>@nnx.jit def train_step(model, optimizer, batch):     return spmd_train_step(model, optimizer, my_loss_fn, batch)</p> <p>with jax.set_mesh(mesh):     loss = train_step(model, optimizer, batch)</p>"},{"location":"api/testing/","title":"Testing","text":"<p><code>substrax.testing</code> holds the test infrastructure JAX packages share: fresh-interpreter runs with a chosen JAX configuration, trace counts, example runs, and an opt-in pytest plugin. Install it with <code>substrax[testing]</code>.</p> <pre><code>from substrax.runtime import JaxRuntime\nfrom substrax.testing import run_python\n\nresult = run_python(\n    \"import json, jax; print(json.dumps(jax.device_count()))\",\n    runtime=JaxRuntime(cpu_devices=8),\n    timeout=120,\n)\nassert result.check().last_json() == 8\n</code></pre> <p><code>run_python</code> runs this interpreter on code or a script. The child's environment drops every <code>JAX_*</code> and <code>XLA_*</code> variable the parent inherited, then applies <code>env</code> and the <code>runtime</code>. Unless either of them chooses otherwise, the child runs on the CPU backend without preallocation, so a test never starts an accelerator it did not ask for. <code>check()</code> raises <code>ChildFailedError</code> with the end of the child's standard error, and <code>last_json()</code> parses the last line the child printed. <code>cuda_is_visible()</code> asks a child on the CUDA backend whether jax sees a GPU.</p>"},{"location":"api/testing/#counting-traces","title":"Counting traces","text":"<p><code>TraceCounter</code> counts how often a function's Python body runs. Under <code>jax.jit</code> and <code>nnx.jit</code> the body runs once per trace, so the count is the number of traces a test caused:</p> <pre><code>import jax\nimport jax.numpy as jnp\n\nfrom substrax.testing import TraceCounter\n\n\ndef double(x):\n    return x * 2.0\n\n\ncounter = TraceCounter()\nstep = jax.jit(counter.wrap(double))\n\nwith counter.expect(new_traces=1):\n    step(jnp.ones(3))\nwith counter.expect(new_traces=0):  # same shape and dtype: no new trace\n    step(jnp.zeros(3))\n</code></pre> <p><code>expect</code> raises <code>RetraceError</code>, an <code>AssertionError</code> that names the expected and observed counts, and an error raised inside the block propagates unchanged. Each counter keeps its own count, so there is no global registry to clear. Wrap the Python function, then jit the wrapper. The counter does not listen to jax's <code>jax.monitoring</code> trace event: jax records that event only for top-level traces, and its name is not documented.</p>"},{"location":"api/testing/#running-examples","title":"Running examples","text":"<p><code>discover_examples</code> lists a repository's example scripts, and <code>run_example</code> runs one in a fresh interpreter from the repository root:</p> <pre><code>from pathlib import Path\n\nimport pytest\n\nfrom substrax.testing import discover_examples, run_example\n\nROOT = Path(__file__).resolve().parents[2]\nEXAMPLES = discover_examples(ROOT / \"examples\")\n\n\n@pytest.mark.parametrize(\"path\", EXAMPLES, ids=lambda path: path.relative_to(ROOT).as_posix())\ndef test_example_runs(path: Path, output_dir: Path) -&gt; None:\n    run = run_example(\n        path, repo_root=ROOT, output_dir=output_dir, timeout=600, call_main=True\n    )\n    assert run.result.check().returncode == 0\n    assert run.summary[\"relative_l2_error\"] &lt; 1e-2\n</code></pre> <ul> <li><code>discover_examples</code> skips every file or directory whose name starts with <code>_</code>, such as   <code>__init__.py</code>, <code>_common</code> helpers and <code>_templates</code>. <code>include</code> narrows the list further, for   example to a numbering rule.</li> <li><code>call_main=True</code> loads the example under a name other than <code>__main__</code>, calls <code>main()</code> and decodes   its return value from JSON into <code>run.summary</code>. 0-d numpy and jax values become Python numbers,   and a value JSON cannot hold fails the run, naming its key. <code>call_main=False</code> runs the file as   a script. In both modes the example's own directory comes first on <code>sys.path</code>, and it sees no   command-line arguments, as in a script run.</li> <li>The child runs through <code>run_python</code>, with <code>AVITAI_OUTPUT_DIR</code> set to <code>output_dir</code>. An example that   resolves its outputs with <code>resolve_output_dir</code> never writes into the repository, and logging or   64-bit settings made at import end with the child.</li> <li>A timeout raises <code>ExampleTimeoutError</code>, an <code>AssertionError</code> naming the budget and the end of the   example's standard error. A timeout is a failure, not a skip.</li> <li><code>unavailable_reason(run, signatures)</code> returns the first of a repository's messages found in a   failed run's standard error, for skipping an example whose dataset is not configured here.</li> </ul>"},{"location":"api/testing/#the-pytest-plugin","title":"The pytest plugin","text":"<p>Enable it in the top-level <code>conftest.py</code>:</p> <pre><code>pytest_plugins = [\"substrax.testing.pytest_plugin\"]\n</code></pre> Provides Behaviour <code>@pytest.mark.x64</code> Runs the test inside <code>jax.enable_x64(True)</code>, a thread-local setting Configuration isolation Snapshots <code>jax.config.values</code> around every test and fails a test that changed a global value, after setting it back Collection guard Fails the session when a module left x64 on at import although <code>JAX_ENABLE_X64</code> did not ask for it <code>@pytest.mark.devices(count, kind=None)</code> Skips unless at least <code>count</code> devices, of <code>kind</code> when given, are visible <code>@pytest.mark.accelerator(kind=None)</code> Skips unless jax's default backend is an accelerator, of <code>kind</code> when given <code>output_dir</code> fixture A fresh <code>outputs</code> directory under <code>tmp_path</code>, with <code>AVITAI_OUTPUT_DIR</code> pointing at it for the test <p>The isolation follows jax's own test harness, whose <code>JaxTestCase</code> fails a test that changes a global configuration value. A test whose subject changes the global configuration sets it back with <code>restored_jax_config()</code>, which reports the names it restored:</p> <pre><code>import jax\n\nfrom substrax.runtime import JaxRuntime, apply_runtime\nfrom substrax.testing import restored_jax_config\n\n\ndef test_apply_runtime_enables_x64() -&gt; None:\n    with restored_jax_config() as changed:\n        apply_runtime(JaxRuntime(enable_x64=True))\n        assert jax.config.jax_enable_x64\n\n    assert changed == [\"jax_enable_x64\"]\n</code></pre> <p>A malformed <code>devices</code> or <code>accelerator</code> marker is a usage error at collection. No entry point loads the plugin, so installing substrax changes no other project's tests, and importing it imports no jax.</p>"},{"location":"api/testing/#substrax.testing","title":"<code>substrax.testing</code>","text":"<p>Test helpers for JAX packages: fresh-interpreter runs and an opt-in pytest plugin.</p> <p><code>run_python</code> runs code or a script in a child interpreter whose JAX settings the test chooses, with everything the parent's shell exported about jax removed, and returns a <code>ChildResult</code>. <code>cuda_is_visible</code> probes the CUDA backend in such a child. <code>restored_jax_config</code> sets back the global jax configuration a block changed. <code>TraceCounter</code> counts the traces a jitted function takes and raises <code>RetraceError</code> when a block caused an unexpected number. <code>discover_examples</code> lists a repository's examples, and <code>run_example</code> runs one per child interpreter with its outputs redirected. The plugin, <code>substrax.testing.pytest_plugin</code>, is enabled from a <code>conftest.py</code>: it adds the <code>x64</code>, <code>devices</code> and <code>accelerator</code> markers and fails a test that changes jax's global configuration, as jax's own test harness does. Importing this package imports no jax.</p>"},{"location":"api/testing/#substrax.testing.ChildFailedError","title":"<code>ChildFailedError(result)</code>","text":"<p>               Bases: <code>AssertionError</code></p> <p>A child interpreter exited with a non-zero code.</p> <p>Attributes:</p> Name Type Description <code>result</code> <code>ChildResult</code> <p>The failed run.</p> <p>Describe the failed run by its exit code and the end of its standard error.</p> <p>Parameters:</p> Name Type Description Default <code>result</code> <code>ChildResult</code> <p>The failed run.</p> required"},{"location":"api/testing/#substrax.testing.ChildResult","title":"<code>ChildResult(*, argv, returncode, stdout, stderr, seconds)</code>  <code>dataclass</code>","text":"<p>What a child interpreter did.</p> <p>Attributes:</p> Name Type Description <code>argv</code> <code>tuple[str, ...]</code> <p>The command line the child ran.</p> <code>returncode</code> <code>int</code> <p>Its exit code.</p> <code>stdout</code> <code>str</code> <p>Everything it wrote to standard output.</p> <code>stderr</code> <code>str</code> <p>Everything it wrote to standard error.</p> <code>seconds</code> <code>float</code> <p>Wall-clock seconds from start to exit.</p>"},{"location":"api/testing/#substrax.testing.ChildResult.check","title":"<code>check()</code>","text":"<p>Return this result when the child exited with code 0.</p> <p>Returns:</p> Type Description <code>ChildResult</code> <p>This result.</p> <p>Raises:</p> Type Description <code>ChildFailedError</code> <p>If the exit code is not 0; the message ends with the last lines of standard error.</p>"},{"location":"api/testing/#substrax.testing.ChildResult.last_json","title":"<code>last_json()</code>","text":"<p>Parse the last non-empty line of standard output as JSON.</p> <p>Returns:</p> Type Description <code>Any</code> <p>The decoded value.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the child wrote no output, or its last line is not JSON.</p>"},{"location":"api/testing/#substrax.testing.ExampleRun","title":"<code>ExampleRun(*, path, result, summary)</code>  <code>dataclass</code>","text":"<p>One run of an example.</p> <p>Attributes:</p> Name Type Description <code>path</code> <code>Path</code> <p>The example that ran.</p> <code>result</code> <code>ChildResult</code> <p>The child interpreter's exit code, output and duration.</p> <code>summary</code> <code>Any</code> <p>What <code>main()</code> returned, decoded from JSON, for a run that called it and exited 0; <code>None</code> otherwise.</p>"},{"location":"api/testing/#substrax.testing.ExampleTimeoutError","title":"<code>ExampleTimeoutError(path, timeout, stderr)</code>","text":"<p>               Bases: <code>AssertionError</code></p> <p>An example ran past its budget and was killed.</p> <p>Attributes:</p> Name Type Description <code>path</code> <code>Path</code> <p>The example.</p> <code>timeout</code> <code>float</code> <p>Its budget in seconds.</p> <code>stderr_tail</code> <code>str</code> <p>The last lines it wrote to standard error before it was killed.</p> <p>Name the example, its budget and the end of its standard error.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>Path</code> <p>The example.</p> required <code>timeout</code> <code>float</code> <p>Its budget in seconds.</p> required <code>stderr</code> <code>str</code> <p>Everything it wrote to standard error before it was killed.</p> required"},{"location":"api/testing/#substrax.testing.RetraceError","title":"<code>RetraceError(expected, observed)</code>","text":"<p>               Bases: <code>AssertionError</code></p> <p>A block caused a different number of traces than the test expected.</p> <p>Attributes:</p> Name Type Description <code>expected</code> <code>int</code> <p>The number of new traces the test expected.</p> <code>observed</code> <code>int</code> <p>The number of new traces the block caused.</p> <p>Record both counts and name them in the message.</p> <p>Parameters:</p> Name Type Description Default <code>expected</code> <code>int</code> <p>The number of new traces the test expected.</p> required <code>observed</code> <code>int</code> <p>The number of new traces the block caused.</p> required"},{"location":"api/testing/#substrax.testing.TraceCounter","title":"<code>TraceCounter()</code>","text":"<p>Count how often wrapped functions run their Python body.</p> <p><code>jax.jit</code> runs a function's body once per trace and reuses the cached trace for later calls with the same static signature, and <code>nnx.jit</code> calls the user function once inside the function jax traces. The count of body executions is therefore the count of traces. An eager call also runs the body and also counts.</p> <p>Each counter keeps its own count, so tests share no state to clear.</p> <p>Start at zero.</p>"},{"location":"api/testing/#substrax.testing.TraceCounter.count","title":"<code>count</code>  <code>property</code>","text":"<p>The number of body executions of every function this counter wrapped.</p>"},{"location":"api/testing/#substrax.testing.TraceCounter.wrap","title":"<code>wrap(function)</code>","text":"<p>Return <code>function</code> counting each run of its body; jit the result, not the original.</p> <p>The wrapper keeps the function's name, docstring and signature, so <code>static_argnames</code> and other signature-based arguments of a transform still resolve.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[P, R]</code> <p>The Python function to count.</p> required <p>Returns:</p> Type Description <code>Callable[P, R]</code> <p>A function that adds one to the count, then calls <code>function</code>. The count rises</p> <code>Callable[P, R]</code> <p>before the call, so a trace that raises still counts.</p>"},{"location":"api/testing/#substrax.testing.TraceCounter.expect","title":"<code>expect(*, new_traces)</code>","text":"<p>Require exactly <code>new_traces</code> body executions inside the block.</p> <p>An exception raised inside the block propagates unchanged, without a count check.</p> <p>Parameters:</p> Name Type Description Default <code>new_traces</code> <code>int</code> <p>How many new traces the block must cause.</p> required <p>Yields:</p> Name Type Description <code>None</code> <code>None</code> <p>Control to the block.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>new_traces</code> is negative.</p> <code>RetraceError</code> <p>If the block caused a different number of traces.</p>"},{"location":"api/testing/#substrax.testing.cuda_is_visible","title":"<code>cuda_is_visible(*, timeout=120.0)</code>","text":"<p>Whether a fresh interpreter can start jax's CUDA backend and sees at least one GPU.</p> <p>The probe runs in a child without preallocation, so it claims no accelerator memory.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>Seconds before the probe is abandoned.</p> <code>120.0</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when the child found a GPU.</p>"},{"location":"api/testing/#substrax.testing.run_python","title":"<code>run_python(program, *args, timeout, runtime=None, env=None, cwd=None)</code>","text":"<p>Run Python code or a script in a fresh interpreter and return what it did.</p> <p>jax fixes its backends and device set when they start, so tests of device topology or process configuration need their own interpreter. The child runs this interpreter (<code>sys.executable</code>). Its environment is this process's without any <code>JAX_*</code> or <code>XLA_*</code> variable, so settings exported in a developer's shell cannot change what a test measures, then <code>env</code>, then <code>runtime</code> as :func:<code>~substrax.runtime.runtime_environment</code> renders it. Unless <code>runtime</code> or <code>env</code> chooses them, the child runs on the CPU backend without preallocation, so a test never starts an accelerator it did not ask for.</p> <p>Parameters:</p> Name Type Description Default <code>program</code> <code>str | Path</code> <p>Code to run with <code>-c</code>, or the path of a script.</p> required <code>*args</code> <code>str</code> <p>Arguments passed to the program.</p> <code>()</code> <code>timeout</code> <code>float</code> <p>Seconds before the child is killed; every run states its budget.</p> required <code>runtime</code> <code>JaxRuntime | None</code> <p>JAX settings of the child.</p> <code>None</code> <code>env</code> <code>Mapping[str, str] | None</code> <p>Variables set in the child before <code>runtime</code> is applied.</p> <code>None</code> <code>cwd</code> <code>Path | None</code> <p>The child's working directory; this process's when <code>None</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>ChildResult</code> <p>The child's exit code, output and duration. A non-zero exit is returned, not raised.</p> <p>Raises:</p> Type Description <code>XlaFlagConflictError</code> <p>If a flag in <code>runtime</code> conflicts with <code>XLA_FLAGS</code> in <code>env</code>; the child is not started.</p> <code>TimeoutExpired</code> <p>If the child runs longer than <code>timeout</code>.</p>"},{"location":"api/testing/#substrax.testing.discover_examples","title":"<code>discover_examples(root, *, include=None)</code>","text":"<p>List the example scripts under <code>root</code>, sorted.</p> <p>A file or directory whose name starts with <code>_</code> is private, as <code>_common</code> helpers, <code>_templates</code> and <code>__init__.py</code> are, and so is everything under it. Only the parts below <code>root</code> count, so <code>root</code> itself may have such a name.</p> <p>Parameters:</p> Name Type Description Default <code>root</code> <code>Path</code> <p>The examples directory.</p> required <code>include</code> <code>Callable[[Path], bool] | None</code> <p>Keeps a path when it returns <code>True</code>, such as a repository's numbering rule.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[Path]</code> <p>The <code>*.py</code> files under <code>root</code> that are not private and that <code>include</code> keeps.</p>"},{"location":"api/testing/#substrax.testing.run_example","title":"<code>run_example(path, *, repo_root, output_dir, timeout, call_main, runtime=None, env=None)</code>","text":"<p>Run one example in a fresh interpreter from the repository root, with outputs redirected.</p> <p>The child starts through :func:<code>~substrax.testing.run_python</code>, so the JAX settings are the <code>runtime</code> and <code>env</code> given here, not the parent shell's. <code>AVITAI_OUTPUT_DIR</code> is set to <code>output_dir</code>, so an example resolving its outputs with :func:<code>~substrax.artifacts.resolve_output_dir</code> never writes into the repository. Whatever the example configures at import, such as logging or 64-bit types, ends with the child.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>Path</code> <p>The example.</p> required <code>repo_root</code> <code>Path</code> <p>The child's working directory, which relative paths in examples assume.</p> required <code>output_dir</code> <code>Path</code> <p>Where the example's outputs go.</p> required <code>timeout</code> <code>float</code> <p>Seconds before the child is killed; every run states its budget.</p> required <code>call_main</code> <code>bool</code> <p><code>True</code> loads the example under a name other than <code>__main__</code>, calls its <code>main()</code> without arguments and decodes the return value; <code>False</code> runs the file as a script.</p> required <code>runtime</code> <code>JaxRuntime | None</code> <p>JAX settings of the child.</p> <code>None</code> <code>env</code> <code>Mapping[str, str] | None</code> <p>Variables set in the child before <code>runtime</code> is applied.</p> <code>None</code> <p>Returns:</p> Type Description <code>ExampleRun</code> <p>The run. A non-zero exit is returned, not raised; <code>run.result.check()</code> raises it.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>env</code> sets <code>AVITAI_OUTPUT_DIR</code> to a directory other than <code>output_dir</code>.</p> <code>ExampleTimeoutError</code> <p>If the example runs longer than <code>timeout</code>.</p> <code>XlaFlagConflictError</code> <p>If a flag in <code>runtime</code> conflicts with <code>XLA_FLAGS</code> in <code>env</code>; the child is not started.</p>"},{"location":"api/testing/#substrax.testing.unavailable_reason","title":"<code>unavailable_reason(run, signatures)</code>","text":"<p>Return the first signature found in a failed run's standard error, ignoring case.</p> <p>A repository lists the messages that mean an example cannot run in this environment, such as a dataset that is not configured, and skips the example when one matches. A run that exited 0 matches nothing, and standard output is not searched.</p> <p>Parameters:</p> Name Type Description Default <code>run</code> <code>ExampleRun</code> <p>The finished run.</p> required <code>signatures</code> <code>Sequence[str]</code> <p>Messages that mean the example cannot run here.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>The first signature found, as given, or <code>None</code>.</p>"},{"location":"api/testing/#substrax.testing.restored_jax_config","title":"<code>restored_jax_config()</code>","text":"<p>Set back every jax configuration value a block changed, and report which ones.</p> <p>jax's own test base class snapshots <code>jax.config.values</code> around each test and fails a test that changed a global value (<code>assert_global_configs_unchanged</code> in <code>jax/_src/test_util.py</code>). This context manager takes the same snapshot and, on exit, sets every value that differs back through <code>jax.config.update</code>, whether the block finished or raised.</p> <p>A value read inside a thread-local context manager such as <code>jax.enable_x64(True)</code> is that context's, so a context entered and left inside the block changes nothing.</p> <p>Yields:</p> Type Description <code>list[str]</code> <p>list[str]: Empty while the block runs; on exit it holds the sorted names of the values the</p> <code>list[str]</code> <p>block changed, all of them restored.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If jax refuses to set a value back, as it refuses <code>jax_num_cpu_devices</code> once its backends have started.</p>"},{"location":"api/tracking/","title":"Tracking","text":"<p><code>substrax.tracking</code> records scalars, images, histograms, text and hyperparameters per step, to the console, to files, to Weights &amp; Biases or to MLflow, behind one <code>Logger</code> interface.</p> <pre><code>from substrax.tracking import FileLogger, WandbLogger\n\nlogger = FileLogger(\"run-1\", log_dir=\"logs/run-1\")\nlogger.log_scalars({\"loss\": 0.42, \"accuracy\": 0.9}, step=100)\nlogger.log_image(\"samples\", images, step=100)\nlogger.log_hyperparams({\"learning_rate\": 1e-3})\nlogger.close()\n\nwandb_logger = WandbLogger(\"run-1\", project=\"my-project\")\n</code></pre> Backend Extra Where things go <code>ConsoleLogger</code> none <code>logging</code> lines on stdout <code>FileLogger</code> <code>plots</code> for figures Lines to a <code>.log</code> file, scalars to a long-form CSV (<code>timestamp,step,name,value</code>), images and histograms to PNGs, texts and hyperparameters to text files <code>WandbLogger</code> <code>wandb</code> A W&amp;B run; <code>log_code</code> and <code>log_model</code> upload code and model artifacts <code>MLFlowLogger</code> <code>mlflow</code>, <code>plots</code> for figures An MLflow run; images, histograms and texts as run artifacts <p>Importing <code>substrax.tracking</code> imports neither SDK. A backend loads its SDK when constructed and raises <code>ImportError</code> naming the extra to install:</p> <pre><code>uv add 'substrax[wandb]'\n</code></pre>"},{"location":"api/tracking/#substrax.tracking","title":"<code>substrax.tracking</code>","text":"<p>Step-wise experiment tracking with console, file, Weights &amp; Biases and MLflow backends.</p> <p>Importing this package imports neither <code>wandb</code> nor <code>mlflow</code>; each backend loads its SDK when constructed and raises <code>ImportError</code> naming the extra that provides it.</p>"},{"location":"api/tracking/#substrax.tracking.ConsoleLogger","title":"<code>ConsoleLogger(name, log_dir=None, level=logging.INFO)</code>","text":"<p>               Bases: <code>Logger</code></p> <p>Logger that writes lines to the console only.</p>"},{"location":"api/tracking/#substrax.tracking.ConsoleLogger.log_scalars","title":"<code>log_scalars(scalars, step=None)</code>","text":"<p>Log several scalars as one line.</p>"},{"location":"api/tracking/#substrax.tracking.ConsoleLogger.log_image","title":"<code>log_image(name, image, step=None)</code>","text":"<p>Log the image count or shape, not the pixels.</p>"},{"location":"api/tracking/#substrax.tracking.ConsoleLogger.log_histogram","title":"<code>log_histogram(name, values, step=None)</code>","text":"<p>Log the summary statistics of <code>values</code>, not the histogram.</p>"},{"location":"api/tracking/#substrax.tracking.ConsoleLogger.log_text","title":"<code>log_text(name, text, step=None)</code>","text":"<p>Log <code>text</code> under <code>name</code>.</p>"},{"location":"api/tracking/#substrax.tracking.ConsoleLogger.log_hyperparams","title":"<code>log_hyperparams(params)</code>","text":"<p>Log one line per hyperparameter.</p>"},{"location":"api/tracking/#substrax.tracking.FileLogger","title":"<code>FileLogger(name, log_dir, filename=None, level=logging.INFO)</code>","text":"<p>               Bases: <code>ConsoleLogger</code></p> <p>Logger that writes the console lines to a file as well.</p> <p>Scalars also go to <code>&lt;name&gt;_&lt;timestamp&gt;_metrics.csv</code> (one <code>timestamp, step, name, value</code> row per scalar), images to <code>images/</code>, histograms to <code>histograms/</code>, texts to <code>texts/</code> and hyperparameters to <code>hyperparams_&lt;timestamp&gt;.txt</code>. Images and histograms need the <code>plots</code> extra; without it they are logged as lines only.</p> <p>Initialise the file logger.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the logger.</p> required <code>log_dir</code> <code>str | Path</code> <p>Directory to save logs to.</p> required <code>filename</code> <code>str | None</code> <p>Log filename. <code>None</code> derives one from <code>name</code> and the time.</p> <code>None</code> <code>level</code> <code>int</code> <p>Logging level.</p> <code>INFO</code>"},{"location":"api/tracking/#substrax.tracking.FileLogger.log_scalars","title":"<code>log_scalars(scalars, step=None)</code>","text":"<p>Log several scalars as one line and append them to the metrics CSV.</p>"},{"location":"api/tracking/#substrax.tracking.FileLogger.log_image","title":"<code>log_image(name, image, step=None)</code>","text":"<p>Log the image and save it under <code>images/</code>.</p>"},{"location":"api/tracking/#substrax.tracking.FileLogger.log_histogram","title":"<code>log_histogram(name, values, step=None)</code>","text":"<p>Log the summary statistics and save the histogram under <code>histograms/</code>.</p>"},{"location":"api/tracking/#substrax.tracking.FileLogger.log_text","title":"<code>log_text(name, text, step=None)</code>","text":"<p>Log <code>text</code> and save it under <code>texts/</code>.</p>"},{"location":"api/tracking/#substrax.tracking.FileLogger.log_hyperparams","title":"<code>log_hyperparams(params)</code>","text":"<p>Log the hyperparameters and save them to <code>hyperparams_&lt;timestamp&gt;.txt</code>.</p>"},{"location":"api/tracking/#substrax.tracking.Logger","title":"<code>Logger(name, log_dir=None, level=logging.INFO)</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Base class for all loggers.</p> <p>Concrete loggers record scalars, images, histograms, text and hyperparameters somewhere; every logger also owns a :mod:<code>logging</code> logger named after it whose handlers it replaces, so lines reach stdout exactly once.</p> <p>Initialise the logger.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the logger and of its :mod:<code>logging</code> logger.</p> required <code>log_dir</code> <code>str | Path | None</code> <p>Directory for files, created if missing. <code>None</code> writes no files.</p> <code>None</code> <code>level</code> <code>int</code> <p>Logging level from the :mod:<code>logging</code> module.</p> <code>INFO</code>"},{"location":"api/tracking/#substrax.tracking.Logger.log","title":"<code>log(msg, level=logging.INFO)</code>","text":"<p>Log a message at <code>level</code>.</p>"},{"location":"api/tracking/#substrax.tracking.Logger.debug","title":"<code>debug(msg)</code>","text":"<p>Log a debug message.</p>"},{"location":"api/tracking/#substrax.tracking.Logger.info","title":"<code>info(msg)</code>","text":"<p>Log an info message.</p>"},{"location":"api/tracking/#substrax.tracking.Logger.warning","title":"<code>warning(msg)</code>","text":"<p>Log a warning message.</p>"},{"location":"api/tracking/#substrax.tracking.Logger.error","title":"<code>error(msg)</code>","text":"<p>Log an error message.</p>"},{"location":"api/tracking/#substrax.tracking.Logger.critical","title":"<code>critical(msg)</code>","text":"<p>Log a critical message.</p>"},{"location":"api/tracking/#substrax.tracking.Logger.log_scalar","title":"<code>log_scalar(name, value, step=None)</code>","text":"<p>Log one scalar; the default records it through :meth:<code>log_scalars</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the scalar.</p> required <code>value</code> <code>float</code> <p>Scalar value to log.</p> required <code>step</code> <code>int | None</code> <p>Global step value to record, for example the training iteration.</p> <code>None</code>"},{"location":"api/tracking/#substrax.tracking.Logger.log_scalars","title":"<code>log_scalars(scalars, step=None)</code>  <code>abstractmethod</code>","text":"<p>Log several scalars.</p> <p>Parameters:</p> Name Type Description Default <code>scalars</code> <code>Mapping[str, float]</code> <p>Scalar names to values.</p> required <code>step</code> <code>int | None</code> <p>Global step value to record.</p> <code>None</code>"},{"location":"api/tracking/#substrax.tracking.Logger.log_image","title":"<code>log_image(name, image, step=None)</code>  <code>abstractmethod</code>","text":"<p>Log an image or a list of images.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the image.</p> required <code>image</code> <code>ArrayLike | Sequence[ArrayLike]</code> <p>One array of shape <code>(H, W)</code> or <code>(H, W, C)</code>, or a list of them.</p> required <code>step</code> <code>int | None</code> <p>Global step value to record.</p> <code>None</code>"},{"location":"api/tracking/#substrax.tracking.Logger.log_histogram","title":"<code>log_histogram(name, values, step=None)</code>  <code>abstractmethod</code>","text":"<p>Log a histogram of values.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the histogram.</p> required <code>values</code> <code>ArrayLike | Sequence[float]</code> <p>Values to build the histogram from.</p> required <code>step</code> <code>int | None</code> <p>Global step value to record.</p> <code>None</code>"},{"location":"api/tracking/#substrax.tracking.Logger.log_text","title":"<code>log_text(name, text, step=None)</code>  <code>abstractmethod</code>","text":"<p>Log text.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the text entry.</p> required <code>text</code> <code>str</code> <p>Text to log.</p> required <code>step</code> <code>int | None</code> <p>Global step value to record.</p> <code>None</code>"},{"location":"api/tracking/#substrax.tracking.Logger.log_hyperparams","title":"<code>log_hyperparams(params)</code>  <code>abstractmethod</code>","text":"<p>Log hyperparameters.</p> <p>Parameters:</p> Name Type Description Default <code>params</code> <code>Mapping[str, Any]</code> <p>Hyperparameter names to values.</p> required"},{"location":"api/tracking/#substrax.tracking.Logger.close","title":"<code>close()</code>","text":"<p>Close the logger's handlers and release their resources.</p>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger","title":"<code>MLFlowLogger(name, *, log_dir=None, experiment_name=None, run_name=None, run_id=None, tracking_uri=None, registry_uri=None, level=logging.INFO)</code>","text":"<p>               Bases: <code>Logger</code></p> <p>Logger that records metrics, parameters and artifacts in an MLflow run.</p> <p>Images, histograms and texts are written to <code>&lt;log_dir&gt;/artifacts/&lt;kind&gt;/</code> when a log directory is given, else to a temporary file, and logged as run artifacts under <code>&lt;kind&gt;</code>. Images and histograms need the <code>plots</code> extra.</p> <p>Start or resume the MLflow run.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the logger; also the experiment name when none is given.</p> required <code>log_dir</code> <code>str | Path | None</code> <p>Local directory for artifacts, in addition to the run.</p> <code>None</code> <code>experiment_name</code> <code>str | None</code> <p>MLflow experiment, created if missing.</p> <code>None</code> <code>run_name</code> <code>str | None</code> <p>Name for a new run. MLflow generates one when omitted.</p> <code>None</code> <code>run_id</code> <code>str | None</code> <p>An existing run to continue; <code>experiment_name</code> and <code>run_name</code> are then ignored.</p> <code>None</code> <code>tracking_uri</code> <code>str | None</code> <p>MLflow tracking server. <code>None</code> keeps the environment's.</p> <code>None</code> <code>registry_uri</code> <code>str | None</code> <p>MLflow model registry. <code>None</code> keeps the tracking URI.</p> <code>None</code> <code>level</code> <code>int</code> <p>Logging level.</p> <code>INFO</code> <p>Constructing the logger raises <code>ImportError</code> when the <code>mlflow</code> extra is not installed.</p>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.log_scalar","title":"<code>log_scalar(name, value, step=None)</code>","text":"<p>Log one metric.</p>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.log_scalars","title":"<code>log_scalars(scalars, step=None)</code>","text":"<p>Log several metrics.</p>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.log_image","title":"<code>log_image(name, image, step=None)</code>","text":"<p>Log an image or a list of images as an artifact under <code>images</code>.</p>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.log_histogram","title":"<code>log_histogram(name, values, step=None)</code>","text":"<p>Log the summary statistics as metrics and the histogram as an artifact.</p>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.log_text","title":"<code>log_text(name, text, step=None)</code>","text":"<p>Log <code>text</code> as an artifact under <code>texts</code>.</p>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.log_hyperparams","title":"<code>log_hyperparams(params)</code>","text":"<p>Log the hyperparameters as run parameters.</p> <p>MLflow accepts strings, numbers and booleans; anything else is logged as its <code>str</code> representation.</p>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.log_artifact","title":"<code>log_artifact(local_path, artifact_path=None)</code>","text":"<p>Log one file as a run artifact.</p> <p>Parameters:</p> Name Type Description Default <code>local_path</code> <code>str | Path</code> <p>File to log.</p> required <code>artifact_path</code> <code>str | None</code> <p>Directory within the run's artifacts to place it in.</p> <code>None</code>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.log_artifacts","title":"<code>log_artifacts(local_dir, artifact_path=None)</code>","text":"<p>Log every file in a directory as run artifacts.</p> <p>Parameters:</p> Name Type Description Default <code>local_dir</code> <code>str | Path</code> <p>Directory whose files to log.</p> required <code>artifact_path</code> <code>str | None</code> <p>Directory within the run's artifacts to place them in.</p> <code>None</code>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.end_run","title":"<code>end_run()</code>","text":"<p>End the run; a second call is a no-op.</p>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.close","title":"<code>close()</code>","text":"<p>End the run and close the logger.</p>"},{"location":"api/tracking/#substrax.tracking.WandbLogger","title":"<code>WandbLogger(name, project, *, entity=None, log_dir=None, config=None, tags=None, notes=None, level=logging.INFO, **init_options)</code>","text":"<p>               Bases: <code>Logger</code></p> <p>Logger that records metrics, media and artifacts in a Weights &amp; Biases run.</p> <p>Start the W&amp;B run.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the run and of the logger.</p> required <code>project</code> <code>str</code> <p>Name of the W&amp;B project.</p> required <code>entity</code> <code>str | None</code> <p>User or team that owns the project.</p> <code>None</code> <code>log_dir</code> <code>str | Path | None</code> <p>Local directory for W&amp;B's own files, in addition to the run.</p> <code>None</code> <code>config</code> <code>Mapping[str, Any] | None</code> <p>Hyperparameters to record on the run.</p> <code>None</code> <code>tags</code> <code>Sequence[str] | None</code> <p>Tags for the run.</p> <code>None</code> <code>notes</code> <code>str | None</code> <p>Notes about the run.</p> <code>None</code> <code>level</code> <code>int</code> <p>Logging level.</p> <code>INFO</code> <code>**init_options</code> <code>Any</code> <p>Further keyword arguments for <code>wandb.init</code>, such as <code>mode=\"offline\"</code> or <code>resume=\"allow\"</code>; the SDK owns their meaning.</p> <code>{}</code> <p>Constructing the logger raises <code>ImportError</code> when the <code>wandb</code> extra is not installed.</p>"},{"location":"api/tracking/#substrax.tracking.WandbLogger.log_scalars","title":"<code>log_scalars(scalars, step=None)</code>","text":"<p>Log several scalars to the run.</p>"},{"location":"api/tracking/#substrax.tracking.WandbLogger.log_image","title":"<code>log_image(name, image, step=None)</code>","text":"<p>Log an image or a list of images to the run.</p>"},{"location":"api/tracking/#substrax.tracking.WandbLogger.log_histogram","title":"<code>log_histogram(name, values, step=None)</code>","text":"<p>Log the histogram and its summary statistics (<code>&lt;name&gt;/min</code> and so on).</p>"},{"location":"api/tracking/#substrax.tracking.WandbLogger.log_text","title":"<code>log_text(name, text, step=None)</code>","text":"<p>Log <code>text</code> as a preformatted HTML block.</p>"},{"location":"api/tracking/#substrax.tracking.WandbLogger.log_hyperparams","title":"<code>log_hyperparams(params)</code>","text":"<p>Record the hyperparameters on the run's config.</p>"},{"location":"api/tracking/#substrax.tracking.WandbLogger.log_code","title":"<code>log_code(root=None)</code>","text":"<p>Save the code under <code>root</code> (default: the current directory) to the run.</p>"},{"location":"api/tracking/#substrax.tracking.WandbLogger.log_model","title":"<code>log_model(model_path, name=None, metadata=None)</code>","text":"<p>Upload a model file or directory as a W&amp;B artifact of type <code>model</code>.</p> <p>Parameters:</p> Name Type Description Default <code>model_path</code> <code>str | Path</code> <p>File or directory to upload.</p> required <code>name</code> <code>str | None</code> <p>Artifact name; defaults to <code>&lt;run name&gt;_model</code>.</p> <code>None</code> <code>metadata</code> <code>Mapping[str, Any] | None</code> <p>Metadata to record on the artifact.</p> <code>None</code>"},{"location":"api/tracking/#substrax.tracking.WandbLogger.finish","title":"<code>finish(exit_code=0)</code>","text":"<p>Finish the run; a second call is a no-op.</p> <p>Parameters:</p> Name Type Description Default <code>exit_code</code> <code>int</code> <p>Exit code to report to W&amp;B.</p> <code>0</code>"},{"location":"api/tracking/#substrax.tracking.WandbLogger.close","title":"<code>close()</code>","text":"<p>Finish the run and close the logger.</p>"},{"location":"api/tracking/#substrax.tracking.create_logger","title":"<code>create_logger(name, log_dir=None, *, log_to_file=True, level=logging.INFO)</code>","text":"<p>Create a console logger, or a file logger that also logs to the console.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the logger.</p> required <code>log_dir</code> <code>str | Path | None</code> <p>Directory to save logs to. <code>None</code> with <code>log_to_file</code> means <code>./logs/&lt;name&gt;</code>.</p> <code>None</code> <code>log_to_file</code> <code>bool</code> <p>Whether to write files as well as console lines.</p> <code>True</code> <code>level</code> <code>int</code> <p>Logging level.</p> <code>INFO</code> <p>Returns:</p> Name Type Description <code>A</code> <code>Logger</code> <p>class:<code>FileLogger</code> when <code>log_to_file</code> is set, else a :class:<code>ConsoleLogger</code>.</p>"}]}