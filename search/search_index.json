{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Substrax","text":"<p>Shared training and hardware infrastructure for the Avitai JAX stack. Substrax sits at the bottom of the dependency chain \u2014 <code>substrax \u2192 calibrax \u2192 datarax \u2192 artifex \u2192 opifex</code> \u2014 and holds the code those packages used to carry separately.</p> Subpackage What it owns <code>substrax.runtime</code> JAX process settings applied before or after jax is imported, XLA flags merged by name, test-run device emulation, entry-point logging <code>substrax.artifacts</code> Output directories from an argument, <code>AVITAI_OUTPUT_DIR</code> or a per-run temporary directory, never the working tree <code>substrax.testing</code> Opt-in test infrastructure: fresh-interpreter runs with a chosen JAX configuration, and a pytest plugin with device markers and jax configuration isolation <code>substrax.devices</code> Device information and device placement <code>substrax.mesh</code> Device meshes, mesh rules, partition-spec helpers, sharding strategies <code>substrax.spmd</code> Data-parallel placement, <code>spmd_train_step</code>, gradient reduction, collectives <code>substrax.checkpoint</code> The <code>CheckpointStore</code> protocol and its Orbax implementation <code>substrax.callbacks</code> Training callbacks, <code>BestMetricTracker</code>, <code>EarlyStopping</code> <code>substrax.tracking</code> Step-wise experiment tracking backends <code>substrax.typing</code>, <code>substrax.records</code> The shared type aliases (<code>PyTree</code>, <code>JsonValue</code>) and typed reading of JSON records <p>Each subpackage has an API page.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>uv add substrax\n</code></pre> <p>Python 3.12 or later, <code>jax&gt;=0.11.1</code>, <code>flax&gt;=0.12.9</code>, <code>orbax-checkpoint&gt;=0.11.33</code>, <code>pydantic&gt;=2.10</code>.</p>"},{"location":"api/artifacts/","title":"Artifacts","text":"<p><code>substrax.artifacts</code> decides where a run writes its figures, tables and other outputs, and never defaults into the working tree.</p> <pre><code>import argparse\nfrom pathlib import Path\n\nfrom substrax.artifacts import resolve_output_dir\n\n\ndef main() -&gt; None:\n    parser = argparse.ArgumentParser()\n    parser.add_argument(\"--output-dir\", type=Path, default=None)\n    args = parser.parse_args()\n\n    location = resolve_output_dir(\"fno_darcy\", explicit=args.output_dir)\n    figure_path = location.path / \"prediction.png\"\n</code></pre> <p><code>resolve_output_dir</code> takes the first of these that applies and creates the directory:</p> Order Directory <code>source</code> 1 <code>explicit</code>, resolved against the working directory when it is relative <code>\"argument\"</code> 2 <code>$AVITAI_OUTPUT_DIR/&lt;name&gt;</code>; the variable must hold an absolute path <code>\"environment\"</code> 3 <code>&lt;name&gt;</code> inside a directory created once per process under the system temporary directory <code>\"run_default\"</code> <p>Writing figures into the documentation is an explicit choice: <code>python examples/neural-operators/fno_darcy.py --output-dir docs/assets/examples/fno_darcy</code>. A test run or a CI job sets <code>AVITAI_OUTPUT_DIR</code> instead, so running an example never rewrites tracked files.</p> <p>The per-run directory is created with <code>tempfile.mkdtemp</code>, so its name cannot be claimed by another user of a shared temporary directory and only its owner can read it. Every default output in one process shares it. <code>name</code> must be a relative path that stays inside the output directory. Importing the package creates nothing.</p>"},{"location":"api/artifacts/#substrax.artifacts","title":"<code>substrax.artifacts</code>","text":"<p>Output directories that never default into the working tree.</p> <p><code>resolve_output_dir</code> picks the directory a run writes to: an explicit argument, then <code>AVITAI_OUTPUT_DIR</code>, then a directory created once per process under the system temporary directory. Importing this package creates nothing.</p>"},{"location":"api/artifacts/#substrax.artifacts.OutputLocation","title":"<code>OutputLocation(*, path, source)</code>  <code>dataclass</code>","text":"<p>A resolved output directory and where the choice came from.</p> <p>Attributes:</p> Name Type Description <code>path</code> <code>Path</code> <p>The absolute directory, which exists.</p> <code>source</code> <code>Literal['argument', 'environment', 'run_default']</code> <p><code>\"argument\"</code> for a directory the caller passed, <code>\"environment\"</code> for one under <code>AVITAI_OUTPUT_DIR</code>, <code>\"run_default\"</code> for one under the per-run directory.</p>"},{"location":"api/artifacts/#substrax.artifacts.resolve_output_dir","title":"<code>resolve_output_dir(name, *, explicit=None, env=None)</code>","text":"<p>Return the directory a run writes the output called <code>name</code> to, creating it.</p> <p>The directory is <code>explicit</code> when given, with a relative path resolved against the working directory. Otherwise it is <code>$AVITAI_OUTPUT_DIR/&lt;name&gt;</code>, and otherwise <code>&lt;name&gt;</code> inside a directory created once per process under the system temporary directory. It never defaults into the working tree, so running an example or a test cannot overwrite tracked files, and writing into <code>docs/assets</code> is always an explicit choice.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The output's relative path, such as <code>\"fno_darcy\"</code>.</p> required <code>explicit</code> <code>Path | None</code> <p>A directory chosen by the caller, typically from an <code>--output-dir</code> option.</p> <code>None</code> <code>env</code> <code>Mapping[str, str] | None</code> <p>The environment <code>AVITAI_OUTPUT_DIR</code> is read from; <code>os.environ</code> when <code>None</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>OutputLocation</code> <p>The created directory and the source it was chosen from.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>name</code> is empty, absolute or leaves the output directory, or <code>AVITAI_OUTPUT_DIR</code> holds a relative path.</p>"},{"location":"api/callbacks/","title":"Callbacks","text":"<p><code>substrax.callbacks</code> holds the training-callback protocol and metric-driven early stopping, in two shapes:</p> <ul> <li><code>EarlyStopping</code> for a loop that feeds it one validation value per epoch and asks   <code>should_stop</code>;</li> <li><code>EarlyStoppingCallback</code> for a trainer that drives <code>TrainingCallback</code> hooks and reads   the metric from the epoch's logs.</li> </ul> <p><code>EarlyStopping</code> keeps its counters in a <code>BestMetricTracker</code>; <code>EarlyStoppingCallback</code> applies an <code>EarlyStopping</code> to the logged metric and adds the goal and divergence thresholds and the non-finite check. <code>patience</code> is at least 1: patience counts epochs without improvement, so an improving epoch never stops training.</p> <pre><code>from substrax.callbacks import EarlyStopping, EarlyStoppingCallback, EarlyStoppingConfig\n\nstopper = EarlyStopping(patience=3, min_delta=1e-3)\nfor epoch in range(epochs):\n    stopper.update(validate())\n    if stopper.should_stop:\n        break\n\ncallback = EarlyStoppingCallback(EarlyStoppingConfig(monitor=\"val_loss\", patience=10))\n</code></pre> <p>Learning-rate decay on a plateau is not here: use <code>optax.contrib.reduce_on_plateau</code>.</p>"},{"location":"api/callbacks/#resuming-a-run","title":"Resuming a run","text":"<p><code>BestMetricTracker</code>, <code>EarlyStopping</code>, <code>EarlyStoppingCallback</code> and <code>CallbackList</code> satisfy <code>substrax.typing.Checkpointable</code>: <code>get_state()</code> returns their bookkeeping as a dictionary a checkpoint store writes, and <code>set_state()</code> on objects built the same way continues where the saved ones stood, so a resumed run stops at the epoch the uninterrupted run would have. <code>CallbackList</code> keeps each stateful callback's state under its position, skips callbacks without state, and refuses a state saved from a list whose stateful callbacks stood elsewhere.</p> <pre><code>from substrax.callbacks import CallbackList\nfrom substrax.checkpoint import OrbaxCheckpointStore\n\ncallbacks = CallbackList([EarlyStoppingCallback(EarlyStoppingConfig(patience=10))])\nwith OrbaxCheckpointStore(directory) as store:\n    store.save(step, {\"extensions\": {\"callbacks\": callbacks.get_state()}})\n    callbacks.set_state(store.restore(step).items[\"extensions\"][\"callbacks\"])\n</code></pre>"},{"location":"api/callbacks/#substrax.callbacks","title":"<code>substrax.callbacks</code>","text":"<p>Training callbacks, best-metric tracking and early stopping.</p>"},{"location":"api/callbacks/#substrax.callbacks.BaseCallback","title":"<code>BaseCallback</code>","text":"<p>No-op implementation of every hook; subclasses override what they need.</p>"},{"location":"api/callbacks/#substrax.callbacks.BaseCallback.on_train_begin","title":"<code>on_train_begin(_trainer)</code>","text":"<p>Called once before the first epoch.</p>"},{"location":"api/callbacks/#substrax.callbacks.BaseCallback.on_train_end","title":"<code>on_train_end(_trainer)</code>","text":"<p>Called once after the last epoch.</p>"},{"location":"api/callbacks/#substrax.callbacks.BaseCallback.on_epoch_begin","title":"<code>on_epoch_begin(_trainer, _epoch)</code>","text":"<p>Called before each epoch.</p>"},{"location":"api/callbacks/#substrax.callbacks.BaseCallback.on_epoch_end","title":"<code>on_epoch_end(_trainer, _epoch, _logs)</code>","text":"<p>Called after each epoch with that epoch's metrics.</p>"},{"location":"api/callbacks/#substrax.callbacks.BaseCallback.on_batch_begin","title":"<code>on_batch_begin(_trainer, _batch)</code>","text":"<p>Called before each batch.</p>"},{"location":"api/callbacks/#substrax.callbacks.BaseCallback.on_batch_end","title":"<code>on_batch_end(_trainer, _batch, _logs)</code>","text":"<p>Called after each batch with that batch's metrics.</p>"},{"location":"api/callbacks/#substrax.callbacks.BaseCallback.on_validation_begin","title":"<code>on_validation_begin(_trainer)</code>","text":"<p>Called before a validation pass.</p>"},{"location":"api/callbacks/#substrax.callbacks.BaseCallback.on_validation_end","title":"<code>on_validation_end(_trainer, _logs)</code>","text":"<p>Called after a validation pass with its metrics.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList","title":"<code>CallbackList(callbacks=None)</code>","text":"<p>Dispatches every hook to its callbacks in registration order.</p> <p>Wrap an initial set of callbacks.</p> <p>Parameters:</p> Name Type Description Default <code>callbacks</code> <code>Iterable[TrainingCallback] | None</code> <p>The callbacks to start with; each receives every hook, in this order.</p> <code>None</code>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.add","title":"<code>add(callback)</code>","text":"<p>Append a callback.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.remove","title":"<code>remove(callback)</code>","text":"<p>Remove a callback; raises <code>ValueError</code> if it was never added.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.__len__","title":"<code>__len__()</code>","text":"<p>The number of callbacks.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.__iter__","title":"<code>__iter__()</code>","text":"<p>Iterate over the callbacks in dispatch order.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.get_state","title":"<code>get_state()</code>","text":"<p>Every stateful callback's state, for a checkpoint.</p> <p>Returns:</p> Name Type Description <code>Each</code> <code>dict[str, Any]</code> <p>class:<code>~substrax.typing.Checkpointable</code> callback's state under its position</p> <code>dict[str, Any]</code> <p>in dispatch order; callbacks without state are skipped.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.set_state","title":"<code>set_state(state)</code>","text":"<p>Take back a state :meth:<code>get_state</code> returned, into a list built the same way.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>dict[str, Any]</code> <p>Each stateful callback's saved state, by position.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If the stateful callbacks do not stand at the saved positions.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.on_train_begin","title":"<code>on_train_begin(trainer)</code>","text":"<p>Dispatch <code>on_train_begin</code>.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.on_train_end","title":"<code>on_train_end(trainer)</code>","text":"<p>Dispatch <code>on_train_end</code>.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.on_epoch_begin","title":"<code>on_epoch_begin(trainer, epoch)</code>","text":"<p>Dispatch <code>on_epoch_begin</code>.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.on_epoch_end","title":"<code>on_epoch_end(trainer, epoch, logs)</code>","text":"<p>Dispatch <code>on_epoch_end</code>.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.on_batch_begin","title":"<code>on_batch_begin(trainer, batch)</code>","text":"<p>Dispatch <code>on_batch_begin</code>.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.on_batch_end","title":"<code>on_batch_end(trainer, batch, logs)</code>","text":"<p>Dispatch <code>on_batch_end</code>.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.on_validation_begin","title":"<code>on_validation_begin(trainer)</code>","text":"<p>Dispatch <code>on_validation_begin</code>.</p>"},{"location":"api/callbacks/#substrax.callbacks.CallbackList.on_validation_end","title":"<code>on_validation_end(trainer, logs)</code>","text":"<p>Dispatch <code>on_validation_end</code>.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainerLike","title":"<code>TrainerLike</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>What a callback may ask of the trainer that drives it.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainerLike.model","title":"<code>model</code>  <code>property</code>","text":"<p>The model being trained.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainingCallback","title":"<code>TrainingCallback</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>Lifecycle hooks a training loop fires; implement any subset via <code>BaseCallback</code>.</p> <p>Parameters are positional-only so implementations may name them as they like.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainingCallback.on_train_begin","title":"<code>on_train_begin(trainer)</code>","text":"<p>Called once before the first epoch.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainingCallback.on_train_end","title":"<code>on_train_end(trainer)</code>","text":"<p>Called once after the last epoch.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainingCallback.on_epoch_begin","title":"<code>on_epoch_begin(trainer, epoch)</code>","text":"<p>Called before each epoch.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainingCallback.on_epoch_end","title":"<code>on_epoch_end(trainer, epoch, logs)</code>","text":"<p>Called after each epoch with that epoch's metrics.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainingCallback.on_batch_begin","title":"<code>on_batch_begin(trainer, batch)</code>","text":"<p>Called before each batch.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainingCallback.on_batch_end","title":"<code>on_batch_end(trainer, batch, logs)</code>","text":"<p>Called after each batch with that batch's metrics.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainingCallback.on_validation_begin","title":"<code>on_validation_begin(trainer)</code>","text":"<p>Called before a validation pass.</p>"},{"location":"api/callbacks/#substrax.callbacks.TrainingCallback.on_validation_end","title":"<code>on_validation_end(trainer, logs)</code>","text":"<p>Called after a validation pass with its metrics.</p>"},{"location":"api/callbacks/#substrax.callbacks.EarlyStoppingCallback","title":"<code>EarlyStoppingCallback(config)</code>","text":"<p>               Bases: <code>BaseCallback</code></p> <p>Stop training when a monitored metric stops improving.</p> <p>Initialize early stopping callback.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>EarlyStoppingConfig</code> <p>Early stopping configuration.</p> required"},{"location":"api/callbacks/#substrax.callbacks.EarlyStoppingCallback.wait_count","title":"<code>wait_count</code>  <code>property</code>","text":"<p>Epochs since the last improvement.</p>"},{"location":"api/callbacks/#substrax.callbacks.EarlyStoppingCallback.best_score","title":"<code>best_score</code>  <code>property</code>","text":"<p>The best monitored value so far, or <code>None</code> before the metric first appears.</p>"},{"location":"api/callbacks/#substrax.callbacks.EarlyStoppingCallback.stopped_epoch","title":"<code>stopped_epoch</code>  <code>property</code>","text":"<p>The epoch at which stopping was decided, if it has been.</p>"},{"location":"api/callbacks/#substrax.callbacks.EarlyStoppingCallback.should_stop","title":"<code>should_stop</code>  <code>property</code>","text":"<p>Whether training should stop.</p>"},{"location":"api/callbacks/#substrax.callbacks.EarlyStoppingCallback.on_epoch_end","title":"<code>on_epoch_end(_trainer, epoch, logs)</code>","text":"<p>Read the monitored metric, if present, and decide whether to stop.</p> <p>Parameters:</p> Name Type Description Default <code>_trainer</code> <code>TrainerLike</code> <p>The trainer instance (unused).</p> required <code>epoch</code> <code>int</code> <p>Current epoch number.</p> required <code>logs</code> <code>dict[str, Any]</code> <p>Dictionary of metrics from this epoch.</p> required"},{"location":"api/callbacks/#substrax.callbacks.EarlyStoppingCallback.get_state","title":"<code>get_state()</code>","text":"<p>The stopper's state and the stopping epoch, for a checkpoint.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p><code>stopper</code> (see :meth:<code>BestMetricTracker.get_state</code>) and <code>stopped_epoch</code></p> <code>dict[str, Any]</code> <p>(<code>None</code> while training continues).</p>"},{"location":"api/callbacks/#substrax.callbacks.EarlyStoppingCallback.set_state","title":"<code>set_state(state)</code>","text":"<p>Take back a state :meth:<code>get_state</code> returned.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>dict[str, Any]</code> <p>The saved stopper state and stopping epoch.</p> required"},{"location":"api/callbacks/#substrax.callbacks.EarlyStoppingConfig","title":"<code>EarlyStoppingConfig(*, monitor='val_loss', min_delta=0.0, patience=10, mode='min', check_finite=True, stopping_threshold=None, divergence_threshold=None)</code>  <code>dataclass</code>","text":"<p>Configuration for early stopping.</p> <p>Attributes:</p> Name Type Description <code>monitor</code> <code>str</code> <p>Metric name to monitor (e.g., \"val_loss\", \"accuracy\").</p> <code>min_delta</code> <code>float</code> <p>Minimum change to qualify as an improvement.</p> <code>patience</code> <code>int</code> <p>Number of epochs with no improvement before stopping.</p> <code>mode</code> <code>Literal['min', 'max']</code> <p>\"min\" if lower is better, \"max\" if higher is better.</p> <code>check_finite</code> <code>bool</code> <p>If True, stop when metric becomes NaN or Inf.</p> <code>stopping_threshold</code> <code>float | None</code> <p>Stop immediately when metric reaches this value.</p> <code>divergence_threshold</code> <code>float | None</code> <p>Stop if metric exceeds this value (min mode only).</p>"},{"location":"api/callbacks/#substrax.callbacks.EarlyStoppingConfig.__post_init__","title":"<code>__post_init__()</code>","text":"<p>Refuse a patience below one, which would stop on an improving epoch.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>patience</code> is below one.</p>"},{"location":"api/callbacks/#substrax.callbacks.BestMetricTracker","title":"<code>BestMetricTracker(*, mode, min_delta)</code>","text":"<p>Shared best-so-far + stagnation bookkeeping for the plateau callbacks.</p> <p>Tracks the best monitored value and the number of consecutive updates without a <code>min_delta</code> improvement, in either <code>\"min\"</code> or <code>\"max\"</code> mode.</p> <p>Initialise the tracker.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>PlateauMode | str</code> <p><code>\"min\"</code> (lower is better) or <code>\"max\"</code> (higher is better).</p> required <code>min_delta</code> <code>float</code> <p>Minimum absolute change counted as an improvement.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>min_delta</code> is negative.</p>"},{"location":"api/callbacks/#substrax.callbacks.BestMetricTracker.mode","title":"<code>mode</code>  <code>property</code>","text":"<p>The improvement direction.</p>"},{"location":"api/callbacks/#substrax.callbacks.BestMetricTracker.best","title":"<code>best</code>  <code>property</code>","text":"<p>The best monitored value seen so far.</p>"},{"location":"api/callbacks/#substrax.callbacks.BestMetricTracker.num_bad_epochs","title":"<code>num_bad_epochs</code>  <code>property</code>","text":"<p>Consecutive updates without a <code>min_delta</code> improvement.</p>"},{"location":"api/callbacks/#substrax.callbacks.BestMetricTracker.register","title":"<code>register(value)</code>","text":"<p>Record <code>value</code>; return <code>True</code> if it improves on the best so far.</p> <p>Updates the best value and resets the stagnation counter on an improvement; otherwise increments the stagnation counter.</p>"},{"location":"api/callbacks/#substrax.callbacks.BestMetricTracker.get_state","title":"<code>get_state()</code>","text":"<p>The best value and the stagnation count, for a checkpoint.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p><code>best</code> (infinite before the first value) and <code>num_bad_epochs</code>.</p>"},{"location":"api/callbacks/#substrax.callbacks.BestMetricTracker.set_state","title":"<code>set_state(state)</code>","text":"<p>Take back a state :meth:<code>get_state</code> returned.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>dict[str, Any]</code> <p>The saved best value and stagnation count.</p> required"},{"location":"api/callbacks/#substrax.callbacks.EarlyStopping","title":"<code>EarlyStopping(*, patience, min_delta=0.0, mode=PlateauMode.MIN)</code>","text":"<p>               Bases: <code>BestMetricTracker</code></p> <p>Signal to stop once a monitored metric stops improving.</p> <p>Initialise the stopper.</p> <p>Parameters:</p> Name Type Description Default <code>patience</code> <code>int</code> <p>Epochs without a <code>min_delta</code> improvement before stopping.</p> required <code>min_delta</code> <code>float</code> <p>Minimum absolute change counted as an improvement.</p> <code>0.0</code> <code>mode</code> <code>PlateauMode | str</code> <p><code>\"min\"</code> (lower is better) or <code>\"max\"</code> (higher is better).</p> <code>MIN</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>patience</code> is not positive.</p>"},{"location":"api/callbacks/#substrax.callbacks.EarlyStopping.should_stop","title":"<code>should_stop</code>  <code>property</code>","text":"<p>Whether the metric has stagnated for <code>patience</code> epochs.</p>"},{"location":"api/callbacks/#substrax.callbacks.EarlyStopping.update","title":"<code>update(value)</code>","text":"<p>Record the latest metric; return <code>True</code> if it improved on the best.</p>"},{"location":"api/callbacks/#substrax.callbacks.PlateauMode","title":"<code>PlateauMode</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Whether a monitored metric improves by decreasing or increasing.</p>"},{"location":"api/checkpoint/","title":"Checkpoint","text":"<p><code>substrax.checkpoint</code> is one checkpoint store over Orbax: step-addressed, pickle-free, with retention delegated to <code>orbax.checkpoint.CheckpointManager</code>. A checkpoint is a step holding named items, the things a training loop owns (<code>model</code>, <code>optimizer</code>, <code>rng</code>, <code>data_iterator</code>, <code>extensions</code>), beside one <code>CheckpointMetadata</code> record.</p> <pre><code>from flax import nnx\nfrom substrax.checkpoint import OrbaxCheckpointStore\n\nwith OrbaxCheckpointStore(\"checkpoints\", max_to_keep=5) as store:\n    store.save(100, {\"model\": nnx.state(model), \"rng\": key}, epoch=2, metrics={\"loss\": 0.42})\n    checkpoint = store.restore(100, templates={\"model\": nnx.state(model)})\n    nnx.update(model, checkpoint.items[\"model\"])\n    best = store.best_step(\"loss\")\n</code></pre> <p>Every item is written with <code>PyTreeSave</code>, which carries arrays, typed PRNG keys and plain-Python leaves (ints, strings, booleans, lists) alike, and the metadata with <code>JsonSave</code>, so restoring a checkpoint never executes code. On disk each item holds its pytree under a single <code>tree</code> node, so an item that is a bare array (the <code>rng</code> key) is accepted by every supported Orbax, the 0.11.33 floor included. Restoring onto templates places every array on its template leaf's device, whatever topology the checkpoint was written on; an item without a template comes back as stored.</p> <p>A template never changes a saved array's dtype silently. Orbax casts every array to its template leaf's dtype, so a float64 array restored onto a float32 template, or a float32 model onto a bfloat16 one, would lose precision or change without a word. <code>save</code> writes every array's dtype, by item and leaf path, as a <code>dtypes</code> JSON item beside the metadata record (which <code>best_step</code> reads for every step, so it stays small), and <code>restore</code> compares each template leaf against it before reading any array, raising <code>CheckpointDtypeMismatchError</code> with a <code>DtypeMismatch</code> (item, leaf, saved and restored dtype) for every differing array. An item restored without a template is compared after it is read: while jax's x64 mode is off, jax creates a 64-bit array at 32 bits, and the error says how to enable x64. <code>restore(..., cast_dtypes=True)</code> accepts the cast in either case. Template leaves may be arrays or <code>jax.ShapeDtypeStruct</code>; a typed PRNG key compares by its <code>uint32</code> data, as Orbax stores it, and strings and Python numbers are not compared. A checkpoint written without the <code>dtypes</code> item is compared against Orbax's per-array metadata instead, which opens every array's store on each restore.</p> <p>Writes are strict: an existing step is refused unless <code>overwrite=True</code>, a step below the latest is refused, and a step Orbax declines raises. Every refusal is a <code>CheckpointNotWrittenError</code> naming the step, the latest step and the reason. Nothing is created before the first save, and <code>resolve_checkpoint_dir</code> picks a run's directory without creating it.</p> <p>A checkpoint whose metadata names another format, another producer or another version number is refused with <code>UnsupportedCheckpointError</code> naming what it holds, rather than read as this one.</p>"},{"location":"api/checkpoint/#substrax.checkpoint","title":"<code>substrax.checkpoint</code>","text":"<p>Checkpoints: named items beside one metadata record.</p> <p>A checkpoint is a step holding the items a training loop owns (<code>model</code>, <code>optimizer</code>, <code>rng</code>, <code>data_iterator</code>, <code>extensions</code>), each an Orbax pytree item, and one :class:<code>CheckpointMetadata</code> record. :class:<code>OrbaxCheckpointStore</code> is the one implementation of the :class:<code>CheckpointStore</code> protocol; it refuses to overwrite or reorder steps unless told to, restores onto templates so a checkpoint written on one device topology lands on whatever the restoring process has, and records each array's dtype at save so a restore that would change one is refused rather than silent.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointDtypeMismatchError","title":"<code>CheckpointDtypeMismatchError(*, step, mismatches, x64_disabled=False)</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>A restore would give saved arrays another dtype, so it is refused.</p> <p>Orbax casts every array to its template leaf's dtype, and without a template jax creates a 64-bit array at 32 bits while its x64 mode is off. <code>step</code> is the checkpoint's step and <code>mismatches</code> every array concerned; <code>restore(..., cast_dtypes=True)</code> accepts the cast.</p> <p>Record the step and every array concerned.</p> <p>Parameters:</p> Name Type Description Default <code>step</code> <code>int</code> <p>The checkpoint's step.</p> required <code>mismatches</code> <code>tuple[DtypeMismatch, ...]</code> <p>Every array whose restored dtype differs from its saved one.</p> required <code>x64_disabled</code> <code>bool</code> <p>Whether jax's x64 mode was off for a restore without templates, which adds how to keep the saved precision to the message.</p> <code>False</code>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointNotFoundError","title":"<code>CheckpointNotFoundError(step, directory)</code>","text":"<p>               Bases: <code>FileNotFoundError</code></p> <p>The store holds no checkpoint at the requested step.</p> <p>Record the missing step and the store's directory.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointNotWrittenError","title":"<code>CheckpointNotWrittenError(*, step, latest_step, reason)</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>A save was refused, so the step on disk is whatever it was before the call.</p> <p>The error carries <code>step</code>, the step the caller asked to write; <code>latest_step</code>, the newest step the store held at the time (<code>None</code> for an empty store); and <code>reason</code>: <code>\"exists\"</code> when the step is already written and <code>overwrite</code> was not given, <code>\"below_latest\"</code> when the step is older than the newest one (a store's steps only grow), <code>\"rejected\"</code> when Orbax declined the write.</p> <p>Record the refused step, the store's latest step and the reason.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.DtypeMismatch","title":"<code>DtypeMismatch(*, item, leaf, saved, restored)</code>  <code>dataclass</code>","text":"<p>One array a restore would give another dtype than the one it was saved with.</p> <p><code>item</code> is the Orbax item on disk and <code>leaf</code> the array's path inside it joined with <code>/</code>. <code>saved</code> is the dtype written; <code>restored</code> the dtype the restore gives it: the template leaf's, or, without a template, the one this process creates for the saved array.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.UnsupportedCheckpointError","title":"<code>UnsupportedCheckpointError</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>The checkpoint's metadata describes a format this substrax cannot read.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointMetadata","title":"<code>CheckpointMetadata(*, format=FORMAT_NAME, format_version=CURRENT_FORMAT_VERSION, step, epoch=None, items, libraries=dict(), producer=None, metrics=dict(), extra=dict(), created_at)</code>  <code>dataclass</code>","text":"<p>What a checkpoint says about itself.</p> <p>Attributes:</p> Name Type Description <code>format</code> <code>str</code> <p><code>\"substrax-checkpoint\"</code>; another name is another producer's file.</p> <code>format_version</code> <code>int</code> <p>The on-disk format; a reader refuses a version newer than its own.</p> <code>step</code> <code>int</code> <p>The step the checkpoint is addressed by.</p> <code>epoch</code> <code>int | None</code> <p>The epoch the step falls in, when the producer counts epochs.</p> <code>items</code> <code>tuple[str, ...]</code> <p>The item names written, in the order they were given.</p> <code>libraries</code> <code>Mapping[str, str]</code> <p>Distribution versions at write time, informational only.</p> <code>producer</code> <code>Producer | None</code> <p>The library that wrote the checkpoint, when it says.</p> <code>metrics</code> <code>Mapping[str, float]</code> <p>Scalar metrics, the values <code>best_step</code> compares.</p> <code>extra</code> <code>Mapping[str, JsonValue]</code> <p>The caller's own JSON values; a key here never shadows a field of the record.</p> <code>created_at</code> <code>str</code> <p>The write time, ISO 8601 in UTC.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointMetadata.to_dict","title":"<code>to_dict()</code>","text":"<p>The JSON form Orbax writes as the <code>metadata</code> item.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointMetadata.from_dict","title":"<code>from_dict(payload)</code>  <code>classmethod</code>","text":"<p>Read the JSON form, refusing a record this substrax cannot interpret.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>Mapping[str, JsonValue]</code> <p>The record as Orbax's <code>JsonRestore</code> returns it.</p> required <p>Returns:</p> Type Description <code>CheckpointMetadata</code> <p>The metadata record.</p> <p>Raises:</p> Type Description <code>UnsupportedCheckpointError</code> <p>If the record names another format or another version of it.</p> <code>ValidationError</code> <p>If a field is missing or holds a value its annotation does not admit.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.Producer","title":"<code>Producer(*, name, version)</code>  <code>dataclass</code>","text":"<p>The library that wrote a checkpoint, by distribution name and version.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.Producer.to_dict","title":"<code>to_dict()</code>","text":"<p>The JSON form.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.Producer.from_dict","title":"<code>from_dict(payload)</code>  <code>classmethod</code>","text":"<p>Read the JSON form.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <code>Mapping[str, JsonValue]</code> <p>The producer's JSON object.</p> required <p>Returns:</p> Type Description <code>Producer</code> <p>The producer.</p> <p>Raises:</p> Type Description <code>ValidationError</code> <p>If <code>name</code> or <code>version</code> is missing or not a string.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.Checkpoint","title":"<code>Checkpoint(*, step, items, metadata)</code>  <code>dataclass</code>","text":"<p>A restored checkpoint: its step, its items by name and its metadata.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointStore","title":"<code>CheckpointStore</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>Step-addressed checkpoint store contract.</p> <p>Implementations persist named items plus a metadata record under an integer step and restore them back. Training code depends on this protocol; the Orbax class is the one implementation.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointStore.save","title":"<code>save(step, items, *, epoch=None, metrics=None, producer=None, extra=None, overwrite=False)</code>","text":"<p>Write <code>items</code> at <code>step</code> and return the checkpoint's directory.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointStore.restore","title":"<code>restore(step, *, templates=None, cast_dtypes=False)</code>","text":"<p>Read the checkpoint at <code>step</code>, onto <code>templates</code> where given.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointStore.read_metadata","title":"<code>read_metadata(step)</code>","text":"<p>Read only the metadata record of <code>step</code>.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointStore.list_steps","title":"<code>list_steps()</code>","text":"<p>Every step held, ascending.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointStore.latest_step","title":"<code>latest_step()</code>","text":"<p>The newest step, or <code>None</code> for an empty store.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointStore.best_step","title":"<code>best_step(metric, *, mode='min')</code>","text":"<p>The step whose <code>metric</code> is the lowest or highest, or <code>None</code> when none has it.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointStore.delete","title":"<code>delete(step)</code>","text":"<p>Remove the checkpoint at <code>step</code>.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.CheckpointStore.close","title":"<code>close()</code>","text":"<p>Release backend resources.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore","title":"<code>OrbaxCheckpointStore(directory, *, max_to_keep=5)</code>","text":"<p>Orbax-backed :class:<code>CheckpointStore</code>.</p> <p>Wraps :class:<code>orbax.checkpoint.CheckpointManager</code> with step-int addressing and retention by <code>max_to_keep</code>. The manager is opened on first use, so constructing a store writes nothing; the directory appears with the first save. The store doubles as a context manager so backend resources are released deterministically::</p> <pre><code>with OrbaxCheckpointStore(path, max_to_keep=5) as store:\n    store.save(step, {\"model\": nnx.state(model)}, metrics={\"loss\": loss})\n</code></pre> <p>Remember where the checkpoints live and how many to keep.</p> <p>Parameters:</p> Name Type Description Default <code>directory</code> <code>str | Path</code> <p>Directory the checkpoints are stored under; created on the first save.</p> required <code>max_to_keep</code> <code>int | None</code> <p>How many of the newest steps Orbax retains; <code>None</code> keeps every one.</p> <code>5</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>directory</code> is empty or whitespace-only.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.__enter__","title":"<code>__enter__()</code>","text":"<p>Enter the context manager.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.__exit__","title":"<code>__exit__(*_exc_info)</code>","text":"<p>Close the backend on context exit.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.save","title":"<code>save(step, items, *, epoch=None, metrics=None, producer=None, extra=None, overwrite=False)</code>","text":"<p>Write <code>items</code> at <code>step</code> with a metadata record and every array's dtype.</p> <p>Parameters:</p> Name Type Description Default <code>step</code> <code>int</code> <p>Non-negative step the checkpoint is addressed by.</p> required <code>items</code> <code>Mapping[str, Any]</code> <p>The pytrees to write, by item name (one of :data:<code>~substrax.checkpoint.metadata.ITEM_NAMES</code>).</p> required <code>epoch</code> <code>int | None</code> <p>The epoch the step falls in, when the caller counts epochs.</p> <code>None</code> <code>metrics</code> <code>Mapping[str, float] | None</code> <p>Scalar metrics recorded for <code>best_step</code>.</p> <code>None</code> <code>producer</code> <code>Producer | None</code> <p>The library writing the checkpoint.</p> <code>None</code> <code>extra</code> <code>Mapping[str, JsonValue] | None</code> <p>The caller's own JSON values, under keys that are not fields of the record.</p> <code>None</code> <code>overwrite</code> <code>bool</code> <p>Replace an existing checkpoint at <code>step</code> instead of refusing.</p> <code>False</code> <p>Returns:</p> Type Description <code>Path</code> <p>The checkpoint's directory, <code>directory / str(step)</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>step</code> is negative, <code>items</code> is empty or names an unknown item, or <code>extra</code> shadows a reserved key.</p> <code>CheckpointNotWrittenError</code> <p>If <code>step</code> is already written and <code>overwrite</code> is false, if it is below the latest step, or if Orbax declines the write.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.restore","title":"<code>restore(step, *, templates=None, cast_dtypes=False)</code>","text":"<p>Read the checkpoint at <code>step</code>.</p> <p>Parameters:</p> Name Type Description Default <code>step</code> <code>int</code> <p>The step to read.</p> required <code>templates</code> <code>Mapping[str, Any] | None</code> <p>Pytrees by item name; each named item is restored onto its template's leaves (device placement included), and an item without a template comes back as stored. Leaves may be arrays or <code>jax.ShapeDtypeStruct</code>.</p> <code>None</code> <code>cast_dtypes</code> <code>bool</code> <p>Accept an array coming back with another dtype than it was saved with: cast to its template leaf's, or, without a template, a 64-bit array at 32 bits while jax's x64 mode is off. Without it, either is refused.</p> <code>False</code> <p>Returns:</p> Type Description <code>Checkpoint</code> <p>The restored items and the checkpoint's metadata.</p> <p>Raises:</p> Type Description <code>CheckpointNotFoundError</code> <p>If the store holds no checkpoint at <code>step</code>.</p> <code>UnsupportedCheckpointError</code> <p>If the checkpoint is in another format than the one this substrax reads, or is not a substrax checkpoint.</p> <code>CheckpointDtypeMismatchError</code> <p>If an array would come back with another dtype than it was saved with and <code>cast_dtypes</code> is false.</p> <code>ValueError</code> <p>If <code>templates</code> names an item the checkpoint lacks, or a template's tree does not match the checkpoint's.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.read_metadata","title":"<code>read_metadata(step)</code>","text":"<p>Read the metadata record of <code>step</code> without its items.</p> <p>Parameters:</p> Name Type Description Default <code>step</code> <code>int</code> <p>The step to read.</p> required <p>Returns:</p> Type Description <code>CheckpointMetadata</code> <p>The metadata record.</p> <p>Raises:</p> Type Description <code>CheckpointNotFoundError</code> <p>If the store holds no checkpoint at <code>step</code>.</p> <code>UnsupportedCheckpointError</code> <p>If the checkpoint is in another format than the one this substrax reads, or is not a substrax checkpoint.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.list_steps","title":"<code>list_steps()</code>","text":"<p>Every step held, ascending.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.latest_step","title":"<code>latest_step()</code>","text":"<p>The newest step, or <code>None</code> for an empty store.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.best_step","title":"<code>best_step(metric, *, mode='min')</code>","text":"<p>The step whose <code>metric</code> is the lowest (<code>\"min\"</code>) or highest (<code>\"max\"</code>).</p> <p>Steps whose metadata lacks the metric are skipped.</p> <p>Parameters:</p> Name Type Description Default <code>metric</code> <code>str</code> <p>The metric name, as recorded in each step's <code>metrics</code>.</p> required <code>mode</code> <code>BestMode</code> <p><code>\"min\"</code> for the lowest value, <code>\"max\"</code> for the highest.</p> <code>'min'</code> <p>Returns:</p> Type Description <code>int | None</code> <p>The best step, or <code>None</code> when no step records the metric.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>mode</code> is neither <code>\"min\"</code> nor <code>\"max\"</code>.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.delete","title":"<code>delete(step)</code>","text":"<p>Remove the checkpoint at <code>step</code>.</p> <p>Parameters:</p> Name Type Description Default <code>step</code> <code>int</code> <p>The step to remove.</p> required <p>Raises:</p> Type Description <code>CheckpointNotFoundError</code> <p>If the store holds no checkpoint at <code>step</code>.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.OrbaxCheckpointStore.close","title":"<code>close()</code>","text":"<p>Close the Orbax manager, if one was opened.</p>"},{"location":"api/checkpoint/#substrax.checkpoint.resolve_checkpoint_dir","title":"<code>resolve_checkpoint_dir(checkpoint_dir, run_dir)</code>","text":"<p>Return the directory a run's checkpoints go to, without creating it.</p> <p>An explicit <code>checkpoint_dir</code> wins; otherwise the run's directory holds a <code>checkpoints</code> subdirectory. A relative path resolves against the working directory. The store creates the directory on its first save, so resolving one writes nothing.</p> <p>Parameters:</p> Name Type Description Default <code>checkpoint_dir</code> <code>Path | None</code> <p>The directory the caller chose, or <code>None</code>.</p> required <code>run_dir</code> <code>Path | None</code> <p>The run's directory, whose <code>checkpoints</code> subdirectory is the default.</p> required <p>Returns:</p> Type Description <code>Path</code> <p>The absolute directory the checkpoints go to.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If neither directory is given.</p>"},{"location":"api/compute/","title":"Compute","text":"<p><code>substrax.compute</code> runs a project's jobs on a compute backend: this machine, Modal, or any cloud SkyPilot reaches. A job is declared once, in the project's <code>pyproject.toml</code>, and runs the same way on every backend, because every backend runs the same program on the remote machine: the worker.</p>"},{"location":"api/compute/#declaring-jobs","title":"Declaring jobs","text":"<pre><code>[tool.substrax.compute]\nbackend = \"modal\"                        # used when the command line names none\n\n[tool.substrax.compute.backends.modal]   # passed to the backend as its settings\noutputs_volume = \"demo-outputs\"\n\n[tool.substrax.compute.jobs.examples]\nexamples = [\"examples/metrics\"]          # one task per script\ncommands = { gpu-tests = [\"python\", \"-m\", \"pytest\", \"tests/gpu\"] }\nextras = [\"cuda12\"]\naccelerator = { kind = \"L4\", count = 1 }\ntimeout_seconds = 1800\ntask_timeout_seconds = 300\nruntime = { platforms = [\"cuda\"], xla_flags = [\"--xla_gpu_deterministic_ops=true\"] }\nenv = { TF_CPP_MIN_LOG_LEVEL = \"1\" }\nmounts = [{ name = \"datasets\", path = \"/data\", access = \"read-only\" }]\n</code></pre> <p>A misspelled key is refused, not ignored. <code>examples</code> lists each directory's scripts as <code>substrax.examples.discover_examples</code> does, skipping every name that starts with <code>_</code>; a task's leading <code>\"python\"</code> is the project environment's interpreter.</p>"},{"location":"api/compute/#running-them","title":"Running them","text":"<pre><code>uv run substrax-compute run examples                  # submit, follow, wait, fetch\nuv run substrax-compute run examples --accelerator H100:2 --detach\nuv run substrax-compute status                        # every stored run\nuv run substrax-compute logs &lt;run-id&gt; --follow\nuv run substrax-compute fetch &lt;run-id&gt; --into ~/runs\nuv run substrax-compute cancel &lt;run-id&gt;\n</code></pre> <p>The command runs from the project's own environment, whose lock must include substrax: the worker is the project's locked substrax. A run's handle is kept under the state directory (<code>$SUBSTRAX_STATE_DIR</code>, else <code>$XDG_STATE_HOME/substrax</code>), so a detached run is found again from any shell. Fetched outputs go to <code>--into</code> or under the state directory, never into the project.</p>"},{"location":"api/compute/#what-a-run-produces","title":"What a run produces","text":"<pre><code>&lt;run-id&gt;/\n  manifest.json            # every task: argv, status, exit code, seconds, files written\n  &lt;task&gt;/stdout.log\n  &lt;task&gt;/stderr.log\n  &lt;task&gt;/&lt;name&gt;/...        # what the task saved through resolve_output_dir(name)\n</code></pre> <p>Each task runs from the project root with <code>AVITAI_OUTPUT_DIR</code> set to its own directory and the job's <code>JaxRuntime</code> applied, for at most its own budget or what is left of the job's. A failed task does not stop the ones after it. The worker rewrites the manifest after every task, and marks it <code>finished</code> after the last.</p>"},{"location":"api/compute/#backends","title":"Backends","text":"Backend Runs on Needs <code>local</code> this machine, in the project's current environment nothing <code>modal</code> Modal, one detached function per run, outputs on a Volume <code>substrax[modal]</code>, a Modal account, and a workspace image builder of 2025.06 or later <code>skypilot</code> a cluster SkyPilot provisions for the run on any cloud it supports, torn down after it; outputs on a <code>gs://</code> or <code>s3://</code> bucket SkyPilot installed as its documentation says (<code>uv tool install --with pip \"skypilot[gcp]\"</code>), and <code>gcloud</code> or <code>aws</code> for the bucket <p>The Modal image is built in layers keyed on what they depend on: the locked dependencies (<code>Image.uv_sync</code>), then the project's source, then the project installed without dependencies, so an edit to the source rebuilds only the last two. <code>Image.uv_sync</code> needs image builder 2025.06 or later, set for the workspace in Modal's dashboard under Settings \u2192 Image config; an older builder fails the build with <code>Image builder version &lt;= 2024.10 requires modal to be specified in your pyproject.toml file</code>. The image runs the Python this interpreter runs, which the serialized worker requires, and which <code>uv sync</code> is told to use so a project's own <code>python-preference</code> cannot choose another. SkyPilot is never imported: the backend writes a task file and runs <code>sky launch</code>, <code>sky queue -o json</code>, <code>sky logs</code>, <code>sky cancel</code> and <code>sky down</code>.</p>"},{"location":"api/compute/#adding-a-provider","title":"Adding a provider","text":"<p>A backend is any object that satisfies <code>ComputeBackend</code> and is built from its settings table. Register the factory in the <code>substrax.compute.backends</code> entry-point group:</p> <pre><code>[project.entry-points.\"substrax.compute.backends\"]\nmyprovider = \"mypackage.backend:MyBackend\"\n</code></pre> <p>and inherit the contract every backend must pass:</p> <pre><code>from pathlib import Path\n\nfrom substrax.compute.backend import ComputeBackend\nfrom substrax.testing.compute import BackendContract\n\nfrom mypackage.backend import MyBackend\n\n\nclass TestMyBackend(BackendContract):\n    def make_backend(self, state_dir: Path) -&gt; ComputeBackend:\n        return MyBackend({\"state_dir\": str(state_dir)})\n</code></pre>"},{"location":"api/compute/#substrax.compute","title":"<code>substrax.compute</code>","text":"<p>Run a project's commands on remote accelerators through provider plugins.</p> <p>A <code>JobSpec</code> states what runs: tasks, accelerators, extras, JAX settings and mounts. The worker (<code>python -m substrax.compute.worker</code>) runs it the same way on every provider. Importing this package imports no jax and no provider library.</p>"},{"location":"api/compute/#substrax.compute.JOB_SPEC_VERSION","title":"<code>JOB_SPEC_VERSION = 1</code>  <code>module-attribute</code>","text":"<p>The job spec layout this release writes and reads.</p>"},{"location":"api/compute/#substrax.compute.Accelerator","title":"<code>Accelerator(*, kind, count=1)</code>  <code>dataclass</code>","text":"<p>The accelerators a job runs on.</p> <p>Attributes:</p> Name Type Description <code>kind</code> <code>str</code> <p>The accelerator's name as the providers spell it, such as <code>\"L4\"</code>, <code>\"A100-80GB\"</code> or <code>\"H100\"</code>; a backend refuses a kind its provider does not offer.</p> <code>count</code> <code>int</code> <p>How many.</p>"},{"location":"api/compute/#substrax.compute.Accelerator.__post_init__","title":"<code>__post_init__()</code>","text":"<p>Validate the kind and count.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>kind</code> is not one word or <code>count</code> is below one.</p>"},{"location":"api/compute/#substrax.compute.JobSpec","title":"<code>JobSpec(*, schema_version=JOB_SPEC_VERSION, name, tasks, timeout_seconds, accelerator=None, extras=(), runtime=JaxRuntime(), env=dict(), mounts=())</code>  <code>dataclass</code>","text":"<p>A job: its tasks and everything the machine that runs them must provide.</p> <p>Attributes:</p> Name Type Description <code>schema_version</code> <code>int</code> <p>The layout of this record; see :data:<code>JOB_SPEC_VERSION</code>.</p> <code>name</code> <code>str</code> <p>The job's name, such as <code>\"examples\"</code>.</p> <code>tasks</code> <code>tuple[Task, ...]</code> <p>The commands, run in order; a failed task does not stop the ones after it.</p> <code>timeout_seconds</code> <code>float</code> <p>Seconds the whole job may take; the backend stops it after that.</p> <code>accelerator</code> <code>Accelerator | None</code> <p>The accelerators it needs; <code>None</code> runs on CPUs.</p> <code>extras</code> <code>tuple[str, ...]</code> <p>The project's optional dependency groups installed with its locked dependencies.</p> <code>runtime</code> <code>JaxRuntime</code> <p>The JAX settings of every task.</p> <code>env</code> <code>Mapping[str, str]</code> <p>Variables set in every task before <code>runtime</code> is applied.</p> <code>mounts</code> <code>tuple[Mount, ...]</code> <p>The named storage the tasks read or write, besides the run's outputs.</p>"},{"location":"api/compute/#substrax.compute.JobSpec.__post_init__","title":"<code>__post_init__()</code>","text":"<p>Validate the job as a whole.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the version is not this release's, the name is not one path component, there are no tasks, the timeout is not a positive finite number, or two tasks or two mounts share a name, or two mounts a path.</p>"},{"location":"api/compute/#substrax.compute.Mount","title":"<code>Mount(*, name, path, access)</code>  <code>dataclass</code>","text":"<p>Named storage a job sees at a path; each backend maps the name to its own storage.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>The storage's name in the backend's settings, such as <code>\"datasets\"</code>.</p> <code>path</code> <code>str</code> <p>The absolute directory the job sees it at.</p> <code>access</code> <code>MountAccess</code> <p>Whether the job may write to it.</p>"},{"location":"api/compute/#substrax.compute.Mount.__post_init__","title":"<code>__post_init__()</code>","text":"<p>Validate the name and path.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the name is not one path component, or the path is not an absolute, normalised directory below the root.</p>"},{"location":"api/compute/#substrax.compute.MountAccess","title":"<code>MountAccess</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Whether a job may change what a mount holds.</p>"},{"location":"api/compute/#substrax.compute.Task","title":"<code>Task(*, name, argv, timeout_seconds)</code>  <code>dataclass</code>","text":"<p>One command a job runs, from the project's root, in the project's environment.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>The task's name, and the directory its logs and outputs go to.</p> <code>argv</code> <code>tuple[str, ...]</code> <p>The command. A leading <code>\"python\"</code> is the project environment's interpreter.</p> <code>timeout_seconds</code> <code>float</code> <p>Seconds before the task is killed.</p>"},{"location":"api/compute/#substrax.compute.Task.__post_init__","title":"<code>__post_init__()</code>","text":"<p>Validate the name, command and timeout.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the name is not one path component, the command is empty, or the timeout is not a positive finite number.</p>"},{"location":"api/compute/#substrax.compute.example_tasks","title":"<code>example_tasks(paths, *, project, timeout_seconds)</code>","text":"<p>Make one task per example script, run by the project's interpreter from its root.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>Iterable[Path]</code> <p>The examples, such as :func:<code>~substrax.examples.discover_examples</code> lists them.</p> required <code>project</code> <code>Path</code> <p>The project root the paths lie under.</p> required <code>timeout_seconds</code> <code>float</code> <p>Each example's budget.</p> required <p>Returns:</p> Type Description <code>Task</code> <p>The tasks, in the order of <code>paths</code>, each named by its path relative to <code>project</code> with</p> <code>...</code> <p>dots for separators and without the suffix.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If a path lies outside <code>project</code>.</p>"},{"location":"api/compute/#substrax.compute.read_job_spec","title":"<code>read_job_spec(data)</code>","text":"<p>Read a job spec from its parsed JSON object.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Mapping[str, JsonValue]</code> <p>The JSON object <code>dump_record</code> wrote.</p> required <p>Returns:</p> Type Description <code>JobSpec</code> <p>The spec.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the spec's version is not this release's (the message names both), or a field is missing or holds a value its annotation does not admit.</p>"},{"location":"api/compute/#substrax.compute.backend","title":"<code>substrax.compute.backend</code>","text":"<p>The interface every compute provider implements, and the handles that name submitted runs.</p> <p>A backend turns a :class:<code>~substrax.compute.JobSpec</code> into a run on its provider and reports on it afterwards. It returns a :class:<code>RunHandle</code> from :meth:<code>ComputeBackend.submit</code> and needs nothing else to find the run again, so a run submitted from one shell can be followed, fetched or cancelled from another. :class:<code>RunStore</code> keeps the handles.</p>"},{"location":"api/compute/#substrax.compute.backend.SPEC_NAME","title":"<code>SPEC_NAME = 'spec.json'</code>  <code>module-attribute</code>","text":"<p>The file a staged run's spec is written to.</p>"},{"location":"api/compute/#substrax.compute.backend.STATE_DIR_ENV","title":"<code>STATE_DIR_ENV = 'SUBSTRAX_STATE_DIR'</code>  <code>module-attribute</code>","text":"<p>Overrides the directory substrax keeps run handles and local runs in.</p>"},{"location":"api/compute/#substrax.compute.backend.BackendFactory","title":"<code>BackendFactory = Callable[[Mapping[str, JsonValue]], ComputeBackend]</code>  <code>module-attribute</code>","text":"<p>What a backend's entry point loads: it builds the backend from its settings table.</p>"},{"location":"api/compute/#substrax.compute.backend.RunState","title":"<code>RunState</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Where a run is.</p>"},{"location":"api/compute/#substrax.compute.backend.RunState.is_final","title":"<code>is_final</code>  <code>property</code>","text":"<p>Whether the run has ended.</p>"},{"location":"api/compute/#substrax.compute.backend.RunHandle","title":"<code>RunHandle(*, schema_version=HANDLE_VERSION, run_id, backend, job, project, submitted_at, details=dict())</code>  <code>dataclass</code>","text":"<p>Everything needed to find a submitted run again.</p> <p>Attributes:</p> Name Type Description <code>schema_version</code> <code>int</code> <p>The layout of this record.</p> <code>run_id</code> <code>str</code> <p>The run's name, unique across backends; also its outputs' directory name.</p> <code>backend</code> <code>str</code> <p>The name of the backend that ran it.</p> <code>job</code> <code>str</code> <p>The job's name.</p> <code>project</code> <code>str</code> <p>The project root it was submitted from.</p> <code>submitted_at</code> <code>datetime</code> <p>When it was submitted, in UTC.</p> <code>details</code> <code>Mapping[str, str]</code> <p>The provider's own references to the run, such as an app or cluster id.</p>"},{"location":"api/compute/#substrax.compute.backend.NewRun","title":"<code>NewRun(*, run_id, spec, project)</code>  <code>dataclass</code>","text":"<p>A run being submitted: its id, its job and its project, until the provider names it.</p> <p>Attributes:</p> Name Type Description <code>run_id</code> <code>str</code> <p>The run's id, from :func:<code>new_run_id</code>.</p> <code>spec</code> <code>JobSpec</code> <p>The job.</p> <code>project</code> <code>Path</code> <p>The resolved project root.</p>"},{"location":"api/compute/#substrax.compute.backend.NewRun.handle","title":"<code>handle(backend, details)</code>","text":"<p>The handle of this run, submitted now.</p> <p>Parameters:</p> Name Type Description Default <code>backend</code> <code>str</code> <p>The backend's name.</p> required <code>details</code> <code>Mapping[str, str]</code> <p>The provider's references to the run.</p> required <p>Returns:</p> Type Description <code>RunHandle</code> <p>The handle.</p>"},{"location":"api/compute/#substrax.compute.backend.ComputeBackend","title":"<code>ComputeBackend</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>A compute provider that runs jobs.</p> <p>Every method but :meth:<code>submit</code> takes a handle this backend returned, possibly in another process, and must work from it alone.</p>"},{"location":"api/compute/#substrax.compute.backend.ComputeBackend.name","title":"<code>name</code>  <code>instance-attribute</code>","text":"<p>The name the backend is registered under.</p>"},{"location":"api/compute/#substrax.compute.backend.ComputeBackend.submit","title":"<code>submit(spec, *, project)</code>","text":"<p>Start <code>spec</code> from the project at <code>project</code> and return once it is submitted.</p>"},{"location":"api/compute/#substrax.compute.backend.ComputeBackend.status","title":"<code>status(handle)</code>","text":"<p>Where the run is now.</p>"},{"location":"api/compute/#substrax.compute.backend.ComputeBackend.logs","title":"<code>logs(handle, *, follow)</code>","text":"<p>The worker's output lines; with <code>follow</code>, until the run ends.</p>"},{"location":"api/compute/#substrax.compute.backend.ComputeBackend.fetch","title":"<code>fetch(handle, destination)</code>","text":"<p>Copy the run's outputs to <code>destination / handle.run_id</code> and return that directory.</p>"},{"location":"api/compute/#substrax.compute.backend.ComputeBackend.cancel","title":"<code>cancel(handle)</code>","text":"<p>Stop the run; a run that already ended is left as it is.</p>"},{"location":"api/compute/#substrax.compute.backend.RunStore","title":"<code>RunStore(root=None)</code>","text":"<p>The handles of submitted runs, one JSON file per run.</p> <p>Keep handles under <code>root</code>.</p> <p>Parameters:</p> Name Type Description Default <code>root</code> <code>Path | None</code> <p>The directory; <code>default_state_dir() / \"compute\" / \"runs\"</code> when <code>None</code>.</p> <code>None</code>"},{"location":"api/compute/#substrax.compute.backend.RunStore.save","title":"<code>save(handle)</code>","text":"<p>Write a handle, replacing an earlier one of the same run.</p> <p>Parameters:</p> Name Type Description Default <code>handle</code> <code>RunHandle</code> <p>The handle.</p> required"},{"location":"api/compute/#substrax.compute.backend.RunStore.load","title":"<code>load(run_id)</code>","text":"<p>Read the handle of a run.</p> <p>Parameters:</p> Name Type Description Default <code>run_id</code> <code>str</code> <p>The run.</p> required <p>Returns:</p> Type Description <code>RunHandle</code> <p>Its handle.</p> <p>Raises:</p> Type Description <code>KeyError</code> <p>If no handle of that run is kept; the message names the directory.</p>"},{"location":"api/compute/#substrax.compute.backend.RunStore.handles","title":"<code>handles()</code>","text":"<p>Every kept handle, newest first.</p> <p>Returns:</p> Type Description <code>list[RunHandle]</code> <p>The handles.</p>"},{"location":"api/compute/#substrax.compute.backend.new_run_id","title":"<code>new_run_id(job)</code>","text":"<p>Name a new run of <code>job</code> by its UTC start and a random suffix.</p> <p>Parameters:</p> Name Type Description Default <code>job</code> <code>str</code> <p>The job's name.</p> required <p>Returns:</p> Type Description <code>str</code> <p>An id such as <code>examples-20260919T120000Z-3f9a1c</code>.</p>"},{"location":"api/compute/#substrax.compute.backend.default_state_dir","title":"<code>default_state_dir()</code>","text":"<p>The directory substrax keeps run handles and local runs in.</p> <p>Returns:</p> Type Description <code>Path</code> <p><code>$SUBSTRAX_STATE_DIR</code> when set, else <code>$XDG_STATE_HOME/substrax</code>, else</p> <code>Path</code> <p><code>~/.local/state/substrax</code>.</p>"},{"location":"api/compute/#substrax.compute.backend.backend_state_dir","title":"<code>backend_state_dir(state_dir, backend)</code>","text":"<p>The directory a backend keeps its runs' local files in.</p> <p>Parameters:</p> Name Type Description Default <code>state_dir</code> <code>str | None</code> <p>The backend's <code>state_dir</code> setting; the default state directory when <code>None</code>.</p> required <code>backend</code> <code>str</code> <p>The backend's name.</p> required <p>Returns:</p> Type Description <code>Path</code> <p><code>&lt;state_dir&gt;/compute/&lt;backend&gt;</code>.</p>"},{"location":"api/compute/#substrax.compute.backend.new_run","title":"<code>new_run(spec, project)</code>","text":"<p>Start submitting a run of <code>spec</code> from <code>project</code>.</p> <p>Parameters:</p> Name Type Description Default <code>spec</code> <code>JobSpec</code> <p>The job.</p> required <code>project</code> <code>Path</code> <p>The project root.</p> required <p>Returns:</p> Type Description <code>NewRun</code> <p>The run, named by :func:<code>new_run_id</code>.</p>"},{"location":"api/compute/#substrax.compute.backend.stage_run","title":"<code>stage_run(runs, run)</code>","text":"<p>Write the run's spec into a directory of its own under <code>runs</code>.</p> <p>Parameters:</p> Name Type Description Default <code>runs</code> <code>Path</code> <p>The backend's directory of runs.</p> required <code>run</code> <code>NewRun</code> <p>The run.</p> required <p>Returns:</p> Type Description <code>Path</code> <p>Its directory, which holds <code>spec.json</code>.</p>"},{"location":"api/compute/#substrax.compute.config","title":"<code>substrax.compute.config</code>","text":"<p>A project's compute configuration: the <code>[tool.substrax.compute]</code> table of its pyproject.toml.</p> <pre><code>[tool.substrax.compute]\nbackend = \"modal\"                        # used when the command line names none\n\n[tool.substrax.compute.backends.modal]   # passed to the backend as its settings\noutputs_volume = \"demo-outputs\"\n\n[tool.substrax.compute.jobs.examples]\nexamples = [\"examples/metrics\"]          # one task per script, as discover_examples lists them\ncommands = { gpu-tests = [\"python\", \"-m\", \"pytest\", \"tests/gpu\"] }\nextras = [\"cuda12\"]\naccelerator = { kind = \"L4\", count = 1 }\ntimeout_seconds = 1800\ntask_timeout_seconds = 300               # each task's budget; the job's when unset\nruntime = { platforms = [\"cuda\"], xla_flags = [\"--xla_gpu_deterministic_ops=true\"] }\nenv = { TF_CPP_MIN_LOG_LEVEL = \"1\" }\nmounts = [{ name = \"datasets\", path = \"/data\", access = \"read-only\" }]\n</code></pre> <p>A misspelled key is refused rather than ignored.</p>"},{"location":"api/compute/#substrax.compute.config.TaskSources","title":"<code>TaskSources(*, timeout_seconds, examples=(), commands=dict(), task_timeout_seconds=None)</code>  <code>dataclass</code>","text":"<p>Where a configured job's tasks come from, and their budget.</p> <p>The job's other keys are :class:<code>~substrax.compute.JobSpec</code> fields.</p> <p>Attributes:</p> Name Type Description <code>timeout_seconds</code> <code>float</code> <p>The job's budget, which is also a <code>JobSpec</code> field.</p> <code>examples</code> <code>tuple[str, ...]</code> <p>Directories, relative to the project, whose example scripts become tasks.</p> <code>commands</code> <code>Mapping[str, tuple[str, ...]]</code> <p>Further tasks, by name, run after the examples.</p> <code>task_timeout_seconds</code> <code>float | None</code> <p>Each task's budget; <code>timeout_seconds</code> when <code>None</code>.</p>"},{"location":"api/compute/#substrax.compute.config.ComputeConfig","title":"<code>ComputeConfig(*, backend=None, backends=dict(), jobs=dict())</code>  <code>dataclass</code>","text":"<p>A project's compute configuration.</p> <p>Attributes:</p> Name Type Description <code>backend</code> <code>str | None</code> <p>The backend used when none is named; <code>None</code> requires naming one.</p> <code>backends</code> <code>Mapping[str, Mapping[str, JsonValue]]</code> <p>Each backend's settings, by backend name.</p> <code>jobs</code> <code>Mapping[str, Mapping[str, JsonValue]]</code> <p>Each job's table, by name, read into a <code>JobSpec</code> when the job is resolved.</p>"},{"location":"api/compute/#substrax.compute.config.read_compute_config","title":"<code>read_compute_config(project)</code>","text":"<p>Read the <code>[tool.substrax.compute]</code> table of the project's pyproject.toml.</p> <p>Parameters:</p> Name Type Description Default <code>project</code> <code>Path</code> <p>The project root.</p> required <p>Returns:</p> Type Description <code>ComputeConfig</code> <p>The configuration.</p> <p>Raises:</p> Type Description <code>LookupError</code> <p>If pyproject.toml has no such table.</p> <code>ValueError</code> <p>If a key is unknown or a value is of the wrong type.</p>"},{"location":"api/compute/#substrax.compute.config.resolve_job","title":"<code>resolve_job(config, name, *, project, accelerator=None)</code>","text":"<p>Build the spec of a configured job, listing its examples now.</p> <p>The job's table holds <code>examples</code>, <code>commands</code> and <code>task_timeout_seconds</code>, which make its tasks, and otherwise the fields of :class:<code>~substrax.compute.JobSpec</code> under their own names.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>ComputeConfig</code> <p>The project's configuration.</p> required <code>name</code> <code>str</code> <p>The job.</p> required <code>project</code> <code>Path</code> <p>The project root.</p> required <code>accelerator</code> <code>Accelerator | None</code> <p>Replaces the configured accelerator for this run.</p> <code>None</code> <p>Returns:</p> Type Description <code>JobSpec</code> <p>The spec.</p> <p>Raises:</p> Type Description <code>KeyError</code> <p>If the job is not configured; the message lists the configured ones.</p> <code>FileNotFoundError</code> <p>If an examples directory does not exist.</p> <code>ValueError</code> <p>If a key of the job's table is unknown or a value of the wrong type.</p>"},{"location":"api/compute/#substrax.compute.config.parse_accelerator","title":"<code>parse_accelerator(text)</code>","text":"<p>Read an accelerator written as <code>KIND</code> or <code>KIND:COUNT</code>, such as <code>H100:8</code>.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>The accelerator.</p> required <p>Returns:</p> Type Description <code>Accelerator</code> <p>It.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>text</code> is not of that form.</p>"},{"location":"api/compute/#substrax.compute.config.require_locked_substrax","title":"<code>require_locked_substrax(project)</code>","text":"<p>Return the substrax the project locks, which the worker runs from.</p> <p>Parameters:</p> Name Type Description Default <code>project</code> <code>Path</code> <p>The project root.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The locked version, or <code>\"editable\"</code> when the project is substrax or installs it from a</p> <code>str</code> <p>path.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If the project has no uv.lock.</p> <code>LookupError</code> <p>If its uv.lock does not include substrax.</p>"},{"location":"api/compute/#substrax.compute.worker","title":"<code>substrax.compute.worker</code>","text":"<p>The program every backend runs on the remote machine: a job's tasks, their logs and a manifest.</p> <p>Run as <code>python -m substrax.compute.worker --spec spec.json --project . --outputs /outputs</code> from the project's environment. Each task runs from the project root with <code>AVITAI_OUTPUT_DIR</code> set to <code>&lt;outputs&gt;/&lt;task&gt;</code>, so whatever it writes through :func:<code>~substrax.artifacts.resolve_output_dir</code> lands beside its <code>stdout.log</code> and <code>stderr.log</code>. The worker writes <code>&lt;outputs&gt;/manifest.json</code> after every task, so a run cut short still says what finished. Its exit code is 0 when every task succeeded and 1 otherwise.</p>"},{"location":"api/compute/#substrax.compute.worker.TaskStatus","title":"<code>TaskStatus</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>How a task ended.</p>"},{"location":"api/compute/#substrax.compute.worker.TaskOutcome","title":"<code>TaskOutcome(*, name, argv, status, returncode, seconds, outputs)</code>  <code>dataclass</code>","text":"<p>What one task did.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>The task.</p> <code>argv</code> <code>tuple[str, ...]</code> <p>The command that ran, with <code>python</code> resolved to the interpreter.</p> <code>status</code> <code>TaskStatus</code> <p>How it ended.</p> <code>returncode</code> <code>int | None</code> <p>Its exit code; <code>None</code> when it could not start.</p> <code>seconds</code> <code>float</code> <p>Wall-clock seconds from start to end.</p> <code>outputs</code> <code>tuple[str, ...]</code> <p>The files it wrote under its directory, relative to it, without the logs.</p>"},{"location":"api/compute/#substrax.compute.worker.JobManifest","title":"<code>JobManifest(*, schema_version=MANIFEST_VERSION, job, tasks=(), finished=False)</code>  <code>dataclass</code>","text":"<p>What a run of a job did, task by task.</p> <p>Attributes:</p> Name Type Description <code>schema_version</code> <code>int</code> <p>The layout of this record.</p> <code>job</code> <code>str</code> <p>The job's name.</p> <code>tasks</code> <code>tuple[TaskOutcome, ...]</code> <p>The tasks that ran, in order.</p> <code>finished</code> <code>bool</code> <p>Whether the worker ran every task; a run cut short leaves it <code>False</code>.</p>"},{"location":"api/compute/#substrax.compute.worker.JobManifest.succeeded","title":"<code>succeeded</code>  <code>property</code>","text":"<p>Whether every task succeeded.</p>"},{"location":"api/compute/#substrax.compute.worker.read_manifest","title":"<code>read_manifest(data)</code>","text":"<p>Read a manifest from its parsed JSON object.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, JsonValue]</code> <p>The JSON object the worker wrote.</p> required <p>Returns:</p> Type Description <code>JobManifest</code> <p>The manifest.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the manifest's version is not this release's, or a field does not match its annotation.</p>"},{"location":"api/compute/#substrax.compute.worker.run_job","title":"<code>run_job(spec, *, project, outputs, echo=True, python=sys.executable)</code>","text":"<p>Run every task of <code>spec</code> in order and write the manifest after each.</p> <p>Each task runs for at most its own budget or what is left of the job's, whichever is less; a task the job's budget leaves no time for is recorded as timed out without starting.</p> <p>Parameters:</p> Name Type Description Default <code>spec</code> <code>JobSpec</code> <p>The job.</p> required <code>project</code> <code>Path</code> <p>The project root each task runs from.</p> required <code>outputs</code> <code>Path</code> <p>The directory the run's logs, outputs and manifest go to.</p> required <code>echo</code> <code>bool</code> <p>Also copy each line a task writes to this process's standard output or error, prefixed with the task's name, so the provider's log shows the run as it goes.</p> <code>True</code> <code>python</code> <code>str</code> <p>The interpreter a task's leading <code>\"python\"</code> names.</p> <code>executable</code> <p>Returns:</p> Type Description <code>JobManifest</code> <p>The manifest.</p>"},{"location":"api/compute/#substrax.compute.worker.main","title":"<code>main(argv=None)</code>","text":"<p>Run the job a spec file describes.</p> <p>Parameters:</p> Name Type Description Default <code>argv</code> <code>Sequence[str] | None</code> <p>The command-line arguments; this process's when <code>None</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>int</code> <p>0 when every task succeeded, 1 otherwise.</p>"},{"location":"api/compute/#substrax.compute.registry","title":"<code>substrax.compute.registry</code>","text":"<p>Backends by name, from the <code>substrax.compute.backends</code> entry-point group.</p> <p>substrax registers its own backends (<code>local</code>, <code>modal</code>, <code>skypilot</code>) in that group, and any other distribution adds a provider by registering a :data:<code>~substrax.compute.backend.BackendFactory</code> there, so a built-in backend and a third-party one load the same way. A backend's module is imported only when its name is chosen; one whose provider library is missing raises the <code>ImportError</code> its module raises, naming the extra to install.</p>"},{"location":"api/compute/#substrax.compute.registry.UnknownBackendError","title":"<code>UnknownBackendError</code>","text":"<p>               Bases: <code>LookupError</code></p> <p>No registered backend has the requested name.</p>"},{"location":"api/compute/#substrax.compute.registry.backend_names","title":"<code>backend_names(*, discover=entry_points)</code>","text":"<p>The names of the registered backends, sorted.</p> <p>Parameters:</p> Name Type Description Default <code>discover</code> <code>Callable[..., EntryPoints]</code> <p>Lists entry points by group; :func:<code>importlib.metadata.entry_points</code> by default.</p> <code>entry_points</code> <p>Returns:</p> Type Description <code>tuple[str, ...]</code> <p>The names.</p>"},{"location":"api/compute/#substrax.compute.registry.load_backend","title":"<code>load_backend(name, settings=None, *, discover=entry_points)</code>","text":"<p>Build the backend registered under <code>name</code> from its settings.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The backend's name.</p> required <code>settings</code> <code>Mapping[str, JsonValue] | None</code> <p>Its settings table; empty when <code>None</code>.</p> <code>None</code> <code>discover</code> <code>Callable[..., EntryPoints]</code> <p>Lists entry points by group; :func:<code>importlib.metadata.entry_points</code> by default.</p> <code>entry_points</code> <p>Returns:</p> Type Description <code>ComputeBackend</code> <p>The backend.</p> <p>Raises:</p> Type Description <code>UnknownBackendError</code> <p>If no backend has that name; the message lists the registered ones.</p> <code>TypeError</code> <p>If the entry point builds something that is not a backend.</p>"},{"location":"api/compute/#substrax.compute.local","title":"<code>substrax.compute.local</code>","text":"<p>The <code>local</code> backend: runs a job's worker on this machine, detached from the shell.</p> <p>It runs the project's current environment as it is (<code>uv run --no-sync</code>), so a job's <code>extras</code> and <code>accelerator</code> are not provisioned here: the machine has what it has. It is the reference backend the contract tests run against, and it serves a developer with a local accelerator.</p> <p>Settings (<code>[tool.substrax.compute.backends.local]</code>):</p> <ul> <li><code>python</code>: the interpreter command that runs the worker, such as <code>\".venv/bin/python\"</code>;   <code>uv run --no-sync --project &lt;project&gt; python</code> when unset.</li> <li><code>state_dir</code>: where runs are kept; :func:<code>~substrax.compute.backend.default_state_dir</code> when   unset.</li> </ul>"},{"location":"api/compute/#substrax.compute.local.LocalSettings","title":"<code>LocalSettings(*, python=None, state_dir=None)</code>  <code>dataclass</code>","text":"<p>The <code>local</code> backend's settings.</p> <p>Attributes:</p> Name Type Description <code>python</code> <code>str | None</code> <p>The interpreter command that runs the worker; the project's through <code>uv</code> when <code>None</code>.</p> <code>state_dir</code> <code>str | None</code> <p>Where runs are kept; the default state directory when <code>None</code>.</p>"},{"location":"api/compute/#substrax.compute.local.LocalBackend","title":"<code>LocalBackend(settings=None)</code>","text":"<p>Runs jobs on this machine.</p> <p>Read the backend's settings.</p> <p>Parameters:</p> Name Type Description Default <code>settings</code> <code>Mapping[str, JsonValue] | None</code> <p>The settings table; every setting has a default.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If a setting is unknown or of the wrong type.</p>"},{"location":"api/compute/#substrax.compute.local.LocalBackend.submit","title":"<code>submit(spec, *, project)</code>","text":"<p>Start the worker on <code>spec</code> in the background and return its handle.</p> <p>Parameters:</p> Name Type Description Default <code>spec</code> <code>JobSpec</code> <p>The job.</p> required <code>project</code> <code>Path</code> <p>The project root.</p> required <p>Returns:</p> Type Description <code>RunHandle</code> <p>The run's handle.</p>"},{"location":"api/compute/#substrax.compute.local.LocalBackend.status","title":"<code>status(handle)</code>","text":"<p>Where the run is: from its manifest when it finished, else from its process.</p> <p>Parameters:</p> Name Type Description Default <code>handle</code> <code>RunHandle</code> <p>The run.</p> required <p>Returns:</p> Type Description <code>RunState</code> <p>Its state.</p>"},{"location":"api/compute/#substrax.compute.local.LocalBackend.logs","title":"<code>logs(handle, *, follow)</code>","text":"<p>The worker's output lines, including every task's, prefixed with the task's name.</p> <p>Parameters:</p> Name Type Description Default <code>handle</code> <code>RunHandle</code> <p>The run.</p> required <code>follow</code> <code>bool</code> <p>Keep reading until the run ends.</p> required <p>Yields:</p> Name Type Description <code>str</code> <code>str</code> <p>Each line, without its line ending.</p>"},{"location":"api/compute/#substrax.compute.local.LocalBackend.fetch","title":"<code>fetch(handle, destination)</code>","text":"<p>Copy the run's outputs to <code>destination / handle.run_id</code>.</p> <p>Parameters:</p> Name Type Description Default <code>handle</code> <code>RunHandle</code> <p>The run.</p> required <code>destination</code> <code>Path</code> <p>The directory to copy into.</p> required <p>Returns:</p> Type Description <code>Path</code> <p>The copy.</p>"},{"location":"api/compute/#substrax.compute.local.LocalBackend.cancel","title":"<code>cancel(handle)</code>","text":"<p>Stop the worker and its tasks; a run that already ended is left as it is.</p> <p>Parameters:</p> Name Type Description Default <code>handle</code> <code>RunHandle</code> <p>The run.</p> required"},{"location":"api/compute/#substrax.compute.modal_backend","title":"<code>substrax.compute.modal_backend</code>","text":"<p>The <code>modal</code> backend: runs a job on Modal (https://modal.com); needs <code>substrax[modal]</code>.</p> <p>The image is built in layers keyed on what they depend on, so editing the project's source rebuilds only the last two:</p> <ol> <li><code>Image.uv_sync</code> installs the locked dependencies and the job's extras, from <code>pyproject.toml</code>    and <code>uv.lock</code> alone, on this interpreter's Python version, which the serialized worker    needs;</li> <li>the project's source is copied in;</li> <li>the project itself is installed into that environment without dependencies.</li> </ol> <p><code>Image.uv_sync</code> needs a Modal image builder of 2025.06 or later, which is a workspace setting.</p> <p>The worker runs in a Modal function, detached from the submitting shell, with the outputs volume mounted at <code>/outputs</code> and each of the job's mounts at its path. Logs come from <code>modal app logs</code>, the provider's own command, run by this interpreter.</p> <p>Settings (<code>[tool.substrax.compute.backends.modal]</code>):</p> <ul> <li><code>outputs_volume</code> (required): the Modal Volume the runs' outputs go to, created when missing.</li> <li><code>volumes</code>: the Modal Volume behind each of a job's mounts, by mount name.</li> <li><code>environment</code>: the Modal environment; the profile's default when unset.</li> <li><code>ignore</code>: patterns of project files left out of the image, <code>.dockerignore</code> style.</li> <li><code>state_dir</code>: where cancellations are recorded; the default state directory when unset.</li> </ul>"},{"location":"api/compute/#substrax.compute.modal_backend.ModalSettings","title":"<code>ModalSettings(*, outputs_volume, volumes=dict(), environment=None, ignore=_DEFAULT_IGNORE, state_dir=None)</code>  <code>dataclass</code>","text":"<p>The <code>modal</code> backend's settings; see the module documentation.</p> <p>Attributes:</p> Name Type Description <code>outputs_volume</code> <code>str</code> <p>The Volume the runs' outputs go to.</p> <code>volumes</code> <code>Mapping[str, str]</code> <p>The Volume behind each mount, by mount name.</p> <code>environment</code> <code>str | None</code> <p>The Modal environment; the profile's default when <code>None</code>.</p> <code>ignore</code> <code>tuple[str, ...]</code> <p>Project files left out of the image.</p> <code>state_dir</code> <code>str | None</code> <p>Where cancellations are recorded.</p>"},{"location":"api/compute/#substrax.compute.modal_backend.ModalBackend","title":"<code>ModalBackend(settings)</code>","text":"<p>Runs jobs on Modal.</p> <p>Read the backend's settings.</p> <p>Parameters:</p> Name Type Description Default <code>settings</code> <code>Mapping[str, JsonValue]</code> <p>The settings table; <code>outputs_volume</code> is required.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If a setting is missing, unknown or of the wrong type.</p>"},{"location":"api/compute/#substrax.compute.modal_backend.ModalBackend.submit","title":"<code>submit(spec, *, project)</code>","text":"<p>Build the image and spawn the worker in a detached app.</p> <p>Parameters:</p> Name Type Description Default <code>spec</code> <code>JobSpec</code> <p>The job.</p> required <code>project</code> <code>Path</code> <p>The project root.</p> required <p>Returns:</p> Type Description <code>RunHandle</code> <p>The run's handle.</p>"},{"location":"api/compute/#substrax.compute.modal_backend.ModalBackend.status","title":"<code>status(handle)</code>","text":"<p>Where the run is: running while its call has no result, then from its manifest.</p> <p>Parameters:</p> Name Type Description Default <code>handle</code> <code>RunHandle</code> <p>The run.</p> required <p>Returns:</p> Type Description <code>RunState</code> <p>Its state.</p>"},{"location":"api/compute/#substrax.compute.modal_backend.ModalBackend.logs","title":"<code>logs(handle, *, follow)</code>","text":"<p>The app's log lines, from <code>modal app logs</code>.</p> <p>Parameters:</p> Name Type Description Default <code>handle</code> <code>RunHandle</code> <p>The run.</p> required <code>follow</code> <code>bool</code> <p>Keep reading until the run ends.</p> required <p>Yields:</p> Name Type Description <code>str</code> <code>str</code> <p>Each line, without its line ending.</p>"},{"location":"api/compute/#substrax.compute.modal_backend.ModalBackend.fetch","title":"<code>fetch(handle, destination)</code>","text":"<p>Copy the run's directory of the outputs volume to <code>destination / handle.run_id</code>.</p> <p>Parameters:</p> Name Type Description Default <code>handle</code> <code>RunHandle</code> <p>The run.</p> required <code>destination</code> <code>Path</code> <p>The directory to copy into.</p> required <p>Returns:</p> Type Description <code>Path</code> <p>The copy.</p>"},{"location":"api/compute/#substrax.compute.modal_backend.ModalBackend.cancel","title":"<code>cancel(handle)</code>","text":"<p>Cancel the call and stop its container; a run that already ended is left as it is.</p> <p>Parameters:</p> Name Type Description Default <code>handle</code> <code>RunHandle</code> <p>The run.</p> required"},{"location":"api/compute/#substrax.compute.modal_backend.run_worker","title":"<code>run_worker(spec_json, run_id, project, outputs, volume)</code>","text":"<p>Run the job inside the Modal function and commit its outputs.</p> <p>Parameters:</p> Name Type Description Default <code>spec_json</code> <code>str</code> <p>The job spec as JSON text.</p> required <code>run_id</code> <code>str</code> <p>The run, whose directory under <code>outputs</code> the files go to.</p> required <code>project</code> <code>str</code> <p>The project's path in the container.</p> required <code>outputs</code> <code>str</code> <p>The outputs volume's path in the container.</p> required <code>volume</code> <code>str</code> <p>The outputs volume's name.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>Whether every task succeeded.</p>"},{"location":"api/compute/#substrax.compute.skypilot_backend","title":"<code>substrax.compute.skypilot_backend</code>","text":"<p>The <code>skypilot</code> backend: runs a job through SkyPilot (https://docs.skypilot.ai) on its clouds.</p> <p>SkyPilot is a separate tool, installed the way its documentation says, in an environment of its own (<code>uv tool install --with pip \"skypilot[gcp]\"</code>). This backend never imports it. It writes a SkyPilot task file and runs the <code>sky</code> command:</p> <ul> <li><code>sky launch -c &lt;cluster&gt; -d --down -y</code> provisions a cluster for the run. The cluster syncs the   project as its workdir and installs the locked environment (<code>uv sync --frozen</code>). It runs the   worker, then tears itself down.</li> <li><code>sky queue -o json</code> gives the job's state while the cluster is up.</li> <li><code>sky logs</code> streams its output, and <code>sky cancel</code> then <code>sky down</code> stop it.</li> </ul> <p>The outputs go to a bucket mounted at <code>/outputs</code>, so they outlive the cluster. They are fetched with the bucket's own command line tool: <code>gcloud storage</code> for <code>gs://</code>, <code>aws s3</code> for <code>s3://</code>.</p> <p>Settings (<code>[tool.substrax.compute.backends.skypilot]</code>):</p> <ul> <li><code>outputs_bucket</code> (required): the bucket URI the runs' outputs go to, such as   <code>gs://avitai-runs</code>.</li> <li><code>buckets</code>: the bucket URI behind each of a job's mounts, by mount name.</li> <li><code>infra</code>: where to run, in SkyPilot's <code>infra</code> syntax, such as <code>gcp</code> or <code>gcp/us-central1</code>;   any enabled cloud when unset.</li> <li><code>use_spot</code>: run on spot instances.</li> <li><code>sky</code>: the <code>sky</code> command; <code>sky</code> on <code>PATH</code> by default.</li> <li><code>worker_python</code>: the command that starts Python in the synced environment;   <code>uv run --no-sync python</code> by default.</li> <li><code>state_dir</code>: where task files and cancellations are kept; the default state directory when   unset.</li> </ul>"},{"location":"api/compute/#substrax.compute.skypilot_backend.SkyPilotSettings","title":"<code>SkyPilotSettings(*, outputs_bucket, buckets=dict(), infra=None, use_spot=False, sky='sky', worker_python='uv run --no-sync python', state_dir=None)</code>  <code>dataclass</code>","text":"<p>The <code>skypilot</code> backend's settings; see the module documentation.</p> <p>Attributes:</p> Name Type Description <code>outputs_bucket</code> <code>str</code> <p>The bucket URI the runs' outputs go to.</p> <code>buckets</code> <code>Mapping[str, str]</code> <p>The bucket URI behind each mount, by mount name.</p> <code>infra</code> <code>str | None</code> <p>Where to run, in SkyPilot's <code>infra</code> syntax.</p> <code>use_spot</code> <code>bool</code> <p>Run on spot instances.</p> <code>sky</code> <code>str</code> <p>The <code>sky</code> command.</p> <code>worker_python</code> <code>str</code> <p>The command that starts Python in the synced environment.</p> <code>state_dir</code> <code>str | None</code> <p>Where task files and cancellations are kept.</p>"},{"location":"api/compute/#substrax.compute.skypilot_backend.SkyPilotBackend","title":"<code>SkyPilotBackend(settings)</code>","text":"<p>Runs jobs through SkyPilot's <code>sky</code> command.</p> <p>Read the backend's settings.</p> <p>Parameters:</p> Name Type Description Default <code>settings</code> <code>Mapping[str, JsonValue]</code> <p>The settings table; <code>outputs_bucket</code> is required.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If a setting is missing, unknown or of the wrong type, or the outputs bucket's scheme has no known download tool.</p>"},{"location":"api/compute/#substrax.compute.skypilot_backend.SkyPilotBackend.submit","title":"<code>submit(spec, *, project)</code>","text":"<p>Write the task file and launch it on a cluster of its own.</p> <p>Parameters:</p> Name Type Description Default <code>spec</code> <code>JobSpec</code> <p>The job.</p> required <code>project</code> <code>Path</code> <p>The project root.</p> required <p>Returns:</p> Type Description <code>RunHandle</code> <p>The run's handle, once the job is submitted on the cluster.</p>"},{"location":"api/compute/#substrax.compute.skypilot_backend.SkyPilotBackend.status","title":"<code>status(handle)</code>","text":"<p>The job's state from <code>sky queue</code> while its cluster is up, else from its manifest.</p> <p>Parameters:</p> Name Type Description Default <code>handle</code> <code>RunHandle</code> <p>The run.</p> required <p>Returns:</p> Type Description <code>RunState</code> <p>Its state.</p>"},{"location":"api/compute/#substrax.compute.skypilot_backend.SkyPilotBackend.logs","title":"<code>logs(handle, *, follow)</code>","text":"<p>The job's output lines, from <code>sky logs</code>.</p> <p>Parameters:</p> Name Type Description Default <code>handle</code> <code>RunHandle</code> <p>The run.</p> required <code>follow</code> <code>bool</code> <p>Keep reading until the job ends.</p> required <p>Yields:</p> Name Type Description <code>str</code> <code>str</code> <p>Each line, without its line ending.</p>"},{"location":"api/compute/#substrax.compute.skypilot_backend.SkyPilotBackend.fetch","title":"<code>fetch(handle, destination)</code>","text":"<p>Download the run's directory of the outputs bucket to <code>destination / handle.run_id</code>.</p> <p>Parameters:</p> Name Type Description Default <code>handle</code> <code>RunHandle</code> <p>The run.</p> required <code>destination</code> <code>Path</code> <p>The directory to copy into.</p> required <p>Returns:</p> Type Description <code>Path</code> <p>The copy.</p>"},{"location":"api/compute/#substrax.compute.skypilot_backend.SkyPilotBackend.cancel","title":"<code>cancel(handle)</code>","text":"<p>Cancel the job and tear its cluster down; a run that already ended is left as it is.</p> <p>Parameters:</p> Name Type Description Default <code>handle</code> <code>RunHandle</code> <p>The run.</p> required"},{"location":"api/devices/","title":"Devices","text":"<p><code>substrax.devices</code> answers two questions: what hardware is this process running on, and where should a batch go.</p> <pre><code>from substrax.devices import DevicePlacement, detect_devices, get_batch_size_recommendation\n\ninfo = detect_devices()          # platform, kind, count, memory, name\nplacement = DevicePlacement()    # defaults to jax.devices()\nbatch = placement.place_on_device(batch)\nrecommendation = get_batch_size_recommendation()  # for the detected hardware\n</code></pre> <p><code>DeviceInfo</code> is a frozen dataclass; <code>detect_devices()</code> reads it from <code>jax.devices()</code> and never imports an accelerator plugin itself. <code>DevicePlacement</code> carries the batch-size recommendation table per <code>HardwareType</code>.</p>"},{"location":"api/devices/#substrax.devices","title":"<code>substrax.devices</code>","text":"<p>Device information and device placement.</p>"},{"location":"api/devices/#substrax.devices.DeviceInfo","title":"<code>DeviceInfo(*, platform, kind, count, device_kinds)</code>  <code>dataclass</code>","text":"<p>Snapshot of the devices visible to the current JAX process.</p> <p>Attributes:</p> Name Type Description <code>platform</code> <code>str</code> <p>The default backend name exactly as JAX reports it.</p> <code>kind</code> <code>DeviceKind</code> <p>The accelerator class of that backend.</p> <code>count</code> <code>int</code> <p>The number of visible devices.</p> <code>device_kinds</code> <code>tuple[str, ...]</code> <p>One <code>device_kind</code> string per visible device, in JAX order.</p>"},{"location":"api/devices/#substrax.devices.DeviceInfo.has_accelerator","title":"<code>has_accelerator</code>  <code>property</code>","text":"<p>Whether the default backend is anything other than the CPU.</p>"},{"location":"api/devices/#substrax.devices.DeviceKind","title":"<code>DeviceKind</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>The accelerator class behind a JAX backend name.</p>"},{"location":"api/devices/#substrax.devices.DeviceKind.from_platform","title":"<code>from_platform(platform)</code>  <code>classmethod</code>","text":"<p>Map a backend name as <code>jax.default_backend()</code> reports it onto a kind.</p> <p>Parameters:</p> Name Type Description Default <code>platform</code> <code>str</code> <p>The backend name, in any case (<code>\"gpu\"</code>, <code>\"cuda\"</code>, <code>\"METAL\"</code>).</p> required <p>Returns:</p> Type Description <code>DeviceKind</code> <p>The matching kind, or <code>OTHER</code> for a backend this package has not seen.</p>"},{"location":"api/devices/#substrax.devices.BatchSizeRecommendation","title":"<code>BatchSizeRecommendation(min_batch_size, optimal_batch_size, critical_batch_size, max_memory_batch_size=None, notes='')</code>  <code>dataclass</code>","text":"<p>Hardware-specific batch size recommendations.</p> <p>Attributes:</p> Name Type Description <code>min_batch_size</code> <code>int</code> <p>Minimum batch size for reasonable efficiency.</p> <code>optimal_batch_size</code> <code>int</code> <p>Optimal batch size for peak throughput.</p> <code>critical_batch_size</code> <code>int</code> <p>Critical batch size for reaching roofline (per JAX guide).</p> <code>max_memory_batch_size</code> <code>int | None</code> <p>Maximum batch size before OOM (estimate).</p> <code>notes</code> <code>str</code> <p>Additional notes about the recommendation.</p>"},{"location":"api/devices/#substrax.devices.DevicePlacement","title":"<code>DevicePlacement(default_device=None)</code>","text":"<p>Utility class for explicit device placement of JAX arrays.</p> <p>This class provides methods for placing arrays on specific devices, distributing batches across devices using sharding, and providing hardware-aware batch size recommendations.</p> Example <pre><code>placement = DevicePlacement()\ndata = jnp.ones((4, 8))\ngpu_data = placement.place_on_device(data, jax.devices(\"gpu\")[0])  # Place on first GPU\nrec = placement.get_batch_size_recommendation()  # Get batch size recommendation\nprint(f\"Optimal batch: {rec.optimal_batch_size}\")\n</code></pre> <p>Initialize DevicePlacement.</p> <p>Parameters:</p> Name Type Description Default <code>default_device</code> <code>Device | None</code> <p>Default device to use when none is specified. If None, lazily resolves to jax.devices()[0] on first access.</p> <code>None</code>"},{"location":"api/devices/#substrax.devices.DevicePlacement.default_device","title":"<code>default_device</code>  <code>property</code>","text":"<p>Get the default device, lazily resolving on first access.</p>"},{"location":"api/devices/#substrax.devices.DevicePlacement.hardware_type","title":"<code>hardware_type</code>  <code>property</code>","text":"<p>Get the detected hardware type, lazily resolving on first access.</p>"},{"location":"api/devices/#substrax.devices.DevicePlacement.num_devices","title":"<code>num_devices</code>  <code>property</code>","text":"<p>Get the number of available devices.</p>"},{"location":"api/devices/#substrax.devices.DevicePlacement.place_on_device","title":"<code>place_on_device(data, device=None)</code>","text":"<p>Place data on a specific device.</p> <p>This uses jax.device_put for explicit device placement, ensuring data is transferred to the target device.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>PyTree</code> <p>PyTree of JAX arrays to place on device.</p> required <code>device</code> <code>Device | None</code> <p>Target device. If None, uses the default device.</p> <code>None</code> <p>Returns:</p> Type Description <code>PyTree</code> <p>PyTree with arrays placed on the specified device.</p> Example <pre><code>data = {\"images\": jnp.ones((4, 28, 28, 3))}\ngpu_data = placement.place_on_device(data, jax.devices(\"gpu\")[0])\n</code></pre>"},{"location":"api/devices/#substrax.devices.DevicePlacement.replicate_across_devices","title":"<code>replicate_across_devices(data, devices=None)</code>","text":"<p>Replicate data across all specified devices.</p> <p>Creates a copy of the data on each device, useful for broadcasting model weights or constants.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>PyTree</code> <p>PyTree of JAX arrays to replicate.</p> required <code>devices</code> <code>list[Device] | None</code> <p>List of devices to replicate to. If None, uses all devices.</p> <code>None</code> <p>Returns:</p> Type Description <code>PyTree</code> <p>PyTree with arrays replicated across devices.</p>"},{"location":"api/devices/#substrax.devices.DevicePlacement.shard_batch_dim","title":"<code>shard_batch_dim(data, mesh, batch_axis=0, mesh_axis='data')</code>","text":"<p>Shard data along the batch dimension.</p> <p>This is the most common sharding pattern for data-parallel training, where each device processes a slice of the batch.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>PyTree</code> <p>PyTree of JAX arrays to shard.</p> required <code>mesh</code> <code>Mesh</code> <p>Device mesh to shard across.</p> required <code>batch_axis</code> <code>int</code> <p>The axis index representing the batch dimension.</p> <code>0</code> <code>mesh_axis</code> <code>str</code> <p>The mesh axis name to shard along.</p> <code>'data'</code> <p>Returns:</p> Type Description <code>PyTree</code> <p>PyTree with arrays sharded along the batch dimension.</p>"},{"location":"api/devices/#substrax.devices.DevicePlacement.get_batch_size_recommendation","title":"<code>get_batch_size_recommendation(hardware_type=None)</code>","text":"<p>Get batch size recommendation for the current hardware.</p> <p>Parameters:</p> Name Type Description Default <code>hardware_type</code> <code>HardwareType | None</code> <p>Override hardware type. If None, uses detected type.</p> <code>None</code> <p>Returns:</p> Type Description <code>BatchSizeRecommendation</code> <p>BatchSizeRecommendation with hardware-specific values.</p>"},{"location":"api/devices/#substrax.devices.DevicePlacement.validate_batch_size","title":"<code>validate_batch_size(batch_size, warn_suboptimal=True)</code>","text":"<p>Validate batch size against hardware recommendations.</p> <p>Parameters:</p> Name Type Description Default <code>batch_size</code> <code>int</code> <p>The batch size to validate.</p> required <code>warn_suboptimal</code> <code>bool</code> <p>Whether to warn for suboptimal (but valid) sizes.</p> <code>True</code> <p>Returns:</p> Type Description <code>tuple[bool, str]</code> <p>Tuple of (is_valid, message).</p>"},{"location":"api/devices/#substrax.devices.DevicePlacement.get_device_info","title":"<code>get_device_info()</code>","text":"<p>Get information about available devices.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dictionary containing device information.</p>"},{"location":"api/devices/#substrax.devices.HardwareType","title":"<code>HardwareType</code>","text":"<p>               Bases: <code>Enum</code></p> <p>Enumeration of supported hardware types.</p>"},{"location":"api/devices/#substrax.devices.detect_devices","title":"<code>detect_devices()</code>","text":"<p>Probe the JAX runtime once and return what it reports.</p> <p>Errors from the runtime (no backend, uninitialised plugins) propagate; a process that cannot see its devices should not be told it is running on a CPU.</p> <p>Returns:</p> Type Description <code>DeviceInfo</code> <p>The device snapshot.</p>"},{"location":"api/devices/#substrax.devices.get_batch_size_recommendation","title":"<code>get_batch_size_recommendation(hardware_type=None)</code>","text":"<p>Get batch size recommendation for current or specified hardware.</p> <p>Parameters:</p> Name Type Description Default <code>hardware_type</code> <code>HardwareType | None</code> <p>Hardware type to get recommendation for.</p> <code>None</code> <p>Returns:</p> Type Description <code>BatchSizeRecommendation</code> <p>BatchSizeRecommendation with hardware-specific values.</p>"},{"location":"api/devices/#substrax.devices.place_on_device","title":"<code>place_on_device(data, device=None)</code>","text":"<p>Convenience function for placing data on a device.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>PyTree</code> <p>PyTree of JAX arrays.</p> required <code>device</code> <code>Device | None</code> <p>Target device. If None, uses first available device.</p> <code>None</code> <p>Returns:</p> Type Description <code>PyTree</code> <p>PyTree with arrays on the specified device.</p>"},{"location":"api/mesh/","title":"Mesh","text":"<p><code>substrax.mesh</code> builds device meshes and describes how arrays are laid out on them.</p> <pre><code>from substrax.mesh import DeviceMeshManager, FSDPStrategy, ShardingConfig, fsdp_rules\n\nmesh = DeviceMeshManager.create_device_mesh({\"data\": 2, \"model\": 4})\nrules = fsdp_rules(data_axis=\"data\", model_axis=\"model\")\nconfig = ShardingConfig(data_parallel_size=2, tensor_parallel_size=4, fsdp_enabled=True)\nstrategy = FSDPStrategy(\"model\", mesh_axis=1, min_weight_size=config.fsdp_min_weight_size)\n</code></pre> <p>Three layers, from low to high:</p> Layer Names Role Meshes <code>DeviceMeshManager</code> <code>jax.make_mesh</code> over the visible devices, plus mesh introspection Rules <code>MeshRules</code>, <code>data_parallel_rules</code>, <code>fsdp_rules</code>, <code>create_named_sharding</code>, <code>partition_spec_for_names</code> Map logical axis names to mesh axes and build <code>NamedSharding</code>s Strategies <code>ShardingConfig</code>, <code>ParallelismConfig</code>, <code>ShardingStrategy</code> and its data-parallel, FSDP, tensor-parallel, pipeline-parallel and multi-dimensional implementations Decide the partition spec of every parameter in a model"},{"location":"api/mesh/#substrax.mesh","title":"<code>substrax.mesh</code>","text":"<p>Device meshes, mesh rules, partition-spec helpers and sharding strategies.</p>"},{"location":"api/mesh/#substrax.mesh.DeviceMeshManager","title":"<code>DeviceMeshManager</code>","text":"<p>Manager for creating and configuring JAX device meshes.</p> <p>This class provides utilities for creating device meshes for different distributed training configurations, including data-parallel, model-parallel, and hybrid approaches.</p>"},{"location":"api/mesh/#substrax.mesh.DeviceMeshManager.create_device_mesh","title":"<code>create_device_mesh(mesh_shape, devices=None, *, axis_types=None)</code>  <code>staticmethod</code>","text":"<p>Create a JAX device mesh with the specified shape.</p> <p>Every axis is <code>AxisType.Auto</code> unless <code>axis_types</code> says otherwise: the data-parallel helpers in :mod:<code>substrax.spmd</code> leave sharding inference to XLA (the gradient all-reduce of a batch-sharded step is inferred from the input sharding). <code>jax.make_mesh</code> defaults to <code>Explicit</code> axes from jax 0.11, under which a contraction over a sharded batch axis, the backward pass of any layer, raises unless every such op names its <code>out_sharding</code>.</p> <p>Parameters:</p> Name Type Description Default <code>mesh_shape</code> <code>dict[str, int] | list[tuple[str, int]]</code> <p>The shape of the mesh, specified either as a dictionary mapping axis names to sizes, or as a list of (name, size) tuples.</p> required <code>devices</code> <code>list[Any] | None</code> <p>Optional list of devices to use. If None, uses all available devices.</p> <code>None</code> <code>axis_types</code> <code>Sequence[AxisType] | None</code> <p>One <code>AxisType</code> per axis; <code>Auto</code> for every axis when None.</p> <code>None</code> <p>Returns:</p> Type Description <code>Mesh</code> <p>A JAX device mesh.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the mesh shape is incompatible with the number of devices, or <code>axis_types</code> does not name one type per axis.</p>"},{"location":"api/mesh/#substrax.mesh.DeviceMeshManager.create_data_parallel_mesh","title":"<code>create_data_parallel_mesh(num_devices=None)</code>  <code>staticmethod</code>","text":"<p>Create a data-parallel device mesh.</p> <p>Parameters:</p> Name Type Description Default <code>num_devices</code> <code>int | None</code> <p>Optional number of devices to use. If None, uses all available devices.</p> <code>None</code> <p>Returns:</p> Type Description <code>Mesh</code> <p>A JAX device mesh configured for data-parallel training.</p>"},{"location":"api/mesh/#substrax.mesh.DeviceMeshManager.create_model_parallel_mesh","title":"<code>create_model_parallel_mesh(num_devices)</code>  <code>staticmethod</code>","text":"<p>Create a model-parallel device mesh.</p> <p>Parameters:</p> Name Type Description Default <code>num_devices</code> <code>int</code> <p>Number of devices to use for model parallelism.</p> required <p>Returns:</p> Type Description <code>Mesh</code> <p>A JAX device mesh configured for model-parallel training.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If there aren't enough devices available.</p>"},{"location":"api/mesh/#substrax.mesh.DeviceMeshManager.create_hybrid_mesh","title":"<code>create_hybrid_mesh(data_parallel_size, model_parallel_size)</code>  <code>staticmethod</code>","text":"<p>Create a hybrid data-parallel and model-parallel device mesh.</p> <p>Parameters:</p> Name Type Description Default <code>data_parallel_size</code> <code>int</code> <p>Number of devices to use for data parallelism.</p> required <code>model_parallel_size</code> <code>int</code> <p>Number of devices to use for model parallelism.</p> required <p>Returns:</p> Type Description <code>Mesh</code> <p>A JAX device mesh configured for hybrid parallel training.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If there aren't enough devices available.</p>"},{"location":"api/mesh/#substrax.mesh.DeviceMeshManager.get_mesh_info","title":"<code>get_mesh_info(mesh)</code>  <code>staticmethod</code>","text":"<p>Get information about a device mesh.</p> <p>Parameters:</p> Name Type Description Default <code>mesh</code> <code>Mesh</code> <p>The device mesh to inspect.</p> required <p>Returns:</p> Type Description <code>dict[str, int | dict[str, int]]</code> <p>A dictionary containing information about the mesh, including the</p> <code>dict[str, int | dict[str, int]]</code> <p>number of devices and the size of each axis.</p>"},{"location":"api/mesh/#substrax.mesh.MeshRules","title":"<code>MeshRules(*, data=None, embed=None, mlp=None, heads=None)</code>  <code>dataclass</code>","text":"<p>Logical-to-physical mesh axis mapping for SPMD sharding.</p> <p>Maps logical dimension names to physical mesh axis names. Following the pattern from Flax NNX FSDP examples.</p> <p>Attributes:</p> Name Type Description <code>data</code> <code>str | None</code> <p>Mesh axis for the batch/data dimension.</p> <code>embed</code> <code>str | None</code> <p>Mesh axis for embedding dimensions.</p> <code>mlp</code> <code>str | None</code> <p>Mesh axis for MLP hidden dimensions.</p> <code>heads</code> <code>str | None</code> <p>Mesh axis for attention head dimensions.</p>"},{"location":"api/mesh/#substrax.mesh.MeshRules.__call__","title":"<code>__call__(*keys)</code>","text":"<p>Look up mesh axes for the given logical dimension names.</p> <p>Parameters:</p> Name Type Description Default <code>*keys</code> <code>str</code> <p>Logical dimension names (must be attributes of MeshRules).</p> <code>()</code> <p>Returns:</p> Type Description <code>tuple[str | None, ...]</code> <p>Tuple of mesh axis names (or None for unmapped dimensions).</p>"},{"location":"api/mesh/#substrax.mesh.DataParallelStrategy","title":"<code>DataParallelStrategy(axis_name, mesh_axis)</code>","text":"<p>               Bases: <code>ShardingStrategy</code></p> <p>Data parallel sharding strategy.</p> <p>Shards the batch dimension across devices while replicating model parameters and computation.</p>"},{"location":"api/mesh/#substrax.mesh.DataParallelStrategy.get_partition_spec","title":"<code>get_partition_spec(tensor_shape)</code>","text":"<p>Get partition spec for data parallel sharding.</p> <p>Only shards the batch dimension, leaves others replicated.</p>"},{"location":"api/mesh/#substrax.mesh.DataParallelStrategy.apply_sharding","title":"<code>apply_sharding(array, mesh)</code>","text":"<p>Apply data parallel sharding to array.</p>"},{"location":"api/mesh/#substrax.mesh.FSDPStrategy","title":"<code>FSDPStrategy(axis_name, mesh_axis, min_weight_size=1024)</code>","text":"<p>               Bases: <code>ShardingStrategy</code></p> <p>Fully Sharded Data Parallel strategy.</p> <p>Shards model parameters across devices to reduce memory usage while maintaining training efficiency.</p> <p>Initialize FSDP strategy.</p> <p>Parameters:</p> Name Type Description Default <code>axis_name</code> <code>str</code> <p>Name of the mesh axis</p> required <code>mesh_axis</code> <code>int</code> <p>Index of the mesh axis</p> required <code>min_weight_size</code> <code>int</code> <p>Minimum first dimension size to enable sharding</p> <code>1024</code>"},{"location":"api/mesh/#substrax.mesh.FSDPStrategy.should_shard_weight","title":"<code>should_shard_weight(weight)</code>","text":"<p>Determine if a weight should be sharded based on its size.</p> <p>Parameters:</p> Name Type Description Default <code>weight</code> <code>Array</code> <p>Weight array to check</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if weight should be sharded, False otherwise</p>"},{"location":"api/mesh/#substrax.mesh.FSDPStrategy.get_partition_spec","title":"<code>get_partition_spec(tensor_shape)</code>","text":"<p>Get partition spec for FSDP sharding.</p> <p>Shards along the first dimension of weight tensors.</p>"},{"location":"api/mesh/#substrax.mesh.FSDPStrategy.get_gradient_partition_spec","title":"<code>get_gradient_partition_spec(tensor_shape)</code>","text":"<p>Get partition spec for gradient sharding (same as weights).</p>"},{"location":"api/mesh/#substrax.mesh.FSDPStrategy.apply_sharding","title":"<code>apply_sharding(array, mesh)</code>","text":"<p>Apply FSDP sharding to array.</p>"},{"location":"api/mesh/#substrax.mesh.MultiDimensionalStrategy","title":"<code>MultiDimensionalStrategy(strategies, config)</code>","text":"<p>Multi-dimensional parallelism strategy combining multiple approaches.</p> <p>Combines different sharding strategies (data, tensor, FSDP, pipeline) to achieve optimal performance for large-scale training.</p> <p>Initialize multi-dimensional strategy.</p> <p>Parameters:</p> Name Type Description Default <code>strategies</code> <code>Mapping[str, ShardingStrategy]</code> <p>Dictionary mapping strategy names to strategy instances</p> required <code>config</code> <code>ParallelismConfig</code> <p>Sharding configuration for the multi-dimensional strategy</p> required"},{"location":"api/mesh/#substrax.mesh.MultiDimensionalStrategy.get_combined_partition_spec","title":"<code>get_combined_partition_spec(tensor_name, tensor_shape)</code>","text":"<p>Get combined partition spec from all strategies.</p> <p>Parameters:</p> Name Type Description Default <code>tensor_name</code> <code>str</code> <p>Name/type of the tensor</p> required <code>tensor_shape</code> <code>tuple[str, ...]</code> <p>Shape dimension names of the tensor</p> required <p>Returns:</p> Type Description <code>PartitionSpec</code> <p>Combined PartitionSpec</p>"},{"location":"api/mesh/#substrax.mesh.MultiDimensionalStrategy.resolve_sharding_conflicts","title":"<code>resolve_sharding_conflicts(tensor_name, proposed_specs)</code>","text":"<p>Resolve conflicts between multiple proposed partition specs.</p> <p>Parameters:</p> Name Type Description Default <code>tensor_name</code> <code>str</code> <p>Name of the tensor</p> required <code>proposed_specs</code> <code>dict[str, PartitionSpec]</code> <p>Dictionary of strategy names to proposed specs</p> required <p>Returns:</p> Type Description <code>PartitionSpec</code> <p>Resolved PartitionSpec</p>"},{"location":"api/mesh/#substrax.mesh.ParallelismConfig","title":"<code>ParallelismConfig(mesh_shape, mesh_axis_names, sharding_config)</code>  <code>dataclass</code>","text":"<p>Complete parallelism configuration including mesh topology.</p> <p>Combines sharding configuration with device mesh setup.</p>"},{"location":"api/mesh/#substrax.mesh.ParallelismConfig.is_valid","title":"<code>is_valid()</code>","text":"<p>Validate that mesh shape matches sharding configuration.</p>"},{"location":"api/mesh/#substrax.mesh.ParallelismConfig.from_sharding_config","title":"<code>from_sharding_config(config)</code>  <code>classmethod</code>","text":"<p>Create parallelism config from sharding configuration.</p>"},{"location":"api/mesh/#substrax.mesh.PipelineParallelStrategy","title":"<code>PipelineParallelStrategy(axis_name, mesh_axis, num_stages)</code>","text":"<p>               Bases: <code>ShardingStrategy</code></p> <p>Pipeline parallel sharding strategy.</p> <p>Distributes model layers across devices to enable pipeline parallelism for very large models that don't fit on single devices.</p> <p>Initialize pipeline parallel strategy.</p> <p>Parameters:</p> Name Type Description Default <code>axis_name</code> <code>str</code> <p>Name of the mesh axis</p> required <code>mesh_axis</code> <code>int</code> <p>Index of the mesh axis</p> required <code>num_stages</code> <code>int</code> <p>Number of pipeline stages</p> required"},{"location":"api/mesh/#substrax.mesh.PipelineParallelStrategy.assign_layers_to_stages","title":"<code>assign_layers_to_stages(num_layers)</code>","text":"<p>Assign layers to pipeline stages.</p> <p>Parameters:</p> Name Type Description Default <code>num_layers</code> <code>int</code> <p>Total number of layers in the model</p> required <p>Returns:</p> Type Description <code>list[int]</code> <p>list of layer counts per stage</p>"},{"location":"api/mesh/#substrax.mesh.PipelineParallelStrategy.get_partition_spec","title":"<code>get_partition_spec(tensor_shape)</code>","text":"<p>Get partition spec for pipeline parallel sharding.</p> <p>Pipeline parallelism doesn't shard individual tensors, but rather assigns entire layers to different devices.</p>"},{"location":"api/mesh/#substrax.mesh.PipelineParallelStrategy.get_forward_communication_pattern","title":"<code>get_forward_communication_pattern()</code>","text":"<p>Get communication pattern for forward pass.</p> <p>Returns:</p> Type Description <code>list[tuple[int, int]]</code> <p>list of (source_stage, dest_stage) pairs</p>"},{"location":"api/mesh/#substrax.mesh.PipelineParallelStrategy.get_backward_communication_pattern","title":"<code>get_backward_communication_pattern()</code>","text":"<p>Get communication pattern for backward pass.</p> <p>Returns:</p> Type Description <code>list[tuple[int, int]]</code> <p>list of (source_stage, dest_stage) pairs</p>"},{"location":"api/mesh/#substrax.mesh.PipelineParallelStrategy.apply_sharding","title":"<code>apply_sharding(array, mesh)</code>","text":"<p>Apply pipeline parallel sharding to array.</p> <p>Pipeline parallelism handles layer assignment rather than tensor sharding.</p>"},{"location":"api/mesh/#substrax.mesh.ShardingConfig","title":"<code>ShardingConfig(data_parallel_size=1, tensor_parallel_size=1, pipeline_parallel_size=1, fsdp_enabled=False, fsdp_min_weight_size=1024)</code>  <code>dataclass</code>","text":"<p>Configuration for multi-dimensional parallelism setup.</p> <p>Defines the parallelism dimensions and FSDP settings for a model.</p>"},{"location":"api/mesh/#substrax.mesh.ShardingConfig.get_total_device_count","title":"<code>get_total_device_count()</code>","text":"<p>Calculate total devices needed for this configuration.</p>"},{"location":"api/mesh/#substrax.mesh.ShardingConfig.from_device_count","title":"<code>from_device_count(device_count)</code>  <code>classmethod</code>","text":"<p>Create optimal sharding config for given device count.</p> <p>Uses heuristics to balance different parallelism dimensions.</p>"},{"location":"api/mesh/#substrax.mesh.ShardingStrategy","title":"<code>ShardingStrategy(axis_name, mesh_axis)</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for sharding strategies.</p> <p>Defines the interface that all sharding strategies must implement for consistent handling of different parallelism types.</p> <p>Initialize sharding strategy.</p> <p>Parameters:</p> Name Type Description Default <code>axis_name</code> <code>str</code> <p>Name of the mesh axis for this strategy</p> required <code>mesh_axis</code> <code>int</code> <p>Index of the mesh axis</p> required"},{"location":"api/mesh/#substrax.mesh.ShardingStrategy.get_partition_spec","title":"<code>get_partition_spec(tensor_shape)</code>  <code>abstractmethod</code>","text":"<p>Get partition specification for a tensor with given shape names.</p> <p>Parameters:</p> Name Type Description Default <code>tensor_shape</code> <code>tuple[str, ...]</code> <p>Tuple of dimension names for the tensor</p> required <p>Returns:</p> Type Description <code>PartitionSpec</code> <p>PartitionSpec defining how to shard the tensor</p>"},{"location":"api/mesh/#substrax.mesh.ShardingStrategy.apply_sharding","title":"<code>apply_sharding(array, mesh)</code>  <code>abstractmethod</code>","text":"<p>Apply sharding to an array using the given mesh.</p> <p>Parameters:</p> Name Type Description Default <code>array</code> <code>Array</code> <p>JAX array to shard</p> required <code>mesh</code> <code>Mesh</code> <p>Device mesh for sharding</p> required <p>Returns:</p> Type Description <code>Array</code> <p>Sharded array</p>"},{"location":"api/mesh/#substrax.mesh.ShardingStrategy.get_sharding_constraints","title":"<code>get_sharding_constraints()</code>","text":"<p>Get sharding constraints for this strategy.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dictionary of sharding constraints</p>"},{"location":"api/mesh/#substrax.mesh.TensorParallelStrategy","title":"<code>TensorParallelStrategy(axis_name, mesh_axis, shard_dimension=None)</code>","text":"<p>               Bases: <code>ShardingStrategy</code></p> <p>Tensor parallel sharding strategy.</p> <p>Shards model computation across devices by splitting tensors along specific dimensions (typically features).</p> <p>Initialize tensor parallel strategy.</p> <p>Parameters:</p> Name Type Description Default <code>axis_name</code> <code>str</code> <p>Name of the mesh axis</p> required <code>mesh_axis</code> <code>int</code> <p>Index of the mesh axis</p> required <code>shard_dimension</code> <code>str | None</code> <p>Preferred dimension to shard ('in_features' or 'out_features')</p> <code>None</code>"},{"location":"api/mesh/#substrax.mesh.TensorParallelStrategy.get_partition_spec","title":"<code>get_partition_spec(tensor_shape)</code>","text":"<p>Get partition spec for tensor parallel sharding.</p>"},{"location":"api/mesh/#substrax.mesh.TensorParallelStrategy.get_linear_weight_spec","title":"<code>get_linear_weight_spec()</code>","text":"<p>Get partition spec for linear layer weights.</p>"},{"location":"api/mesh/#substrax.mesh.TensorParallelStrategy.get_attention_qkv_spec","title":"<code>get_attention_qkv_spec()</code>","text":"<p>Get partition spec for attention QKV projections.</p>"},{"location":"api/mesh/#substrax.mesh.TensorParallelStrategy.get_attention_output_spec","title":"<code>get_attention_output_spec()</code>","text":"<p>Get partition spec for attention output projection.</p>"},{"location":"api/mesh/#substrax.mesh.TensorParallelStrategy.apply_sharding","title":"<code>apply_sharding(array, mesh)</code>","text":"<p>Apply tensor parallel sharding to array.</p>"},{"location":"api/mesh/#substrax.mesh.create_named_sharding","title":"<code>create_named_sharding(mesh, *axis_names)</code>","text":"<p>Create a NamedSharding from a mesh and axis names.</p> <p>Parameters:</p> Name Type Description Default <code>mesh</code> <code>Mesh</code> <p>The device mesh.</p> required <code>*axis_names</code> <code>str | None</code> <p>Axis names for each dimension. Use None for replicated dimensions. If no axes are provided, creates a fully replicated sharding.</p> <code>()</code> <p>Returns:</p> Type Description <code>NamedSharding</code> <p>A JAX NamedSharding object.</p>"},{"location":"api/mesh/#substrax.mesh.data_parallel_rules","title":"<code>data_parallel_rules(data_axis='data')</code>","text":"<p>Create MeshRules for pure data-parallel training.</p> <p>Shards only the batch dimension across devices.</p> <p>Parameters:</p> Name Type Description Default <code>data_axis</code> <code>str</code> <p>Name of the mesh axis for data parallelism.</p> <code>'data'</code> <p>Returns:</p> Type Description <code>MeshRules</code> <p>MeshRules with only the data axis mapped.</p>"},{"location":"api/mesh/#substrax.mesh.fsdp_rules","title":"<code>fsdp_rules(data_axis='data', model_axis='model')</code>","text":"<p>Create MeshRules for Fully Sharded Data Parallel (FSDP) training.</p> <p>Shards the batch dimension across the data axis and model weight dimensions across the model axis.</p> <p>Parameters:</p> Name Type Description Default <code>data_axis</code> <code>str</code> <p>Name of the mesh axis for data parallelism.</p> <code>'data'</code> <code>model_axis</code> <code>str</code> <p>Name of the mesh axis for model parallelism.</p> <code>'model'</code> <p>Returns:</p> Type Description <code>MeshRules</code> <p>MeshRules with data and model axes mapped.</p>"},{"location":"api/mesh/#substrax.mesh.partition_spec_for_names","title":"<code>partition_spec_for_names(rules, *logical_names)</code>","text":"<p>Apply MeshRules to create a PartitionSpec for a tensor.</p> <p>Maps logical dimension names through the rules to produce physical mesh axis assignments.</p> <p>Parameters:</p> Name Type Description Default <code>rules</code> <code>MeshRules</code> <p>The MeshRules mapping to apply.</p> required <code>*logical_names</code> <code>str</code> <p>Logical dimension names for each tensor axis.</p> <code>()</code> <p>Returns:</p> Type Description <code>PartitionSpec</code> <p>A PartitionSpec with physical mesh axis assignments.</p>"},{"location":"api/optim/","title":"Optim","text":"<p><code>substrax.optim</code> turns one specification into an optax transformation or an NNX optimizer, and reads back the learning rate the last update applied.</p> <pre><code>import optax\nfrom flax import nnx\n\nfrom substrax.optim import (\n    EXCLUDE_BIAS_AND_NORM_SCALE,\n    OptimizerConfig,\n    create_optimizer,\n    current_learning_rate,\n)\n\nconfig = OptimizerConfig(\n    optimizer_type=\"adamw\",\n    learning_rate=optax.warmup_cosine_decay_schedule(0.0, 3e-4, warmup_steps=500, decay_steps=10_000),\n    weight_decay=0.05,\n    weight_decay_filter=EXCLUDE_BIAS_AND_NORM_SCALE,\n    gradient_clip_norm=1.0,\n)\noptimizer = create_optimizer(model, config)\n\ngrads = nnx.grad(loss)(model, batch)\noptimizer.update(model, grads)\nprint(float(current_learning_rate(optimizer)))\n</code></pre> Name Behaviour <code>OptimizerConfig</code> Frozen, keyword-only; <code>optimizer_type</code> is one of <code>adam</code>, <code>adamw</code>, <code>sgd</code>, <code>rmsprop</code>, <code>adagrad</code>, <code>lamb</code>, <code>radam</code>, <code>nadam</code> (<code>adam</code> with Nesterov momentum), <code>learning_rate</code> a positive constant or an <code>optax.Schedule</code>, <code>b1</code>, <code>b2</code>, <code>eps</code>, <code>momentum</code> (<code>sgd</code> and <code>rmsprop</code>), <code>weight_decay</code> (<code>adamw</code> and <code>lamb</code>), <code>weight_decay_filter</code>, one of <code>gradient_clip_norm</code> and <code>gradient_clip_value</code> (positive), and <code>wrt</code>; anything else is refused when the configuration is constructed <code>weight_decay_filter</code> A Flax NNX filter naming the parameters that are decayed, the polarity of optax's <code>mask</code>; <code>None</code> decays every leaf in <code>wrt</code>, as <code>torch.optim.AdamW(model.parameters())</code> does; <code>EXCLUDE_BIAS_AND_NORM_SCALE</code> skips biases and normalisation scales, the Hugging Face and timm convention; a string raises, because NNX reads it as a tag, not a path <code>weight_decay_mask(model, config)</code> The filter as a static tree of Python booleans over <code>nnx.state(model, config.wrt)</code>; a filter that selects nothing raises instead of becoming optax's silent no-op <code>create_transformation(model, config)</code> Clipping first, then the base alias wrapped in <code>optax.inject_hyperparams</code>; a schedule is the base optimizer's learning rate, never a <code>scale_by_schedule</code> placed before an adaptive optimizer, whose normalisation would cancel it <code>create_optimizer(model, config)</code> <code>nnx.Optimizer(model, create_transformation(model, config), wrt=config.wrt)</code> <code>current_learning_rate(optimizer)</code> The rate the last update applied, a scalar array on device; the schedule at step zero before the first update, and unchanged by a step <code>optax.apply_if_finite</code> skips <p>The optimizer-state tree has the same structure whatever the filter, so a checkpoint written with one filter restores into an optimizer built with another.</p>"},{"location":"api/optim/#substrax.optim","title":"<code>substrax.optim</code>","text":"<p>One optimizer specification, one builder over optax, and the learning rate read on device.</p> <p><code>OptimizerConfig</code> names the optimizer in optax's terms; <code>create_transformation</code> and <code>create_optimizer</code> build it, passing a schedule as the base optimizer's learning rate and the weight-decay filter as a static mask; <code>current_learning_rate</code> reads the rate the last update applied. <code>EXCLUDE_BIAS_AND_NORM_SCALE</code> is the filter that decays neither biases nor normalisation scales. <code>update_with_line_search</code> steps an optimizer that searches along its direction (<code>optax.lbfgs</code>); <code>switch_at</code> hands over from one transformation to another after a fixed number of updates; <code>with_strong_state_types</code> keeps a transformation's state types fixed across updates, so a jitted step compiles once.</p>"},{"location":"api/optim/#substrax.optim.EXCLUDE_BIAS_AND_NORM_SCALE","title":"<code>EXCLUDE_BIAS_AND_NORM_SCALE = _is_neither_bias_nor_scale</code>  <code>module-attribute</code>","text":"<p>Decay every parameter except biases and normalisation scales, as Hugging Face and timm do.</p>"},{"location":"api/optim/#substrax.optim.OptimizerConfig","title":"<code>OptimizerConfig(*, optimizer_type='adam', learning_rate, b1=0.9, b2=0.999, eps=1e-08, momentum=None, weight_decay=0.0, weight_decay_filter=None, gradient_clip_norm=None, gradient_clip_value=None, wrt=nnx.Param)</code>  <code>dataclass</code>","text":"<p>What an optimizer is, in optax's terms.</p> <p>A schedule is passed as the base optimizer's learning rate, never as a <code>scale_by_schedule</code> placed before an adaptive optimizer, whose normalisation would cancel the scaling. The names map onto other libraries as follows.</p> Field PyTorch Flax NNX <code>learning_rate</code> <code>lr</code> the base optax alias's <code>learning_rate</code> <code>b1</code>, <code>b2</code> <code>betas</code> <code>b1</code>, <code>b2</code> <code>weight_decay</code> <code>weight_decay</code> on a parameter group <code>optax.adamw(weight_decay=...)</code> <code>weight_decay_filter</code> the parameter group <code>optax.adamw(mask=...)</code>, True where decayed <code>wrt</code> <code>model.parameters()</code> <code>nnx.Optimizer(model, tx, wrt=...)</code> <p>Attributes:</p> Name Type Description <code>optimizer_type</code> <code>OptimizerType</code> <p>The optax alias; <code>nadam</code> is <code>adam</code> with Nesterov momentum.</p> <code>learning_rate</code> <code>float | Schedule</code> <p>A positive constant, or an <code>optax.Schedule</code> of the step count.</p> <code>b1</code> <code>float</code> <p>First-moment decay of the Adam family and LAMB.</p> <code>b2</code> <code>float</code> <p>Second-moment decay of the Adam family and LAMB.</p> <code>eps</code> <code>float</code> <p>The denominator offset.</p> <code>momentum</code> <code>float | None</code> <p>Momentum of <code>sgd</code> and <code>rmsprop</code>; <code>None</code> for no momentum.</p> <code>weight_decay</code> <code>float</code> <p>Decoupled weight decay, <code>adamw</code> and <code>lamb</code> only.</p> <code>weight_decay_filter</code> <code>Filter | None</code> <p>A Flax NNX filter naming the parameters that are decayed, the polarity of optax's <code>mask</code>; <code>None</code> decays every leaf in <code>wrt</code>, as <code>torch.optim.AdamW(model.parameters())</code> does. <code>EXCLUDE_BIAS_AND_NORM_SCALE</code> is the Hugging Face and timm convention.</p> <code>gradient_clip_norm</code> <code>float | None</code> <p>Global-norm clipping applied before the update, or <code>None</code>.</p> <code>gradient_clip_value</code> <code>float | None</code> <p>Element-wise clipping applied before the update, or <code>None</code>.</p> <code>wrt</code> <code>Filter</code> <p>The filter naming the parameters the optimizer updates.</p>"},{"location":"api/optim/#substrax.optim.OptimizerConfig.__post_init__","title":"<code>__post_init__()</code>","text":"<p>Refuse a specification optax would silently misread.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If <code>weight_decay_filter</code> is a string, which NNX reads as a tag, not a path.</p> <code>ValueError</code> <p>If <code>optimizer_type</code> is not one built here, both clip fields are set or a clip is not positive, <code>weight_decay</code> is negative or set for an optimizer without decoupled decay, <code>momentum</code> is set for an optimizer that has none, or a constant <code>learning_rate</code> is not positive.</p>"},{"location":"api/optim/#substrax.optim.create_optimizer","title":"<code>create_optimizer(model, config)</code>","text":"<p>Build an <code>nnx.Optimizer</code> over <code>create_transformation</code> for <code>config.wrt</code>.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The model the optimizer updates.</p> required <code>config</code> <code>OptimizerConfig</code> <p>The specification.</p> required <p>Returns:</p> Type Description <code>Optimizer[Any]</code> <p>The optimizer; step it with <code>optimizer.update(model, grads)</code>.</p>"},{"location":"api/optim/#substrax.optim.create_transformation","title":"<code>create_transformation(model, config)</code>","text":"<p>Build the optax transformation <code>config</code> describes for <code>model</code>.</p> <p>Clipping, when configured, comes first; the base alias follows, wrapped in <code>optax.inject_hyperparams</code> so its learning rate, a schedule of optax's own step count when one is given, can be read on device with <code>current_learning_rate</code>. The weight-decay filter becomes a mask of Python booleans over the parameter tree, never optimizer state, so the state has the same structure whatever the filter.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The model the optimizer updates, which fixes the mask's tree.</p> required <code>config</code> <code>OptimizerConfig</code> <p>The specification.</p> required <p>Returns:</p> Type Description <code>GradientTransformation</code> <p>The transformation.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>config.optimizer_type</code> is not an optax alias this builder knows, or the weight-decay filter selects no parameter.</p>"},{"location":"api/optim/#substrax.optim.mask_callable","title":"<code>mask_callable(config)</code>","text":"<p>Return the callable optax evaluates on the parameter tree to get the decay mask.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>OptimizerConfig</code> <p>The specification holding <code>weight_decay_filter</code>.</p> required <p>Returns:</p> Type Description <code>Callable[[Any], Any]</code> <p>A function from a parameter tree to a tree of Python booleans of the same structure.</p>"},{"location":"api/optim/#substrax.optim.mask_from_filter","title":"<code>mask_from_filter(params, chosen)</code>","text":"<p>Map an NNX filter over a parameter tree, giving a Python boolean per leaf.</p> <p>The tree may be an <code>nnx.State</code> or the pure tree optax hands a callable mask; a trailing <code>value</code> attribute key of a Variable is not part of the parameter's path.</p> <p>Parameters:</p> Name Type Description Default <code>params</code> <code>Any</code> <p>The parameter tree.</p> required <code>chosen</code> <code>Filter</code> <p>The filter, evaluated on each leaf's path and value.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>A tree of the same structure with a <code>bool</code> at every leaf.</p>"},{"location":"api/optim/#substrax.optim.weight_decay_mask","title":"<code>weight_decay_mask(model, config)</code>","text":"<p>Return the optax mask for <code>config.weight_decay_filter</code> over <code>nnx.state(model, wrt)</code>.</p> <p>The mask is the parameters' pure tree (nested dicts, as <code>nnx.to_pure_dict</code> gives it) with a static Python boolean at every leaf, <code>True</code> where the filter selects the parameter. <code>None</code> selects every leaf. The builder passes optax the callable form, <code>mask_callable</code>, which maps the same filter over whatever parameter tree optax hands it.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The model the optimizer updates.</p> required <code>config</code> <code>OptimizerConfig</code> <p>The specification holding <code>wrt</code> and <code>weight_decay_filter</code>.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The mask tree.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the filter selects no parameter, which optax would accept as a silent no-op.</p>"},{"location":"api/optim/#substrax.optim.current_learning_rate","title":"<code>current_learning_rate(optimizer)</code>","text":"<p>Return the learning rate the last <code>update</code> applied, as an array on device.</p> <p><code>create_transformation</code> wraps the base alias in <code>optax.inject_hyperparams</code>, which stores the rate it evaluated at optax's own step count. Before the first update the value is the schedule at step zero; a step a wrapper such as <code>optax.apply_if_finite</code> skips leaves it unchanged, because optax's count does not advance.</p> <p>Parameters:</p> Name Type Description Default <code>optimizer</code> <code>Optimizer[Any]</code> <p>An optimizer built over <code>create_transformation</code>.</p> required <p>Returns:</p> Type Description <code>Array</code> <p>The learning rate, a scalar array.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the optimizer's state holds no injected learning rate.</p>"},{"location":"api/optim/#substrax.optim.switch_at","title":"<code>switch_at(first, second, *, step)</code>","text":"<p>The <code>first</code> transformation for <code>step</code> updates, then <code>second</code>.</p> <p>Each runs under <code>optax.conditionally_transform</code> on the update count, so the switch is a branch of the traced step rather than a Python decision, and each keeps its own state from initialisation. Extra arguments reach a transformation that takes them (<code>optax.lbfgs</code>'s <code>value</code>, <code>grad</code> and <code>value_fn</code>).</p> <p>Parameters:</p> Name Type Description Default <code>first</code> <code>GradientTransformation</code> <p>The transformation for the first <code>step</code> updates, such as <code>optax.adam</code>.</p> required <code>second</code> <code>GradientTransformation</code> <p>The transformation from update <code>step</code> on, such as <code>optax.lbfgs()</code>.</p> required <code>step</code> <code>int</code> <p>The number of updates <code>first</code> makes.</p> required <p>Returns:</p> Type Description <code>GradientTransformationExtraArgs</code> <p>The combined transformation, its initial state strongly typed.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>step</code> is not positive.</p>"},{"location":"api/optim/#substrax.optim.update_with_line_search","title":"<code>update_with_line_search(model, optimizer, loss_fn, *, objective_changed)</code>","text":"<p>Update <code>model</code> with an optimizer that evaluates the loss along its search direction.</p> <p>The loss, its gradient and the loss as a function of the <code>optimizer.wrt</code> parameters are passed to <code>optimizer.update</code> as <code>value</code>, <code>grad</code> and <code>value_fn</code>, the extra arguments <code>optax.lbfgs</code> and any transformation built on it read. When <code>objective_changed</code> is false and the state holds the value and gradient the previous line search computed at the current parameters, those are used; otherwise they are evaluated. <code>objective_changed</code> is traced, so either answer runs the same compiled step. Under <code>vmap</code> (an ensemble of models) the choice depends on each member's stored value, and a <code>lax.cond</code> over a batched predicate runs both branches, so each step evaluates the loss as it would with <code>objective_changed</code> true: the result is the same and the saving is lost. The loss is taken in the parameters' dtype: optax's line search keeps its values in that dtype, and a wider loss (a float32 model's loss computed in float64 under x64) would make the branches of its <code>lax.cond</code> disagree.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>M</code> <p>The model; updated in place.</p> required <code>optimizer</code> <code>Optimizer[Any]</code> <p>An <code>nnx.Optimizer</code> over <code>model</code> whose transformation takes the extra arguments and whose state is strongly typed, such as <code>with_strong_state_types(optax.lbfgs())</code> or a <code>switch_at</code> ending in it.</p> required <code>loss_fn</code> <code>Callable[[M], Array]</code> <p>The scalar loss of the model.</p> required <code>objective_changed</code> <code>bool | Array</code> <p>Whether <code>loss_fn</code> is a different objective from the previous call's (a new batch, resampled points), or the parameters moved outside the line search since it; <code>False</code> only for the same objective at the point it accepted.</p> required <p>Returns:</p> Type Description <code>Array</code> <p>The loss before the update, in the parameters' dtype.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the parameters are of more than one floating dtype, which leaves the line search no single dtype to work in, or if the optimizer state has weakly typed leaves, which would compile the step again once its first update types them.</p>"},{"location":"api/optim/#substrax.optim.with_strong_state_types","title":"<code>with_strong_state_types(transformation)</code>","text":"<p><code>transformation</code> with every weakly typed leaf of its initial state strongly typed.</p> <p>The values and dtypes are unchanged; only the weak type goes, so the state a jitted step receives has the types its update returns. Updates are <code>transformation</code>'s own, extra arguments included.</p> <p>Parameters:</p> Name Type Description Default <code>transformation</code> <code>GradientTransformation</code> <p>The optax transformation, such as <code>optax.lbfgs()</code>.</p> required <p>Returns:</p> Type Description <code>GradientTransformationExtraArgs</code> <p>The transformation with a strongly typed initial state.</p>"},{"location":"api/records/","title":"Records","text":"<p>A record is a frozen dataclass written to JSON by <code>substrax.records.dump_record</code> and read back by <code>substrax.records.read_record</code>, which validates every field against its annotation with pydantic's strict JSON mode. <code>substrax.typing</code> holds the aliases records are typed with; it imports nothing beyond the standard library.</p> <pre><code>from dataclasses import dataclass\n\nfrom substrax.records import dump_record, read_record, UNKNOWN_FIELDS_REFUSED\n\n\n@dataclass(frozen=True, slots=True, kw_only=True)\nclass Measurement:\n    __pydantic_config__ = UNKNOWN_FIELDS_REFUSED\n\n    name: str\n    value: float\n    samples: tuple[float, ...] | None = None\n\n\ndump_record(Measurement(name=\"loss\", value=0.25, samples=(0.2, 0.3)))\n# {'name': 'loss', 'value': 0.25, 'samples': [0.2, 0.3]}\n\nread_record(Measurement, {\"name\": \"loss\", \"value\": \"0.25\"})\n# pydantic.ValidationError: 1 validation error for Measurement\n# value\n#   Input should be a valid number [type=float_type, input_value='0.25', input_type=str]\n</code></pre> Annotation Reads <code>str</code>, <code>bool</code> The JSON value of that type only <code>int</code> A JSON integer; a float or a <code>bool</code> is refused <code>float</code> A JSON number, an integer included; a string or a <code>bool</code> is refused <code>datetime</code> ISO 8601 text An enum One of its values <code>tuple[T, ...]</code>, <code>list[T]</code> A JSON array whose elements read as <code>T</code> <code>Mapping[str, T]</code>, <code>dict[str, T]</code> A JSON object whose values read as <code>T</code> A dataclass A nested JSON object, read by the same rules <code>JsonValue</code> Any JSON value, unchecked below it <p>A missing field takes the dataclass default; a missing field without one is refused. A field the record does not declare is ignored, unless the record sets <code>__pydantic_config__ = UNKNOWN_FIELDS_REFUSED</code>, as settings and versioned files do, so a misspelled key is refused rather than dropped. Every refusal is collected into one <code>pydantic.ValidationError</code>, a <code>ValueError</code> whose <code>errors()</code> give each field's path as <code>loc</code>, such as <code>(\"metrics\", \"loss\", \"value\")</code>.</p> <p>The object is written back to JSON text and validated in pydantic's JSON mode, because strict Python-mode validation accepts a dataclass only as an instance. One validator per record type is built on first use and kept. Records are read at the edges of a program, never inside a function JAX traces.</p>"},{"location":"api/records/#substrax.records","title":"<code>substrax.records</code>","text":"<p>Records read from JSON: frozen dataclasses validated by pydantic's strict JSON mode.</p> <p>A record is a frozen dataclass; <code>dump_record</code> writes it as JSON values and <code>read_record</code> reads one back from the parsed JSON object. Each field is checked against its annotation as JSON defines the types: a number is never read from a string, a <code>bool</code> is not a number, an integer is accepted where a float is expected (JSON has one number type), nested dataclasses, <code>datetime</code> (ISO 8601 text), enums (by value) and tuples (from arrays) are read by their annotations, and a missing field takes its default. A refused record raises <code>pydantic.ValidationError</code>, a <code>ValueError</code> listing each field's path through the record.</p> <p>The object is re-serialised to JSON text and validated in pydantic's JSON mode, because its strict Python mode reads a dataclass only from an instance of it; a record is small and read at the edge of a program, never inside a traced function.</p>"},{"location":"api/records/#substrax.records.UNKNOWN_FIELDS_REFUSED","title":"<code>UNKNOWN_FIELDS_REFUSED = ConfigDict(extra='forbid')</code>  <code>module-attribute</code>","text":"<p>Set as a record's <code>__pydantic_config__</code> to refuse fields it does not declare, such as a misspelled setting; without it <code>read_record</code> ignores them.</p>"},{"location":"api/records/#substrax.records.read_record","title":"<code>read_record(record_type, data)</code>","text":"<p>Read a record of <code>record_type</code> from a parsed JSON object.</p> <p>Parameters:</p> Name Type Description Default <code>record_type</code> <code>type[T]</code> <p>The record's dataclass.</p> required <code>data</code> <code>Mapping[str, JsonValue]</code> <p>The JSON object, as <code>json.loads</code> or Orbax's <code>JsonRestore</code> returns it.</p> required <p>Returns:</p> Type Description <code>T</code> <p>The record.</p> <p>Raises:</p> Type Description <code>ValidationError</code> <p>If a field is missing or holds a value its annotation does not admit; <code>errors()</code> gives each field's path as <code>loc</code>.</p> <code>TypeError</code> <p>If the validator returns something other than the record type, which pydantic does not do for a dataclass.</p>"},{"location":"api/records/#substrax.records.dump_record","title":"<code>dump_record(record)</code>","text":"<p>Write a record as the JSON object <code>read_record</code> reads back into an equal record.</p> <p>Nested records become objects, tuples arrays, enums their values, paths and <code>datetime</code> their text.</p> <p>Parameters:</p> Name Type Description Default <code>record</code> <code>object</code> <p>A dataclass instance.</p> required <p>Returns:</p> Type Description <code>dict[str, JsonValue]</code> <p>The JSON object, ready for <code>json.dumps</code>.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If <code>record</code> is not a dataclass instance.</p>"},{"location":"api/records/#substrax.typing","title":"<code>substrax.typing</code>","text":"<p>Type aliases shared across the avitai packages; importing this module imports no jax.</p>"},{"location":"api/records/#substrax.typing.PyTree","title":"<code>PyTree = Any</code>  <code>module-attribute</code>","text":"<p>Any JAX pytree: nested tuples, lists and dicts of arrays and leaves.</p>"},{"location":"api/records/#substrax.typing.JsonValue","title":"<code>JsonValue = str | int | float | bool | list[JsonValue] | dict[str, JsonValue] | None</code>","text":"<p>A value JSON can hold: a scalar, or a list or object of JSON values (as Orbax's tree types).</p>"},{"location":"api/records/#substrax.typing.Checkpointable","title":"<code>Checkpointable</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>An object a checkpoint restores: it hands over its state and takes it back.</p> <p>The state is a dictionary a checkpoint store can write (arrays and plain-Python leaves), and <code>set_state</code> on an object built the same way continues exactly where the saved one stood. A training loop checkpoints its data iterator, callbacks and extensions through this protocol without importing their packages.</p>"},{"location":"api/records/#substrax.typing.Checkpointable.get_state","title":"<code>get_state()</code>","text":"<p>The state to checkpoint.</p>"},{"location":"api/records/#substrax.typing.Checkpointable.set_state","title":"<code>set_state(state)</code>","text":"<p>Take back a state <code>get_state</code> returned.</p>"},{"location":"api/rng/","title":"Rng","text":"<p><code>substrax.rng</code> takes keys from an explicit owner and derives streams from a seed by name. Nothing in it falls back to a default seed: two callers never share a key they did not ask for.</p> <pre><code>from flax import nnx\n\nfrom substrax.rng import fold_in_name, key_from, rngs_from_seed, split_key\n\nrngs = rngs_from_seed(42, streams=(\"params\", \"dropout\", \"sample\"))\n\n# A sampler accepts an Rngs with a `sample` stream, or a key given directly.\nkey = key_from(rngs, streams=(\"sample\", \"default\"), context=\"sampling\")\nper_walker = split_key(key, 1024)\n\n# A sub-component derives its own key from a name, the same in every interpreter.\nnoise_key = fold_in_name(key, \"observation_noise\")\n</code></pre> Function Behaviour <code>key_from(rng, *, streams, context)</code> With an <code>nnx.Rngs</code>, calls the first of <code>streams</code> it holds, in the order given; a typed key or a raw <code>uint32</code> key of shape <code>(2,)</code> is returned as given. A missing stream raises <code>MissingRngStreamError</code> naming <code>context</code> and the streams; <code>None</code>, a Python integer or any other array raises <code>TypeError</code> <code>rngs_from_seed(seed, streams=(\"default\",))</code> One stream per name, each the seed's key folded by the name, so adding or reordering streams leaves the others unchanged <code>split_key(key, num)</code> <code>jax.random.split</code> with <code>num</code> at least one; asking for no keys is a defect <code>fold_in_name(key, name)</code> Folds in the name's BLAKE2b digest truncated to 32 bits, stable across interpreters whatever <code>PYTHONHASHSEED</code> is <p><code>rngs_from_seed</code> differs from splitting the seed key into one part per stream, which every package used to do: there, appending a stream moved every existing stream's key, so a recorded run stopped reproducing when a model gained a <code>dropout</code> stream.</p> <p>The pytest plugin (<code>substrax.testing.pytest_plugin</code>) provides <code>rng_key</code>, a typed key from seed 42, and <code>rngs</code>, an <code>nnx.Rngs</code> from the same seed with <code>default</code>, <code>params</code>, <code>dropout</code> and <code>sample</code> streams.</p>"},{"location":"api/rng/#substrax.rng","title":"<code>substrax.rng</code>","text":"<p>Keys from an explicit owner, streams derived from a seed by name, splits and named folds.</p> <p><code>key_from</code> takes a key from an <code>nnx.Rngs</code> stream or a key given directly and never falls back to a default seed; <code>rngs_from_seed</code> derives each stream from the seed and the stream's name, so streams are independent of each other; <code>split_key</code> and <code>fold_in_name</code> are the two derivations every package needs, with a digest that is stable across interpreters.</p>"},{"location":"api/rng/#substrax.rng.MissingRngStreamError","title":"<code>MissingRngStreamError(context, streams)</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>An <code>nnx.Rngs</code> holds none of the streams a call accepts.</p> <p>Attributes:</p> Name Type Description <code>context</code> <code>str</code> <p>What needed the key, as the caller named it.</p> <code>streams</code> <code>tuple[str, ...]</code> <p>The stream names the caller accepts, in order of preference.</p> <p>Name what needed the key and the streams it accepts.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>str</code> <p>What needed the key, as the caller named it.</p> required <code>streams</code> <code>Sequence[str]</code> <p>The stream names the caller accepts, in order of preference.</p> required"},{"location":"api/rng/#substrax.rng.fold_in_name","title":"<code>fold_in_name(key, name)</code>","text":"<p>Fold a name into <code>key</code> through a stable digest.</p> <p>The name's BLAKE2b digest, truncated to 32 bits, is folded in, so the derived key is the same in every interpreter, whatever <code>PYTHONHASHSEED</code> is; Python's <code>hash</code> is salted per process and is never used.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Array</code> <p>The key to derive from.</p> required <code>name</code> <code>str</code> <p>A non-empty name, such as a stream's.</p> required <p>Returns:</p> Type Description <code>Array</code> <p>The derived key.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>name</code> is empty.</p>"},{"location":"api/rng/#substrax.rng.key_from","title":"<code>key_from(rng, *, streams, context)</code>","text":"<p>Return a key from an explicit owner: an <code>nnx.Rngs</code> stream or a key given directly.</p> <p>With an <code>nnx.Rngs</code>, the first of <code>streams</code> it holds is called, in the order given. A typed key (<code>jax.random.key</code>) or a raw <code>uint32</code> key of shape <code>(2,)</code> is returned as given. There is no fallback: a missing stream, <code>None</code>, a Python integer or any other array raises, so two callers never share a key drawn from a default seed.</p> <p>Parameters:</p> Name Type Description Default <code>rng</code> <code>Array | Rngs</code> <p>The key's owner.</p> required <code>streams</code> <code>Sequence[str]</code> <p>Stream names accepted, most preferred first.</p> required <code>context</code> <code>str</code> <p>What needs the key, named in the errors (<code>\"sampling\"</code>, <code>\"dropout\"</code>).</p> required <p>Returns:</p> Type Description <code>Array</code> <p>The key.</p> <p>Raises:</p> Type Description <code>MissingRngStreamError</code> <p>If <code>rng</code> is an <code>nnx.Rngs</code> holding none of <code>streams</code>.</p> <code>TypeError</code> <p>If <code>rng</code> is neither an <code>nnx.Rngs</code> nor a single key.</p>"},{"location":"api/rng/#substrax.rng.rngs_from_seed","title":"<code>rngs_from_seed(seed, streams=('default',))</code>","text":"<p>Build an <code>nnx.Rngs</code> whose every stream is the seed's key folded by the stream's name.</p> <p>Each stream depends on the seed and its own name only, so adding or reordering streams leaves the others' keys unchanged, which a split of the seed key into <code>len(streams)</code> parts does not.</p> <p>Parameters:</p> Name Type Description Default <code>seed</code> <code>int</code> <p>A non-negative integer.</p> required <code>streams</code> <code>Sequence[str]</code> <p>Distinct stream names; at least one.</p> <code>('default',)</code> <p>Returns:</p> Type Description <code>Rngs</code> <p>The <code>nnx.Rngs</code> with one stream per name.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If <code>seed</code> is not an integer.</p> <code>ValueError</code> <p>If <code>seed</code> is negative, or <code>streams</code> is empty or repeats a name.</p>"},{"location":"api/rng/#substrax.rng.split_key","title":"<code>split_key(key, num)</code>","text":"<p>Split <code>key</code> into <code>num</code> keys along a new leading axis.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Array</code> <p>The key to split.</p> required <code>num</code> <code>int</code> <p>How many keys to return; at least one, so a caller asking for none is a defect.</p> required <p>Returns:</p> Type Description <code>Array</code> <p>Keys of shape <code>(num,)</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>num</code> is below one.</p>"},{"location":"api/runtime/","title":"Runtime","text":"<p><code>substrax.runtime</code> declares the settings a JAX process starts with and applies them at the point where they still take effect.</p> <pre><code>import os\nimport subprocess\nimport sys\n\nfrom substrax.runtime import JaxRuntime, apply_runtime, runtime_environment\n\nruntime = JaxRuntime(platforms=(\"cpu\",), cpu_devices=8, enable_x64=True)\n\n# A child process on eight emulated CPU devices.\nenv = {**os.environ, **runtime_environment(runtime, os.environ)}\nsubprocess.run([sys.executable, \"train.py\"], env=env, check=True)\n\n# This process, before it imports jax.\napply_runtime(runtime)\n\nimport jax\n\nassert jax.device_count() == 8\n</code></pre> <p><code>runtime_environment</code> renders each field as the variable jax or XLA reads when it starts:</p> Field Variable <code>platforms</code> <code>JAX_PLATFORMS</code> <code>cpu_devices</code> <code>JAX_NUM_CPU_DEVICES</code>, which jax prefers over <code>--xla_force_host_platform_device_count</code> <code>enable_x64</code> <code>JAX_ENABLE_X64</code> <code>matmul_precision</code> <code>JAX_DEFAULT_MATMUL_PRECISION</code> <code>compilation_cache_dir</code> <code>JAX_COMPILATION_CACHE_DIR</code> <code>xla_flags</code> <code>XLA_FLAGS</code>, merged by flag name <code>preallocate</code> <code>XLA_PYTHON_CLIENT_PREALLOCATE</code> <code>memory_fraction</code> <code>XLA_CLIENT_MEM_FRACTION</code> <p>A requested XLA flag that is already set to a different value raises <code>XlaFlagConflictError</code>, so a device count or GPU flag exported by the caller is never silently replaced. A memory fraction raises while the deprecated <code>XLA_PYTHON_CLIENT_MEM_FRACTION</code> is still set, because jax refuses both at once. <code>JaxRuntime</code> applies no per-backend flag presets: <code>xla_flags</code> is empty unless a run names the flags it needs.</p> <p>Once jax is imported, <code>apply_runtime</code> applies the CPU device count, 64-bit types, matmul precision and compilation cache directory through <code>jax.config</code>; jax refuses a new device count after its first operation, and that refusal is raised as <code>RuntimeConfigurationError</code>. Platforms, XLA flags and accelerator memory settings are read only when jax starts its backends, and jax gives no public signal of whether that has happened, so after the import they raise instead of being written where they may change nothing.</p> <p>A test suite chooses its backend and emulated CPU devices before anything imports jax:</p> <pre><code># conftest.py\nimport importlib.util\nimport os\n\nfrom substrax.runtime import resolve_test_runtime, runtime_environment\n\nruntime = resolve_test_runtime(\n    os.environ,\n    prefix=\"DATARAX_TEST_\",\n    cuda_plugin_available=importlib.util.find_spec(\"jax_cuda12_plugin\") is not None,\n)\nos.environ.update(runtime_environment(runtime, os.environ))\n</code></pre> <p>Tests run on eight emulated CPU devices unless <code>DATARAX_TEST_JAX_PLATFORMS</code> asks for an accelerator or <code>DATARAX_TEST_DEVICE_COUNT</code> names another count; an inherited <code>JAX_PLATFORMS</code> does not move them onto a GPU.</p> <p>Entry points configure logging inside <code>main()</code>, never at import:</p> <pre><code>from substrax.runtime import configure_entry_point_logging\n\n\ndef main() -&gt; None:\n    configure_entry_point_logging()\n</code></pre> <p>A second call replaces only the handler the first one installed, so it needs no <code>force=True</code> and leaves pytest's log capture in place. Importing <code>substrax.runtime</code> imports no jax and changes no process state.</p>"},{"location":"api/runtime/#the-managed-environment-file","title":"The managed environment file","text":"<p>A repository's <code>setup.sh</code> writes the file its <code>activate.sh</code> sources once the project environment is synced:</p> <pre><code>uv run --no-sync python -m substrax.runtime.managed_env write --prefix DATARAX \\\n    --backend auto --output .datarax.env --set TF_CPP_MIN_LOG_LEVEL=1\npython -m substrax.runtime.managed_env show --prefix DATARAX --env-file .datarax.env \\\n    --user-env .env --user-env .env.local\n</code></pre> <p><code>auto</code> resolves to <code>cuda12</code> on Linux when <code>nvidia-smi</code> lists a GPU, <code>metal</code> on Apple Silicon, and <code>cpu</code> otherwise. The file names every variable it manages in <code>&lt;PREFIX&gt;_MANAGED_ENV_VARS</code>, so <code>activate.sh</code> unsets them before sourcing a newer file, then exports or unsets each:</p> Variable <code>cpu</code> <code>cuda12</code>, <code>metal</code> <code>&lt;PREFIX&gt;_BACKEND</code>, <code>&lt;PREFIX&gt;_ENV_ROOT</code> the resolved backend and the project root the same <code>JAX_PLATFORMS</code> <code>cpu</code> unset, so jax picks the accelerator <code>JAX_ENABLE_X64</code> <code>false</code> <code>false</code> <code>XLA_PYTHON_CLIENT_PREALLOCATE</code> unset <code>false</code> <code>XLA_CLIENT_MEM_FRACTION</code> unset <code>0.75</code> <code>XLA_PYTHON_CLIENT_MEM_FRACTION</code> unset unset, because jax refuses it beside the current name <p>jaxlib reads preallocation and the memory fraction only when it builds the GPU plugin's options, so they are unset on the CPU. Each <code>--set NAME=VALUE</code> adds a repository variable to the file; a <code>--set</code> naming a variable the file manages itself raises.</p>"},{"location":"api/runtime/#substrax.runtime","title":"<code>substrax.runtime</code>","text":"<p>JAX process configuration, declared once and applied before or after jax is imported.</p> <p><code>JaxRuntime</code> describes the settings. <code>runtime_environment</code> renders them as the environment of a process that has not imported jax yet (a child process, or a conftest before its first <code>import jax</code>), <code>child_environment</code> builds a child's whole environment from them, <code>apply_runtime</code> applies them to the current process, and <code>resolve_test_runtime</code> chooses the backend and emulated CPU devices of a test run. <code>configure_entry_point_logging</code> sets up the root logger from an entry point. Importing this package imports no jax and changes no process state.</p>"},{"location":"api/runtime/#substrax.runtime.JaxRuntime","title":"<code>JaxRuntime(*, platforms=None, cpu_devices=None, enable_x64=None, matmul_precision=None, compilation_cache_dir=None, xla_flags=(), preallocate=None, memory_fraction=None)</code>  <code>dataclass</code>","text":"<p>Declarative JAX process settings; <code>None</code> leaves the process's current value alone.</p> <p>:func:<code>~substrax.runtime.runtime_environment</code> renders the settings as the environment variables jax and XLA read when a process starts, and :func:<code>~substrax.runtime.apply_runtime</code> applies them to the current process.</p> <p>Attributes:</p> Name Type Description <code>platforms</code> <code>tuple[str, ...] | None</code> <p>The backends jax initialises, the first being the default (<code>JAX_PLATFORMS</code>).</p> <code>cpu_devices</code> <code>int | None</code> <p>The number of CPU devices (<code>JAX_NUM_CPU_DEVICES</code>). jax prefers it over the <code>--xla_force_host_platform_device_count</code> flag.</p> <code>enable_x64</code> <code>bool | None</code> <p>Whether 64-bit types are enabled (<code>JAX_ENABLE_X64</code>).</p> <code>matmul_precision</code> <code>MatmulPrecision | None</code> <p>The default matrix-multiplication precision (<code>JAX_DEFAULT_MATMUL_PRECISION</code>).</p> <code>compilation_cache_dir</code> <code>Path | None</code> <p>The absolute directory of the persistent compilation cache (<code>JAX_COMPILATION_CACHE_DIR</code>).</p> <code>xla_flags</code> <code>tuple[str, ...]</code> <p>Flags merged into <code>XLA_FLAGS</code> by name, each <code>--name</code> or <code>--name=value</code>.</p> <code>preallocate</code> <code>bool | None</code> <p>Whether the accelerator client preallocates its memory (<code>XLA_PYTHON_CLIENT_PREALLOCATE</code>).</p> <code>memory_fraction</code> <code>float | None</code> <p>The fraction of accelerator memory the client may use, in <code>(0, 1]</code> (<code>XLA_CLIENT_MEM_FRACTION</code>).</p>"},{"location":"api/runtime/#substrax.runtime.JaxRuntime.__post_init__","title":"<code>__post_init__()</code>","text":"<p>Validate every field.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If a field holds a value of the wrong type.</p> <code>ValueError</code> <p>If a field holds a value outside its domain.</p> <code>XlaFlagConflictError</code> <p>If one requested flag is given two different values.</p>"},{"location":"api/runtime/#substrax.runtime.RuntimeConfigurationError","title":"<code>RuntimeConfigurationError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>A JAX process setting cannot be applied, or conflicts with one already in place.</p>"},{"location":"api/runtime/#substrax.runtime.XlaFlagConflictError","title":"<code>XlaFlagConflictError(flag, existing, requested)</code>","text":"<p>               Bases: <code>RuntimeConfigurationError</code></p> <p>One XLA flag was given two different values.</p> <p>Attributes:</p> Name Type Description <code>flag</code> <code>str</code> <p>The flag name, with its leading dashes.</p> <code>existing</code> <code>str | None</code> <p>The value already in place; <code>None</code> for a flag given without a value.</p> <code>requested</code> <code>str | None</code> <p>The conflicting value; <code>None</code> for a flag given without a value.</p> <p>Record the flag and both of its values.</p> <p>Parameters:</p> Name Type Description Default <code>flag</code> <code>str</code> <p>The flag name, with its leading dashes.</p> required <code>existing</code> <code>str | None</code> <p>The value already in place.</p> required <code>requested</code> <code>str | None</code> <p>The conflicting value.</p> required"},{"location":"api/runtime/#substrax.runtime.apply_runtime","title":"<code>apply_runtime(runtime, *, environ=None)</code>","text":"<p>Apply <code>runtime</code> to the current process.</p> <p>Before jax is imported, every field is written to <code>environ</code> as the variables jax and XLA read at start-up (see :func:<code>~substrax.runtime.runtime_environment</code>). Once jax is imported, the CPU device count, 64-bit types, matmul precision and compilation cache directory go through <code>jax.config.update</code>; jax refuses a new device count once its backends have started. Platforms, XLA flags and client memory settings are read only when the backends start, and jax offers no public signal of whether that has happened, so once jax is imported they raise instead of being written where they might change nothing.</p> <p>Parameters:</p> Name Type Description Default <code>runtime</code> <code>JaxRuntime</code> <p>The settings to apply.</p> required <code>environ</code> <code>MutableMapping[str, str] | None</code> <p>The environment written before jax is imported; <code>os.environ</code> when <code>None</code>.</p> <code>None</code> <p>Raises:</p> Type Description <code>RuntimeConfigurationError</code> <p>If jax is imported and <code>runtime</code> sets a platform, an XLA flag or a client memory setting, or jax refuses the CPU device count.</p> <code>XlaFlagConflictError</code> <p>If a requested flag has a different value in the current <code>XLA_FLAGS</code>.</p>"},{"location":"api/runtime/#substrax.runtime.configure_entry_point_logging","title":"<code>configure_entry_point_logging(level=logging.INFO, *, stream=None, fmt='%(message)s')</code>","text":"<p>Send log records to <code>stream</code> without replacing any other handler.</p> <p>Call it inside <code>main()</code>, never at import. A second call replaces only the handler the first one installed and leaves every other handler, pytest's log capture included, in place, so it needs no <code>force=True</code>.</p> <p>Parameters:</p> Name Type Description Default <code>level</code> <code>int</code> <p>The level set on the root logger.</p> <code>INFO</code> <code>stream</code> <code>TextIO | None</code> <p>Where records are written; standard error when <code>None</code>.</p> <code>None</code> <code>fmt</code> <code>str</code> <p>The <code>logging.Formatter</code> format string.</p> <code>'%(message)s'</code>"},{"location":"api/runtime/#substrax.runtime.child_environment","title":"<code>child_environment(runtime, env=None, *, parent=None)</code>","text":"<p>Return the complete environment of a child process whose JAX settings the caller chooses.</p> <p>The child gets <code>parent</code> without any <code>JAX_*</code> or <code>XLA_*</code> variable, so settings exported in the shell that started this process cannot change what the child runs, then <code>env</code>, then <code>runtime</code> as :func:<code>runtime_environment</code> renders it on top of the two.</p> <p>Parameters:</p> Name Type Description Default <code>runtime</code> <code>JaxRuntime</code> <p>The child's JAX settings.</p> required <code>env</code> <code>Mapping[str, str] | None</code> <p>Variables set in the child before <code>runtime</code> is applied.</p> <code>None</code> <code>parent</code> <code>Mapping[str, str] | None</code> <p>The environment the child inherits; this process's when <code>None</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>Variable names mapped to their values.</p> <p>Raises:</p> Type Description <code>XlaFlagConflictError</code> <p>If a flag in <code>runtime</code> conflicts with <code>XLA_FLAGS</code> in <code>env</code>.</p> <code>RuntimeConfigurationError</code> <p>If <code>runtime</code> sets a memory fraction and <code>env</code> holds the deprecated <code>XLA_PYTHON_CLIENT_MEM_FRACTION</code>.</p>"},{"location":"api/runtime/#substrax.runtime.resolve_test_runtime","title":"<code>resolve_test_runtime(env, *, prefix, cuda_plugin_available, default_cpu_devices=8)</code>","text":"<p>Choose the backend and the emulated CPU devices of a test run.</p> <p>Tests run on the CPU unless <code>{prefix}JAX_PLATFORMS</code> names an accelerator, which is then used without CPU emulation. An inherited <code>JAX_PLATFORMS</code>, such as the <code>cuda,cpu</code> a developer shell exports, does not move tests onto an accelerator, so a local run matches CI. The CPU device count is <code>{prefix}DEVICE_COUNT</code> when set (<code>0</code> for no emulation), otherwise the count <code>JAX_NUM_CPU_DEVICES</code> or the XLA device-count flag already names, otherwise <code>default_cpu_devices</code>.</p> <p>Parameters:</p> Name Type Description Default <code>env</code> <code>Mapping[str, str]</code> <p>The process environment.</p> required <code>prefix</code> <code>str</code> <p>The prefix of the repository's test variables, such as <code>\"DATARAX_TEST_\"</code>.</p> required <code>cuda_plugin_available</code> <code>bool</code> <p>Whether a JAX CUDA plugin is installed.</p> required <code>default_cpu_devices</code> <code>int</code> <p>The emulated device count when nothing else names one.</p> <code>8</code> <p>Returns:</p> Type Description <code>JaxRuntime</code> <p>The runtime of the test process.</p> <p>Raises:</p> Type Description <code>RuntimeConfigurationError</code> <p>If CUDA is requested and no JAX CUDA plugin is installed.</p> <code>ValueError</code> <p>If <code>{prefix}DEVICE_COUNT</code> is not a non-negative integer.</p>"},{"location":"api/runtime/#substrax.runtime.runtime_environment","title":"<code>runtime_environment(runtime, base)</code>","text":"<p>Return the environment variables that apply <code>runtime</code> to a process not yet running jax.</p> <p>Only the variables <code>runtime</code> sets are returned, and <code>base</code> is read, never changed. A field replaces the variable <code>base</code> holds, except <code>xla_flags</code>, which merges into <code>base</code>'s <code>XLA_FLAGS</code> by flag name. Pass the result to a child process on top of its environment, or write it to <code>os.environ</code> before importing jax.</p> <p>Parameters:</p> Name Type Description Default <code>runtime</code> <code>JaxRuntime</code> <p>The settings to render.</p> required <code>base</code> <code>Mapping[str, str]</code> <p>The environment the variables will be added to.</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>Variable names mapped to their values.</p> <p>Raises:</p> Type Description <code>RuntimeConfigurationError</code> <p>If <code>runtime</code> sets a memory fraction and <code>base</code> still holds the deprecated <code>XLA_PYTHON_CLIENT_MEM_FRACTION</code>, which jax refuses beside the current name.</p> <code>XlaFlagConflictError</code> <p>If a requested flag has a different value in <code>base</code>'s <code>XLA_FLAGS</code>.</p>"},{"location":"api/runtime/#substrax.runtime.merge_xla_flags","title":"<code>merge_xla_flags(existing, requested)</code>","text":"<p>Merge requested flags into an <code>XLA_FLAGS</code> value by flag name.</p> <p>Flags already present keep their position, and new flags follow in the order requested. A requested flag already set to the same value changes nothing, and one set to a different value raises, so a value already in the environment is never silently replaced.</p> <p>Parameters:</p> Name Type Description Default <code>existing</code> <code>str</code> <p>The current <code>XLA_FLAGS</code> value.</p> required <code>requested</code> <code>Iterable[str]</code> <p>Flags to add, each <code>--name</code> or <code>--name=value</code>.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The merged <code>XLA_FLAGS</code> value, with flags separated by single spaces.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If a token is not a flag.</p> <code>XlaFlagConflictError</code> <p>If a requested flag has a different value from an existing one.</p>"},{"location":"api/runtime/#substrax.runtime.parse_xla_flags","title":"<code>parse_xla_flags(value)</code>","text":"<p>Parse an <code>XLA_FLAGS</code> value into flag names mapped to their values, in order.</p> <p>Flags are separated by whitespace. <code>--name=value</code> maps <code>--name</code> to <code>value</code>, and a bare <code>--name</code> maps it to <code>None</code>. A flag given twice with the same value counts once.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>str</code> <p>The <code>XLA_FLAGS</code> string.</p> required <p>Returns:</p> Type Description <code>dict[str, str | None]</code> <p>Each flag name, with its leading dashes, mapped to its value.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If a token is not a flag.</p> <code>XlaFlagConflictError</code> <p>If one flag is given two different values.</p>"},{"location":"api/runtime/#substrax.runtime.managed_env","title":"<code>substrax.runtime.managed_env</code>","text":"<p>The managed environment file a repository's <code>setup.sh</code> writes and its <code>activate.sh</code> sources.</p> <p>Run as <code>python -m substrax.runtime.managed_env write --prefix DATARAX --backend auto --output .datarax.env</code> after the project environment is synced. The file declares every variable it manages in <code>&lt;PREFIX&gt;_MANAGED_ENV_VARS</code>, so <code>activate.sh</code> can unset them before sourcing a new one, then exports or unsets each. The JAX settings come from :class:<code>JaxRuntime</code> through :func:<code>runtime_environment</code>, so the file uses the names jax reads today: the memory fraction is <code>XLA_CLIENT_MEM_FRACTION</code>, and the deprecated <code>XLA_PYTHON_CLIENT_MEM_FRACTION</code>, which jaxlib refuses beside it, is always unset. Preallocation and the memory fraction apply only to an accelerator backend: jaxlib reads them when it builds the GPU plugin's options, so on the CPU they are unset. A repository adds its own variables (for example TensorFlow's log level) with <code>--set</code>.</p> <p>Importing this module imports no jax.</p>"},{"location":"api/runtime/#substrax.runtime.managed_env.nvidia_gpu_visible","title":"<code>nvidia_gpu_visible()</code>","text":"<p>Whether <code>nvidia-smi</code> lists at least one GPU.</p>"},{"location":"api/runtime/#substrax.runtime.managed_env.resolve_backend","title":"<code>resolve_backend(requested)</code>","text":"<p>The concrete backend for <code>requested</code>, resolving <code>auto</code> from the host.</p>"},{"location":"api/runtime/#substrax.runtime.managed_env.managed_environment","title":"<code>managed_environment(*, prefix, backend, project_root, extra)</code>","text":"<p>Every managed variable mapped to its value, or <code>None</code> for a variable the file unsets.</p> <p>Parameters:</p> Name Type Description Default <code>prefix</code> <code>str</code> <p>The repository's variable prefix, such as <code>\"DATARAX\"</code>.</p> required <code>backend</code> <code>str</code> <p>One of :data:<code>BACKENDS</code>; <code>auto</code> is resolved first.</p> required <code>project_root</code> <code>Path</code> <p>The repository root, written as <code>&lt;PREFIX&gt;_ENV_ROOT</code>.</p> required <code>extra</code> <code>Mapping[str, str]</code> <p>The repository's own variables, written as given.</p> required <p>Returns:</p> Type Description <code>dict[str, str | None]</code> <p>The managed variables in the order they are written.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the prefix is not an upper-case shell name, the backend is unknown, or <code>extra</code> names a variable the file manages itself.</p>"},{"location":"api/runtime/#substrax.runtime.managed_env.render","title":"<code>render(*, prefix, values)</code>","text":"<p>The shell text of a managed environment file: the manifest, then one line per variable.</p>"},{"location":"api/runtime/#substrax.runtime.managed_env.main","title":"<code>main(argv=None)</code>","text":"<p>Run the <code>write</code> or <code>show</code> command.</p> <p>Parameters:</p> Name Type Description Default <code>argv</code> <code>Sequence[str] | None</code> <p>The arguments after the module name; the process arguments when <code>None</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>int</code> <p>The exit status.</p>"},{"location":"api/spmd/","title":"SPMD","text":"<p><code>substrax.spmd</code> is the data-parallel training path on top of <code>substrax.mesh</code>: place a batch on the data axis, run one step, reduce metrics across devices.</p> <pre><code>import jax\nfrom flax import nnx\n\nfrom substrax.mesh import DeviceMeshManager\nfrom substrax.spmd import create_data_parallel_sharding, place_batch_on_shards, spmd_train_step\n\nmesh = DeviceMeshManager.create_device_mesh({\"data\": jax.device_count()})\nsharding = create_data_parallel_sharding(mesh)\n\n\n@nnx.jit\ndef train_step(model, optimizer, batch):\n    return spmd_train_step(model, optimizer, loss_fn, batch)\n\n\nwith jax.set_mesh(mesh):\n    loss = train_step(model, optimizer, place_batch_on_shards(batch, sharding))\n</code></pre> <p><code>spmd_train_step</code> differentiates with <code>nnx.value_and_grad</code> and updates the <code>nnx.Optimizer</code>; XLA inserts the gradient all-reduce when the parameters are sharded under <code>jax.set_mesh</code>. The collectives (<code>reduce_mean</code>, <code>reduce_sum</code>, <code>all_gather</code>, <code>collect_from_devices</code> and friends) work on metric dictionaries.</p>"},{"location":"api/spmd/#substrax.spmd","title":"<code>substrax.spmd</code>","text":"<p>Data-parallel placement, the SPMD training step and collectives.</p> <p>A data-parallel gradient needs no reduction after differentiating. Under <code>jax.jit</code> with the batch sharded over a mesh axis, the gradient of the batch's loss is already the full gradient: the compiler inserts the all-reduce (<code>spmd_train_step</code> relies on this). Inside <code>jax.shard_map</code> each device sees its shard, and the loss is averaged over the axis before differentiating, <code>jax.grad(lambda w: jax.lax.pmean(loss(w, shard), axis))</code>; the gradient of a replicated parameter then comes back as the full one. Averaging the gradient afterwards is wrong by the axis size, because the gradient of a replicated parameter already arrives summed over the axis. <code>reduce_mean_collective</code> and <code>reduce_sum_collective</code> average or sum per-device metrics inside <code>shard_map</code>.</p>"},{"location":"api/spmd/#substrax.spmd.all_gather","title":"<code>all_gather(metrics, axis_name='batch')</code>","text":"<p>Gather metrics from all devices.</p> <p>Only valid inside a pmap or shard_map context.</p> <p>Parameters:</p> Name Type Description Default <code>metrics</code> <code>dict[str, Any]</code> <p>The metrics to gather.</p> required <code>axis_name</code> <code>str</code> <p>The name of the axis to gather across.</p> <code>'batch'</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary of gathered metrics.</p>"},{"location":"api/spmd/#substrax.spmd.collect_from_devices","title":"<code>collect_from_devices(metrics)</code>","text":"<p>Collect metrics from all devices.</p> <p>Call outside of a pmapped function to split per-device values from the leading device axis.</p> <p>Parameters:</p> Name Type Description Default <code>metrics</code> <code>dict[str, Any]</code> <p>The metrics from all devices, with the first dimension corresponding to the device axis.</p> required <p>Returns:</p> Type Description <code>dict[str, list[Any] | Any]</code> <p>A dictionary of metrics, with array values split into per-device lists.</p>"},{"location":"api/spmd/#substrax.spmd.reduce_custom","title":"<code>reduce_custom(metrics, reduce_fn=None)</code>","text":"<p>Apply custom reduction operations to metrics.</p> <p>Uses standard JAX operations. Works in SPMD contexts.</p> <p>Parameters:</p> Name Type Description Default <code>metrics</code> <code>dict[str, Any]</code> <p>The metrics to reduce.</p> required <code>reduce_fn</code> <code>dict[str, str | None] | None</code> <p>A dictionary mapping metric names to reduction operations. Each operation should be one of {\"mean\", \"sum\", \"max\", \"min\"}. If None, defaults to \"mean\" for all metrics.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary of reduced metrics.</p>"},{"location":"api/spmd/#substrax.spmd.reduce_max","title":"<code>reduce_max(metrics)</code>","text":"<p>Compute the maximum of metrics using standard JAX operations.</p> <p>Works with global arrays in SPMD contexts (nnx.jit + mesh).</p> <p>Parameters:</p> Name Type Description Default <code>metrics</code> <code>dict[str, Any]</code> <p>The metrics to reduce.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary of maximum metrics.</p>"},{"location":"api/spmd/#substrax.spmd.reduce_mean","title":"<code>reduce_mean(metrics)</code>","text":"<p>Compute the mean of metrics using standard JAX operations.</p> <p>Works with global arrays in SPMD contexts (nnx.jit + mesh).</p> <p>Parameters:</p> Name Type Description Default <code>metrics</code> <code>dict[str, Any]</code> <p>The metrics to reduce.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary of mean-reduced metrics.</p>"},{"location":"api/spmd/#substrax.spmd.reduce_mean_collective","title":"<code>reduce_mean_collective(metrics, axis_name='batch')</code>","text":"<p>Compute the mean of metrics using collective operations.</p> <p>Only valid inside a pmap or shard_map context.</p> <p>Parameters:</p> Name Type Description Default <code>metrics</code> <code>dict[str, Any]</code> <p>The metrics to reduce.</p> required <code>axis_name</code> <code>str</code> <p>The name of the axis to reduce across.</p> <code>'batch'</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary of mean metrics.</p>"},{"location":"api/spmd/#substrax.spmd.reduce_min","title":"<code>reduce_min(metrics)</code>","text":"<p>Compute the minimum of metrics using standard JAX operations.</p> <p>Works with global arrays in SPMD contexts (nnx.jit + mesh).</p> <p>Parameters:</p> Name Type Description Default <code>metrics</code> <code>dict[str, Any]</code> <p>The metrics to reduce.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary of minimum metrics.</p>"},{"location":"api/spmd/#substrax.spmd.reduce_sum","title":"<code>reduce_sum(metrics)</code>","text":"<p>Compute the sum of metrics using standard JAX operations.</p> <p>Works with global arrays in SPMD contexts (nnx.jit + mesh).</p> <p>Parameters:</p> Name Type Description Default <code>metrics</code> <code>dict[str, Any]</code> <p>The metrics to reduce.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary of summed metrics.</p>"},{"location":"api/spmd/#substrax.spmd.reduce_sum_collective","title":"<code>reduce_sum_collective(metrics, axis_name='batch')</code>","text":"<p>Compute the sum of metrics using collective operations.</p> <p>Only valid inside a pmap or shard_map context.</p> <p>Parameters:</p> Name Type Description Default <code>metrics</code> <code>dict[str, Any]</code> <p>The metrics to reduce.</p> required <code>axis_name</code> <code>str</code> <p>The name of the axis to reduce across.</p> <code>'batch'</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary of summed metrics.</p>"},{"location":"api/spmd/#substrax.spmd.create_data_parallel_sharding","title":"<code>create_data_parallel_sharding(mesh, data_axis='data')</code>","text":"<p>Create the sharding that splits a batch's leading axis over a mesh axis.</p> <p>Parameters:</p> Name Type Description Default <code>mesh</code> <code>Mesh</code> <p>The device mesh to use for sharding.</p> required <code>data_axis</code> <code>str</code> <p>The name of the mesh axis to use for data parallelism.</p> <code>'data'</code> <p>Returns:</p> Type Description <code>NamedSharding</code> <p>A <code>NamedSharding</code> over <code>mesh</code> with spec <code>PartitionSpec(data_axis)</code>.</p>"},{"location":"api/spmd/#substrax.spmd.place_batch_on_shards","title":"<code>place_batch_on_shards(batch, sharding)</code>","text":"<p>Turn this process's batch into global arrays on <code>sharding</code>.</p> <p>Array leaves, whether <code>jax.Array</code> or a NumPy array as a host data loader yields them, go to <code>jax.make_array_from_process_local_data</code> in one call. On a single process that is one batched <code>jax.device_put</code>. On several processes each process passes the slice of the global batch it loaded, and jax stitches the slices into one global array. Any other leaf, such as a string, is returned as it is. Call it on the host batches a loader yields, outside <code>jax.jit</code>: inside a traced function the result is not placed on <code>sharding</code>.</p> <p>Parameters:</p> Name Type Description Default <code>batch</code> <code>PyTree</code> <p>The batch this process loaded.</p> required <code>sharding</code> <code>Sharding</code> <p>The sharding of the global batch.</p> required <p>Returns:</p> Type Description <code>PyTree</code> <p>The batch with every array leaf replaced by the global array on <code>sharding</code>.</p>"},{"location":"api/spmd/#substrax.spmd.place_nnx_state_on_shards","title":"<code>place_nnx_state_on_shards(state, mesh, filter_sharding)</code>","text":"<p>Shard a Flax NNX state tree using current NNX sharding helpers.</p>"},{"location":"api/spmd/#substrax.spmd.spmd_train_step","title":"<code>spmd_train_step(model, optimizer, loss_fn, batch)</code>","text":"<p>Execute a data-parallel training step using SPMD.</p> <p>Uses nnx.value_and_grad for automatic differentiation. The XLA compiler handles gradient AllReduce automatically when model parameters are sharded across devices via jax.set_mesh or explicit NamedSharding.</p> <p>This function should be called inside an @nnx.jit decorated function with a mesh context active.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The NNX model to train.</p> required <code>optimizer</code> <code>Optimizer[Any]</code> <p>The NNX optimizer wrapping the model.</p> required <code>loss_fn</code> <code>Callable[[Module, PyTree], Array]</code> <p>Function (model, batch) -&gt; loss scalar.</p> required <code>batch</code> <code>PyTree</code> <p>The training batch (should be pre-sharded).</p> required <p>Returns:</p> Type Description <code>Array</code> <p>The loss value.</p> Example <p>mesh = DeviceMeshManager.create_device_mesh({\"data\": 4}) rules = data_parallel_rules()</p> <p>@nnx.jit def train_step(model, optimizer, batch):     return spmd_train_step(model, optimizer, my_loss_fn, batch)</p> <p>with jax.set_mesh(mesh):     loss = train_step(model, optimizer, batch)</p>"},{"location":"api/testing/","title":"Testing","text":"<p><code>substrax.testing</code> holds the test infrastructure JAX packages share: fresh-interpreter runs with a chosen JAX configuration, trace counts, example runs, and an opt-in pytest plugin. Install it with <code>substrax[testing]</code>.</p> <pre><code>from substrax.runtime import JaxRuntime\nfrom substrax.testing import run_python\n\nresult = run_python(\n    \"import json, jax; print(json.dumps(jax.device_count()))\",\n    runtime=JaxRuntime(cpu_devices=8),\n    timeout=120,\n)\nassert result.check().last_json() == 8\n</code></pre> <p><code>run_python</code> runs this interpreter on code or a script. The child's environment drops every <code>JAX_*</code> and <code>XLA_*</code> variable the parent inherited, then applies <code>env</code> and the <code>runtime</code>. Unless either of them chooses otherwise, the child runs on the CPU backend without preallocation, so a test never starts an accelerator it did not ask for. <code>check()</code> raises <code>ChildFailedError</code> with the end of the child's standard error, and <code>last_json()</code> parses the last line the child printed. <code>cuda_is_visible()</code> asks a child on the CUDA backend whether jax sees a GPU.</p>"},{"location":"api/testing/#counting-traces","title":"Counting traces","text":"<p><code>TraceCounter</code> counts how often a function's Python body runs. Under <code>jax.jit</code> and <code>nnx.jit</code> the body runs once per trace, so the count is the number of traces a test caused:</p> <pre><code>import jax\nimport jax.numpy as jnp\n\nfrom substrax.testing import TraceCounter\n\n\ndef double(x):\n    return x * 2.0\n\n\ncounter = TraceCounter()\nstep = jax.jit(counter.wrap(double))\n\nwith counter.expect(new_traces=1):\n    step(jnp.ones(3))\nwith counter.expect(new_traces=0):  # same shape and dtype: no new trace\n    step(jnp.zeros(3))\n</code></pre> <p><code>expect</code> raises <code>RetraceError</code>, an <code>AssertionError</code> that names the expected and observed counts, and an error raised inside the block propagates unchanged. Each counter keeps its own count, so there is no global registry to clear. Wrap the Python function, then jit the wrapper. The counter does not listen to jax's <code>jax.monitoring</code> trace event: jax records that event only for top-level traces, and its name is not documented.</p>"},{"location":"api/testing/#running-examples","title":"Running examples","text":"<p><code>substrax.examples.discover_examples</code> lists a repository's example scripts, and <code>run_example</code> runs one in a fresh interpreter from the repository root:</p> <pre><code>from pathlib import Path\n\nimport pytest\n\nfrom substrax.examples import discover_examples\nfrom substrax.testing import run_example\n\nROOT = Path(__file__).resolve().parents[2]\nEXAMPLES = discover_examples(ROOT / \"examples\")\n\n\n@pytest.mark.parametrize(\"path\", EXAMPLES, ids=lambda path: path.relative_to(ROOT).as_posix())\ndef test_example_runs(path: Path, output_dir: Path) -&gt; None:\n    run = run_example(\n        path, repo_root=ROOT, output_dir=output_dir, timeout=600, call_main=True\n    )\n    assert run.result.check().returncode == 0\n    assert run.summary[\"relative_l2_error\"] &lt; 1e-2\n</code></pre> <ul> <li><code>discover_examples</code> skips every file or directory whose name starts with <code>_</code>, such as   <code>__init__.py</code>, <code>_common</code> helpers and <code>_templates</code>. <code>include</code> narrows the list further, for   example to a numbering rule.</li> <li><code>call_main=True</code> loads the example under a name other than <code>__main__</code>, calls <code>main()</code> and decodes   its return value from JSON into <code>run.summary</code>. 0-d numpy and jax values become Python numbers,   and a value JSON cannot hold fails the run, naming its key. <code>call_main=False</code> runs the file as   a script. In both modes the example's own directory comes first on <code>sys.path</code>, and it sees no   command-line arguments, as in a script run.</li> <li>The child runs through <code>run_python</code>, with <code>AVITAI_OUTPUT_DIR</code> set to <code>output_dir</code>. An example that   resolves its outputs with <code>resolve_output_dir</code> never writes into the repository, and logging or   64-bit settings made at import end with the child.</li> <li>A timeout raises <code>ExampleTimeoutError</code>, an <code>AssertionError</code> naming the budget and the end of the   example's standard error. A timeout is a failure, not a skip.</li> <li><code>unavailable_reason(run, signatures)</code> returns the first of a repository's messages found in a   failed run's standard error, for skipping an example whose dataset is not configured here.</li> </ul>"},{"location":"api/testing/#the-pytest-plugin","title":"The pytest plugin","text":"<p>Enable it in the top-level <code>conftest.py</code>:</p> <pre><code>pytest_plugins = [\"substrax.testing.pytest_plugin\"]\n</code></pre> Provides Behaviour <code>@pytest.mark.x64</code> Runs the test inside <code>jax.enable_x64(True)</code>, a thread-local setting Configuration isolation Snapshots <code>jax.config.values</code> around every test and fails a test that changed a global value, after setting it back Collection guard Fails the session when a module left x64 on at import although <code>JAX_ENABLE_X64</code> did not ask for it <code>@pytest.mark.devices(count, kind=None)</code> Skips unless at least <code>count</code> devices, of <code>kind</code> when given, are visible <code>@pytest.mark.accelerator(kind=None)</code> Skips unless jax's default backend is an accelerator, of <code>kind</code> when given <code>output_dir</code> fixture A fresh <code>outputs</code> directory under <code>tmp_path</code>, with <code>AVITAI_OUTPUT_DIR</code> pointing at it for the test <p>The isolation follows jax's own test harness, whose <code>JaxTestCase</code> fails a test that changes a global configuration value. A test whose subject changes the global configuration sets it back with <code>restored_jax_config()</code>, which reports the names it restored:</p> <pre><code>import jax\n\nfrom substrax.runtime import JaxRuntime, apply_runtime\nfrom substrax.testing import restored_jax_config\n\n\ndef test_apply_runtime_enables_x64() -&gt; None:\n    with restored_jax_config() as changed:\n        apply_runtime(JaxRuntime(enable_x64=True))\n        assert jax.config.jax_enable_x64\n\n    assert changed == [\"jax_enable_x64\"]\n</code></pre> <p>A malformed <code>devices</code> or <code>accelerator</code> marker is a usage error at collection. No entry point loads the plugin, so installing substrax changes no other project's tests, and importing it imports no jax.</p>"},{"location":"api/testing/#source-scans","title":"Source scans","text":"<p><code>substrax.testing.source_scans</code> holds the checks a repository's contract tests run over its own source without importing it. Two import-time side effects change the process for everything imported after the module, test collection included: configuring the root logger and writing the environment.</p> <pre><code>from pathlib import Path\n\nfrom substrax.testing.source_scans import (\n    configures_logging,\n    import_time_lines,\n    python_files,\n    writes_environment,\n)\n\nROOT = Path(__file__).resolve().parents[1]\n\n\ndef test_no_module_changes_the_process_at_import() -&gt; None:\n    modules = python_files(ROOT, (\"src\", \"scripts\", \"tests\"), excluded_parts=(\"example_data\",))\n    found = [\n        f\"{path.relative_to(ROOT)}:{line}\"\n        for path in modules\n        for predicate in (configures_logging, writes_environment)\n        for line in import_time_lines(path, predicate)\n    ]\n    assert found == []\n</code></pre> <p><code>import_time_lines</code> skips function, class and lambda bodies and the <code>if __name__ == \"__main__\":</code> block, whose code runs only when called or run as a script; <code>!=</code> and chained comparisons are not guards. <code>writes_environment</code> matches assignment, deletion and <code>|=</code> on <code>os.environ</code>, its mutating methods, and <code>os.putenv</code>/<code>os.unsetenv</code>.</p> <p>The mesh scan finds documented <code>jax.make_mesh</code> calls that do not name <code>axis_types</code>. From jax 0.11 <code>jax.make_mesh</code> builds Explicit axes by default, and the backward pass of a batch-sharded step raises under them, so every mesh a reader copies names its axis types:</p> <pre><code>from substrax.testing.source_scans import documented_texts, unguarded_make_mesh_calls\n\n\ndef test_documented_meshes_name_their_axis_types() -&gt; None:\n    texts = documented_texts(ROOT, package=\"datarax\", script_directories=(\"examples\",))\n    assert {name: calls for name, text in texts.items() if (calls := unguarded_make_mesh_calls(text))} == {}\n</code></pre> <p><code>documented_texts</code> reads every docstring under <code>src/&lt;package&gt;</code>, the README (or the <code>pages</code> given), every Markdown page under <code>docs</code> and every Python file under the script directories. A location that does not exist raises <code>FileNotFoundError</code>, so a mistyped directory fails the check instead of shrinking what it reads.</p>"},{"location":"api/testing/#substrax.testing","title":"<code>substrax.testing</code>","text":"<p>Test helpers for JAX packages: fresh-interpreter runs and an opt-in pytest plugin.</p> <p><code>run_python</code> runs code or a script in a child interpreter whose JAX settings the test chooses, with everything the parent's shell exported about jax removed, and returns a <code>ChildResult</code>. <code>cuda_is_visible</code> probes the CUDA backend in such a child. <code>restored_jax_config</code> sets back the global jax configuration a block changed. <code>TraceCounter</code> counts the traces a jitted function takes and raises <code>RetraceError</code> when a block caused an unexpected number. <code>run_example</code> runs one example per child interpreter with its outputs redirected (<code>substrax.examples.discover_examples</code> lists them). The plugin, <code>substrax.testing.pytest_plugin</code>, is enabled from a <code>conftest.py</code>: it adds the <code>x64</code>, <code>devices</code> and <code>accelerator</code> markers and fails a test that changes jax's global configuration, as jax's own test harness does. <code>substrax.testing.source_scans</code> holds the contract checks a repository runs over its own source: import-time logging and environment writes, and documented meshes without axis types. Importing this package imports no jax.</p>"},{"location":"api/testing/#substrax.testing.ChildFailedError","title":"<code>ChildFailedError(result)</code>","text":"<p>               Bases: <code>AssertionError</code></p> <p>A child interpreter exited with a non-zero code.</p> <p>Attributes:</p> Name Type Description <code>result</code> <code>ChildResult</code> <p>The failed run.</p> <p>Describe the failed run by its exit code and the end of its standard error.</p> <p>Parameters:</p> Name Type Description Default <code>result</code> <code>ChildResult</code> <p>The failed run.</p> required"},{"location":"api/testing/#substrax.testing.ChildResult","title":"<code>ChildResult(*, argv, returncode, stdout, stderr, seconds)</code>  <code>dataclass</code>","text":"<p>What a child interpreter did.</p> <p>Attributes:</p> Name Type Description <code>argv</code> <code>tuple[str, ...]</code> <p>The command line the child ran.</p> <code>returncode</code> <code>int</code> <p>Its exit code.</p> <code>stdout</code> <code>str</code> <p>Everything it wrote to standard output.</p> <code>stderr</code> <code>str</code> <p>Everything it wrote to standard error.</p> <code>seconds</code> <code>float</code> <p>Wall-clock seconds from start to exit.</p>"},{"location":"api/testing/#substrax.testing.ChildResult.check","title":"<code>check()</code>","text":"<p>Return this result when the child exited with code 0.</p> <p>Returns:</p> Type Description <code>ChildResult</code> <p>This result.</p> <p>Raises:</p> Type Description <code>ChildFailedError</code> <p>If the exit code is not 0; the message ends with the last lines of standard error.</p>"},{"location":"api/testing/#substrax.testing.ChildResult.last_json","title":"<code>last_json()</code>","text":"<p>Parse the last non-empty line of standard output as JSON.</p> <p>Returns:</p> Type Description <code>Any</code> <p>The decoded value.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the child wrote no output, or its last line is not JSON.</p>"},{"location":"api/testing/#substrax.testing.ExampleRun","title":"<code>ExampleRun(*, path, result, summary)</code>  <code>dataclass</code>","text":"<p>One run of an example.</p> <p>Attributes:</p> Name Type Description <code>path</code> <code>Path</code> <p>The example that ran.</p> <code>result</code> <code>ChildResult</code> <p>The child interpreter's exit code, output and duration.</p> <code>summary</code> <code>Any</code> <p>What <code>main()</code> returned, decoded from JSON, for a run that called it and exited 0; <code>None</code> otherwise.</p>"},{"location":"api/testing/#substrax.testing.ExampleTimeoutError","title":"<code>ExampleTimeoutError(path, timeout, stderr)</code>","text":"<p>               Bases: <code>AssertionError</code></p> <p>An example ran past its budget and was killed.</p> <p>Attributes:</p> Name Type Description <code>path</code> <code>Path</code> <p>The example.</p> <code>timeout</code> <code>float</code> <p>Its budget in seconds.</p> <code>stderr_tail</code> <code>str</code> <p>The last lines it wrote to standard error before it was killed.</p> <p>Name the example, its budget and the end of its standard error.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>Path</code> <p>The example.</p> required <code>timeout</code> <code>float</code> <p>Its budget in seconds.</p> required <code>stderr</code> <code>str</code> <p>Everything it wrote to standard error before it was killed.</p> required"},{"location":"api/testing/#substrax.testing.RetraceError","title":"<code>RetraceError(expected, observed)</code>","text":"<p>               Bases: <code>AssertionError</code></p> <p>A block caused a different number of traces than the test expected.</p> <p>Attributes:</p> Name Type Description <code>expected</code> <code>int</code> <p>The number of new traces the test expected.</p> <code>observed</code> <code>int</code> <p>The number of new traces the block caused.</p> <p>Record both counts and name them in the message.</p> <p>Parameters:</p> Name Type Description Default <code>expected</code> <code>int</code> <p>The number of new traces the test expected.</p> required <code>observed</code> <code>int</code> <p>The number of new traces the block caused.</p> required"},{"location":"api/testing/#substrax.testing.TraceCounter","title":"<code>TraceCounter()</code>","text":"<p>Count how often wrapped functions run their Python body.</p> <p><code>jax.jit</code> runs a function's body once per trace and reuses the cached trace for later calls with the same static signature, and <code>nnx.jit</code> calls the user function once inside the function jax traces. The count of body executions is therefore the count of traces. An eager call also runs the body and also counts.</p> <p>Each counter keeps its own count, so tests share no state to clear.</p> <p>Start at zero.</p>"},{"location":"api/testing/#substrax.testing.TraceCounter.count","title":"<code>count</code>  <code>property</code>","text":"<p>The number of body executions of every function this counter wrapped.</p>"},{"location":"api/testing/#substrax.testing.TraceCounter.wrap","title":"<code>wrap(function)</code>","text":"<p>Return <code>function</code> counting each run of its body; jit the result, not the original.</p> <p>The wrapper keeps the function's name, docstring and signature, so <code>static_argnames</code> and other signature-based arguments of a transform still resolve.</p> <p>Parameters:</p> Name Type Description Default <code>function</code> <code>Callable[P, R]</code> <p>The Python function to count.</p> required <p>Returns:</p> Type Description <code>Callable[P, R]</code> <p>A function that adds one to the count, then calls <code>function</code>. The count rises</p> <code>Callable[P, R]</code> <p>before the call, so a trace that raises still counts.</p>"},{"location":"api/testing/#substrax.testing.TraceCounter.expect","title":"<code>expect(*, new_traces)</code>","text":"<p>Require exactly <code>new_traces</code> body executions inside the block.</p> <p>An exception raised inside the block propagates unchanged, without a count check.</p> <p>Parameters:</p> Name Type Description Default <code>new_traces</code> <code>int</code> <p>How many new traces the block must cause.</p> required <p>Yields:</p> Name Type Description <code>None</code> <code>None</code> <p>Control to the block.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>new_traces</code> is negative.</p> <code>RetraceError</code> <p>If the block caused a different number of traces.</p>"},{"location":"api/testing/#substrax.testing.cuda_is_visible","title":"<code>cuda_is_visible(*, timeout=120.0)</code>","text":"<p>Whether a fresh interpreter can start jax's CUDA backend and sees at least one GPU.</p> <p>The probe runs in a child without preallocation, so it claims no accelerator memory.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>Seconds before the probe is abandoned.</p> <code>120.0</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when the child found a GPU.</p>"},{"location":"api/testing/#substrax.testing.run_python","title":"<code>run_python(program, *args, timeout, runtime=None, env=None, cwd=None)</code>","text":"<p>Run Python code or a script in a fresh interpreter and return what it did.</p> <p>jax fixes its backends and device set when they start, so tests of device topology or process configuration need their own interpreter. The child runs this interpreter (<code>sys.executable</code>). Its environment is this process's without any <code>JAX_*</code> or <code>XLA_*</code> variable, so settings exported in a developer's shell cannot change what a test measures, then <code>env</code>, then <code>runtime</code> as :func:<code>~substrax.runtime.runtime_environment</code> renders it. Unless <code>runtime</code> or <code>env</code> chooses them, the child runs on the CPU backend without preallocation, so a test never starts an accelerator it did not ask for.</p> <p>Parameters:</p> Name Type Description Default <code>program</code> <code>str | Path</code> <p>Code to run with <code>-c</code>, or the path of a script.</p> required <code>*args</code> <code>str</code> <p>Arguments passed to the program.</p> <code>()</code> <code>timeout</code> <code>float</code> <p>Seconds before the child is killed; every run states its budget.</p> required <code>runtime</code> <code>JaxRuntime | None</code> <p>JAX settings of the child.</p> <code>None</code> <code>env</code> <code>Mapping[str, str] | None</code> <p>Variables set in the child before <code>runtime</code> is applied.</p> <code>None</code> <code>cwd</code> <code>Path | None</code> <p>The child's working directory; this process's when <code>None</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>ChildResult</code> <p>The child's exit code, output and duration. A non-zero exit is returned, not raised.</p> <p>Raises:</p> Type Description <code>XlaFlagConflictError</code> <p>If a flag in <code>runtime</code> conflicts with <code>XLA_FLAGS</code> in <code>env</code>; the child is not started.</p> <code>TimeoutExpired</code> <p>If the child runs longer than <code>timeout</code>.</p>"},{"location":"api/testing/#substrax.testing.run_example","title":"<code>run_example(path, *, repo_root, output_dir, timeout, call_main, runtime=None, env=None)</code>","text":"<p>Run one example in a fresh interpreter from the repository root, with outputs redirected.</p> <p>The child starts through :func:<code>~substrax.testing.run_python</code>, so the JAX settings are the <code>runtime</code> and <code>env</code> given here, not the parent shell's. <code>AVITAI_OUTPUT_DIR</code> is set to <code>output_dir</code>, so an example resolving its outputs with :func:<code>~substrax.artifacts.resolve_output_dir</code> never writes into the repository. Whatever the example configures at import, such as logging or 64-bit types, ends with the child.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>Path</code> <p>The example.</p> required <code>repo_root</code> <code>Path</code> <p>The child's working directory, which relative paths in examples assume.</p> required <code>output_dir</code> <code>Path</code> <p>Where the example's outputs go.</p> required <code>timeout</code> <code>float</code> <p>Seconds before the child is killed; every run states its budget.</p> required <code>call_main</code> <code>bool</code> <p><code>True</code> loads the example under a name other than <code>__main__</code>, calls its <code>main()</code> without arguments and decodes the return value; <code>False</code> runs the file as a script.</p> required <code>runtime</code> <code>JaxRuntime | None</code> <p>JAX settings of the child.</p> <code>None</code> <code>env</code> <code>Mapping[str, str] | None</code> <p>Variables set in the child before <code>runtime</code> is applied.</p> <code>None</code> <p>Returns:</p> Type Description <code>ExampleRun</code> <p>The run. A non-zero exit is returned, not raised; <code>run.result.check()</code> raises it.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>env</code> sets <code>AVITAI_OUTPUT_DIR</code> to a directory other than <code>output_dir</code>.</p> <code>ExampleTimeoutError</code> <p>If the example runs longer than <code>timeout</code>.</p> <code>XlaFlagConflictError</code> <p>If a flag in <code>runtime</code> conflicts with <code>XLA_FLAGS</code> in <code>env</code>; the child is not started.</p>"},{"location":"api/testing/#substrax.testing.unavailable_reason","title":"<code>unavailable_reason(run, signatures)</code>","text":"<p>Return the first signature found in a failed run's standard error, ignoring case.</p> <p>A repository lists the messages that mean an example cannot run in this environment, such as a dataset that is not configured, and skips the example when one matches. A run that exited 0 matches nothing, and standard output is not searched.</p> <p>Parameters:</p> Name Type Description Default <code>run</code> <code>ExampleRun</code> <p>The finished run.</p> required <code>signatures</code> <code>Sequence[str]</code> <p>Messages that mean the example cannot run here.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>The first signature found, as given, or <code>None</code>.</p>"},{"location":"api/testing/#substrax.testing.restored_jax_config","title":"<code>restored_jax_config()</code>","text":"<p>Set back every jax configuration value a block changed, and report which ones.</p> <p>jax's own test base class snapshots <code>jax.config.values</code> around each test and fails a test that changed a global value (<code>assert_global_configs_unchanged</code> in <code>jax/_src/test_util.py</code>). This context manager takes the same snapshot and, on exit, sets every value that differs back through <code>jax.config.update</code>, whether the block finished or raised.</p> <p>A value read inside a thread-local context manager such as <code>jax.enable_x64(True)</code> is that context's, so a context entered and left inside the block changes nothing.</p> <p>Yields:</p> Type Description <code>list[str]</code> <p>list[str]: Empty while the block runs; on exit it holds the sorted names of the values the</p> <code>list[str]</code> <p>block changed, all of them restored.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If jax refuses to set a value back, as it refuses <code>jax_num_cpu_devices</code> once its backends have started.</p>"},{"location":"api/testing/#substrax.testing.source_scans","title":"<code>substrax.testing.source_scans</code>","text":"<p>Source scans a repository's contract tests run over its own files.</p> <p>Two families of check read source rather than run it:</p> <ul> <li>Import-time side effects. :func:<code>import_time_lines</code> walks a module and returns the lines of   the nodes a predicate matches, among those that run when the module is imported: function,   class and lambda bodies and the <code>if __name__ == \"__main__\":</code> block are skipped.   :func:<code>writes_environment</code> matches a change to <code>os.environ</code> (assignment, deletion, <code>|=</code>, a   mutating method, <code>os.putenv</code>/<code>os.unsetenv</code>); :func:<code>configures_logging</code> matches a   <code>logging.basicConfig</code> call. A module that does either at import changes the process for   everything imported after it, test collection included.</li> <li>Documented meshes. :func:<code>unguarded_make_mesh_calls</code> returns each <code>make_mesh</code> call whose   arguments do not name <code>axis_types</code>: from jax 0.11 <code>jax.make_mesh</code> builds   <code>AxisType.Explicit</code> axes by default, and the backward pass of a batch-sharded step raises under   them. :func:<code>documented_texts</code> collects what a reader copies from: every docstring in the   package, the named pages, every Markdown page under the page directories and every Python file   under the script directories.</li> </ul> <p>A corpus location that does not exist raises :class:<code>FileNotFoundError</code>, so a mistyped directory fails the check instead of shrinking what it reads. Importing this module imports no jax.</p>"},{"location":"api/testing/#substrax.testing.source_scans.python_files","title":"<code>python_files(root, directories, *, pattern='*.py', excluded_parts=())</code>","text":"<p>Every file under <code>root / directory</code> matching <code>pattern</code>, in directory then path order.</p> <p>Parameters:</p> Name Type Description Default <code>root</code> <code>Path</code> <p>The repository root.</p> required <code>directories</code> <code>Sequence[str]</code> <p>Directories relative to <code>root</code> to search recursively.</p> required <code>pattern</code> <code>str</code> <p>The file-name glob.</p> <code>'*.py'</code> <code>excluded_parts</code> <code>Collection[str]</code> <p>Directory names whose files are skipped, such as <code>\"example_data\"</code>.</p> <code>()</code> <p>Returns:</p> Type Description <code>list[Path]</code> <p>The matching files.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If a directory does not exist.</p>"},{"location":"api/testing/#substrax.testing.source_scans.writes_environment","title":"<code>writes_environment(node)</code>","text":"<p>Whether <code>node</code> changes <code>os.environ</code> or calls <code>os.putenv</code>/<code>os.unsetenv</code>.</p>"},{"location":"api/testing/#substrax.testing.source_scans.configures_logging","title":"<code>configures_logging(node)</code>","text":"<p>Whether <code>node</code> calls <code>logging.basicConfig</code>, by module attribute or imported name.</p>"},{"location":"api/testing/#substrax.testing.source_scans.is_main_guard","title":"<code>is_main_guard(node)</code>","text":"<p>Whether <code>node</code> is <code>if __name__ == \"__main__\":</code> (either operand order).</p> <p>Its body runs only when the module is run as a script. <code>!=</code> and chained comparisons are not guards: their bodies run at import.</p>"},{"location":"api/testing/#substrax.testing.source_scans.import_time_lines","title":"<code>import_time_lines(path, matches)</code>","text":"<p>Line numbers of the nodes <code>matches</code> accepts that run when <code>path</code> is imported.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>Path</code> <p>A Python source file.</p> required <code>matches</code> <code>Callable[[AST], bool]</code> <p>A predicate over one AST node, such as :func:<code>writes_environment</code>.</p> required <p>Returns:</p> Type Description <code>list[int]</code> <p>The line numbers in source order.</p>"},{"location":"api/testing/#substrax.testing.source_scans.unguarded_make_mesh_calls","title":"<code>unguarded_make_mesh_calls(text)</code>","text":"<p>Each <code>make_mesh</code> call in <code>text</code> whose arguments do not name <code>axis_types</code>.</p> <p>A call whose parentheses never close is returned from its name to the end of the text.</p>"},{"location":"api/testing/#substrax.testing.source_scans.documented_texts","title":"<code>documented_texts(root, *, package, pages=('README.md',), page_directories=('docs',), script_directories=())</code>","text":"<p>Map every text a reader copies code from to its content, keyed by a repository path.</p> <p>Parameters:</p> Name Type Description Default <code>root</code> <code>Path</code> <p>The repository root.</p> required <code>package</code> <code>str</code> <p>The package under <code>root / \"src\"</code> whose docstrings are read.</p> required <code>pages</code> <code>Sequence[str]</code> <p>Individual pages relative to <code>root</code>.</p> <code>('README.md',)</code> <code>page_directories</code> <code>Sequence[str]</code> <p>Directories whose Markdown pages are read.</p> <code>('docs',)</code> <code>script_directories</code> <code>Sequence[str]</code> <p>Directories whose Python files are read whole, such as <code>\"examples\"</code>.</p> <code>()</code> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>Docstrings keyed <code>\"&lt;path&gt;:&lt;line&gt;\"</code> (line 0 for a module docstring), and pages and</p> <code>dict[str, str]</code> <p>scripts keyed by their path, all relative to <code>root</code>.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If the package, a page or a directory does not exist.</p>"},{"location":"api/testing/#substrax.examples","title":"<code>substrax.examples</code>","text":"<p>A repository's example scripts: every <code>*.py</code> under its examples directory that is not private.</p> <p>Importing this module imports no jax.</p>"},{"location":"api/testing/#substrax.examples.discover_examples","title":"<code>discover_examples(root, *, include=None)</code>","text":"<p>List the example scripts under <code>root</code>, sorted.</p> <p>A file or directory whose name starts with <code>_</code> is private, as <code>_common</code> helpers, <code>_templates</code> and <code>__init__.py</code> are, and so is everything under it. Only the parts below <code>root</code> count, so <code>root</code> itself may have such a name.</p> <p>Parameters:</p> Name Type Description Default <code>root</code> <code>Path</code> <p>The examples directory.</p> required <code>include</code> <code>Callable[[Path], bool] | None</code> <p>Keeps a path when it returns <code>True</code>, such as a repository's numbering rule.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[Path]</code> <p>The <code>*.py</code> files under <code>root</code> that are not private and that <code>include</code> keeps.</p>"},{"location":"api/testing/#substrax.testing.compute","title":"<code>substrax.testing.compute</code>","text":"<p>The behaviour every compute backend owes, as pytest tests a backend's own suite inherits.</p> <p>Subclass :class:<code>BackendContract</code> in a test module and define <code>make_backend</code>; the inherited tests submit small jobs of plain Python scripts, wait for them, and check the states, logs, fetched outputs and cancellation the :class:<code>~substrax.compute.backend.ComputeBackend</code> protocol promises. The scripts import no jax, so a backend that runs them locally finishes each test in seconds.</p> <pre><code>class TestLocalBackend(BackendContract):\n    def make_backend(self, state_dir: Path) -&gt; ComputeBackend:\n        return LocalBackend({\"python\": sys.executable, \"state_dir\": str(state_dir)})\n</code></pre>"},{"location":"api/testing/#substrax.testing.compute.BackendContract","title":"<code>BackendContract</code>","text":"<p>Tests every backend must pass; subclass and define :meth:<code>make_backend</code>.</p>"},{"location":"api/testing/#substrax.testing.compute.BackendContract.make_backend","title":"<code>make_backend(state_dir)</code>","text":"<p>Build a fresh backend instance keeping whatever it stores under <code>state_dir</code>.</p> <p>Parameters:</p> Name Type Description Default <code>state_dir</code> <code>Path</code> <p>A directory private to the test.</p> required <p>Returns:</p> Type Description <code>ComputeBackend</code> <p>The backend.</p> <p>Raises:</p> Type Description <code>NotImplementedError</code> <p>Always, until a subclass defines it.</p>"},{"location":"api/testing/#substrax.testing.compute.BackendContract.project","title":"<code>project(tmp_path)</code>","text":"<p>A project holding the contract's scripts.</p> <p>Parameters:</p> Name Type Description Default <code>tmp_path</code> <code>Path</code> <p>The test's directory.</p> required <p>Returns:</p> Type Description <code>Path</code> <p>The project root.</p>"},{"location":"api/testing/#substrax.testing.compute.BackendContract.state_dir","title":"<code>state_dir(tmp_path)</code>","text":"<p>The directory the backend under test keeps its state in.</p> <p>Parameters:</p> Name Type Description Default <code>tmp_path</code> <code>Path</code> <p>The test's directory.</p> required <p>Returns:</p> Type Description <code>Path</code> <p>The directory.</p>"},{"location":"api/testing/#substrax.testing.compute.BackendContract.test_a_job_succeeds_and_its_outputs_come_back","title":"<code>test_a_job_succeeds_and_its_outputs_come_back(project, state_dir, tmp_path)</code>","text":"<p>A succeeding job ends <code>succeeded</code> and fetches its logs, outputs and manifest.</p>"},{"location":"api/testing/#substrax.testing.compute.BackendContract.test_a_failed_task_fails_the_run_and_the_tasks_after_it_still_run","title":"<code>test_a_failed_task_fails_the_run_and_the_tasks_after_it_still_run(project, state_dir, tmp_path)</code>","text":"<p>A failed task makes the run <code>failed</code> without skipping the rest.</p>"},{"location":"api/testing/#substrax.testing.compute.BackendContract.test_following_the_logs_ends_with_the_run_and_names_each_task","title":"<code>test_following_the_logs_ends_with_the_run_and_names_each_task(project, state_dir)</code>","text":"<p>Followed logs carry each task's lines, prefixed with its name, and stop at the end.</p>"},{"location":"api/testing/#substrax.testing.compute.BackendContract.test_a_cancelled_run_ends_cancelled","title":"<code>test_a_cancelled_run_ends_cancelled(project, state_dir)</code>","text":"<p>Cancelling a running job ends it as <code>cancelled</code>; cancelling again changes nothing.</p>"},{"location":"api/testing/#substrax.testing.compute.BackendContract.test_a_stored_handle_finds_the_run_from_a_new_backend","title":"<code>test_a_stored_handle_finds_the_run_from_a_new_backend(project, state_dir, tmp_path)</code>","text":"<p>A handle read back from a <code>RunStore</code> is enough for another backend instance.</p>"},{"location":"api/testing/#substrax.testing.compute.wait_until_final","title":"<code>wait_until_final(backend, handle, *, timeout=_WAIT_SECONDS)</code>","text":"<p>Poll a run until it ends.</p> <p>Parameters:</p> Name Type Description Default <code>backend</code> <code>ComputeBackend</code> <p>The backend that runs it.</p> required <code>handle</code> <code>RunHandle</code> <p>The run.</p> required <code>timeout</code> <code>float</code> <p>Seconds to wait.</p> <code>_WAIT_SECONDS</code> <p>Returns:</p> Type Description <code>RunState</code> <p>Its final state.</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If the run has not ended after <code>timeout</code> seconds.</p>"},{"location":"api/tracking/","title":"Tracking","text":"<p><code>substrax.tracking</code> records scalars, images, histograms, text and hyperparameters per step, to the console, to files, to Weights &amp; Biases or to MLflow, behind one <code>Logger</code> interface.</p> <pre><code>from substrax.tracking import FileLogger, WandbLogger\n\nlogger = FileLogger(\"run-1\", log_dir=\"logs/run-1\")\nlogger.log_scalars({\"loss\": 0.42, \"accuracy\": 0.9}, step=100)\nlogger.log_image(\"samples\", images, step=100)\nlogger.log_hyperparams({\"learning_rate\": 1e-3})\nlogger.close()\n\nwandb_logger = WandbLogger(\"run-1\", project=\"my-project\")\n</code></pre> Backend Extra Where things go <code>ConsoleLogger</code> none <code>logging</code> lines on stdout <code>FileLogger</code> <code>plots</code> for figures Lines to a <code>.log</code> file, scalars to a long-form CSV (<code>timestamp,step,name,value</code>), images and histograms to PNGs, texts and hyperparameters to text files <code>WandbLogger</code> <code>wandb</code> A W&amp;B run; <code>log_code</code> and <code>log_model</code> upload code and model artifacts <code>MLFlowLogger</code> <code>mlflow</code>, <code>plots</code> for figures An MLflow run; images, histograms and texts as run artifacts <p>Importing <code>substrax.tracking</code> imports neither SDK. A backend loads its SDK when constructed and raises <code>ImportError</code> naming the extra to install:</p> <pre><code>uv add 'substrax[wandb]'\n</code></pre>"},{"location":"api/tracking/#substrax.tracking","title":"<code>substrax.tracking</code>","text":"<p>Step-wise experiment tracking with console, file, Weights &amp; Biases and MLflow backends.</p> <p>Importing this package imports neither <code>wandb</code> nor <code>mlflow</code>; each backend loads its SDK when constructed and raises <code>ImportError</code> naming the extra that provides it.</p>"},{"location":"api/tracking/#substrax.tracking.ConsoleLogger","title":"<code>ConsoleLogger(name, log_dir=None, level=logging.INFO)</code>","text":"<p>               Bases: <code>Logger</code></p> <p>Logger that writes lines to the console only.</p>"},{"location":"api/tracking/#substrax.tracking.ConsoleLogger.log_scalars","title":"<code>log_scalars(scalars, step=None)</code>","text":"<p>Log several scalars as one line.</p>"},{"location":"api/tracking/#substrax.tracking.ConsoleLogger.log_image","title":"<code>log_image(name, image, step=None)</code>","text":"<p>Log the image count or shape, not the pixels.</p>"},{"location":"api/tracking/#substrax.tracking.ConsoleLogger.log_histogram","title":"<code>log_histogram(name, values, step=None)</code>","text":"<p>Log the summary statistics of <code>values</code>, not the histogram.</p>"},{"location":"api/tracking/#substrax.tracking.ConsoleLogger.log_text","title":"<code>log_text(name, text, step=None)</code>","text":"<p>Log <code>text</code> under <code>name</code>.</p>"},{"location":"api/tracking/#substrax.tracking.ConsoleLogger.log_hyperparams","title":"<code>log_hyperparams(params)</code>","text":"<p>Log one line per hyperparameter.</p>"},{"location":"api/tracking/#substrax.tracking.FileLogger","title":"<code>FileLogger(name, log_dir, filename=None, level=logging.INFO)</code>","text":"<p>               Bases: <code>ConsoleLogger</code></p> <p>Logger that writes the console lines to a file as well.</p> <p>Scalars also go to <code>&lt;name&gt;_&lt;timestamp&gt;_metrics.csv</code> (one <code>timestamp, step, name, value</code> row per scalar), images to <code>images/</code>, histograms to <code>histograms/</code>, texts to <code>texts/</code> and hyperparameters to <code>hyperparams_&lt;timestamp&gt;.txt</code>. Images and histograms need the <code>plots</code> extra; without it they are logged as lines only.</p> <p>Initialise the file logger.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the logger.</p> required <code>log_dir</code> <code>str | Path</code> <p>Directory to save logs to.</p> required <code>filename</code> <code>str | None</code> <p>Log filename. <code>None</code> derives one from <code>name</code> and the time.</p> <code>None</code> <code>level</code> <code>int</code> <p>Logging level.</p> <code>INFO</code>"},{"location":"api/tracking/#substrax.tracking.FileLogger.log_scalars","title":"<code>log_scalars(scalars, step=None)</code>","text":"<p>Log several scalars as one line and append them to the metrics CSV.</p>"},{"location":"api/tracking/#substrax.tracking.FileLogger.log_image","title":"<code>log_image(name, image, step=None)</code>","text":"<p>Log the image and save it under <code>images/</code>.</p>"},{"location":"api/tracking/#substrax.tracking.FileLogger.log_histogram","title":"<code>log_histogram(name, values, step=None)</code>","text":"<p>Log the summary statistics and save the histogram under <code>histograms/</code>.</p>"},{"location":"api/tracking/#substrax.tracking.FileLogger.log_text","title":"<code>log_text(name, text, step=None)</code>","text":"<p>Log <code>text</code> and save it under <code>texts/</code>.</p>"},{"location":"api/tracking/#substrax.tracking.FileLogger.log_hyperparams","title":"<code>log_hyperparams(params)</code>","text":"<p>Log the hyperparameters and save them to <code>hyperparams_&lt;timestamp&gt;.txt</code>.</p>"},{"location":"api/tracking/#substrax.tracking.Logger","title":"<code>Logger(name, log_dir=None, level=logging.INFO)</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Base class for all loggers.</p> <p>Concrete loggers record scalars, images, histograms, text and hyperparameters somewhere; every logger also owns a :mod:<code>logging</code> logger named after it whose handlers it replaces, so lines reach stdout exactly once.</p> <p>Initialise the logger.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the logger and of its :mod:<code>logging</code> logger.</p> required <code>log_dir</code> <code>str | Path | None</code> <p>Directory for files, created if missing. <code>None</code> writes no files.</p> <code>None</code> <code>level</code> <code>int</code> <p>Logging level from the :mod:<code>logging</code> module.</p> <code>INFO</code>"},{"location":"api/tracking/#substrax.tracking.Logger.log","title":"<code>log(msg, level=logging.INFO)</code>","text":"<p>Log a message at <code>level</code>.</p>"},{"location":"api/tracking/#substrax.tracking.Logger.debug","title":"<code>debug(msg)</code>","text":"<p>Log a debug message.</p>"},{"location":"api/tracking/#substrax.tracking.Logger.info","title":"<code>info(msg)</code>","text":"<p>Log an info message.</p>"},{"location":"api/tracking/#substrax.tracking.Logger.warning","title":"<code>warning(msg)</code>","text":"<p>Log a warning message.</p>"},{"location":"api/tracking/#substrax.tracking.Logger.error","title":"<code>error(msg)</code>","text":"<p>Log an error message.</p>"},{"location":"api/tracking/#substrax.tracking.Logger.critical","title":"<code>critical(msg)</code>","text":"<p>Log a critical message.</p>"},{"location":"api/tracking/#substrax.tracking.Logger.log_scalar","title":"<code>log_scalar(name, value, step=None)</code>","text":"<p>Log one scalar; the default records it through :meth:<code>log_scalars</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the scalar.</p> required <code>value</code> <code>float</code> <p>Scalar value to log.</p> required <code>step</code> <code>int | None</code> <p>Global step value to record, for example the training iteration.</p> <code>None</code>"},{"location":"api/tracking/#substrax.tracking.Logger.log_scalars","title":"<code>log_scalars(scalars, step=None)</code>  <code>abstractmethod</code>","text":"<p>Log several scalars.</p> <p>Parameters:</p> Name Type Description Default <code>scalars</code> <code>Mapping[str, float]</code> <p>Scalar names to values.</p> required <code>step</code> <code>int | None</code> <p>Global step value to record.</p> <code>None</code>"},{"location":"api/tracking/#substrax.tracking.Logger.log_image","title":"<code>log_image(name, image, step=None)</code>  <code>abstractmethod</code>","text":"<p>Log an image or a list of images.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the image.</p> required <code>image</code> <code>ArrayLike | Sequence[ArrayLike]</code> <p>One array of shape <code>(H, W)</code> or <code>(H, W, C)</code>, or a list of them.</p> required <code>step</code> <code>int | None</code> <p>Global step value to record.</p> <code>None</code>"},{"location":"api/tracking/#substrax.tracking.Logger.log_histogram","title":"<code>log_histogram(name, values, step=None)</code>  <code>abstractmethod</code>","text":"<p>Log a histogram of values.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the histogram.</p> required <code>values</code> <code>ArrayLike | Sequence[float]</code> <p>Values to build the histogram from.</p> required <code>step</code> <code>int | None</code> <p>Global step value to record.</p> <code>None</code>"},{"location":"api/tracking/#substrax.tracking.Logger.log_text","title":"<code>log_text(name, text, step=None)</code>  <code>abstractmethod</code>","text":"<p>Log text.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the text entry.</p> required <code>text</code> <code>str</code> <p>Text to log.</p> required <code>step</code> <code>int | None</code> <p>Global step value to record.</p> <code>None</code>"},{"location":"api/tracking/#substrax.tracking.Logger.log_hyperparams","title":"<code>log_hyperparams(params)</code>  <code>abstractmethod</code>","text":"<p>Log hyperparameters.</p> <p>Parameters:</p> Name Type Description Default <code>params</code> <code>Mapping[str, Any]</code> <p>Hyperparameter names to values.</p> required"},{"location":"api/tracking/#substrax.tracking.Logger.close","title":"<code>close()</code>","text":"<p>Close the logger's handlers and release their resources.</p>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger","title":"<code>MLFlowLogger(name, *, log_dir=None, experiment_name=None, run_name=None, run_id=None, tracking_uri=None, registry_uri=None, level=logging.INFO)</code>","text":"<p>               Bases: <code>Logger</code></p> <p>Logger that records metrics, parameters and artifacts in an MLflow run.</p> <p>Images, histograms and texts are written to <code>&lt;log_dir&gt;/artifacts/&lt;kind&gt;/</code> when a log directory is given, else to a temporary file, and logged as run artifacts under <code>&lt;kind&gt;</code>. Images and histograms need the <code>plots</code> extra.</p> <p>Start or resume the MLflow run.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the logger; also the experiment name when none is given.</p> required <code>log_dir</code> <code>str | Path | None</code> <p>Local directory for artifacts, in addition to the run.</p> <code>None</code> <code>experiment_name</code> <code>str | None</code> <p>MLflow experiment, created if missing.</p> <code>None</code> <code>run_name</code> <code>str | None</code> <p>Name for a new run. MLflow generates one when omitted.</p> <code>None</code> <code>run_id</code> <code>str | None</code> <p>An existing run to continue; <code>experiment_name</code> and <code>run_name</code> are then ignored.</p> <code>None</code> <code>tracking_uri</code> <code>str | None</code> <p>MLflow tracking server. <code>None</code> keeps the environment's.</p> <code>None</code> <code>registry_uri</code> <code>str | None</code> <p>MLflow model registry. <code>None</code> keeps the tracking URI.</p> <code>None</code> <code>level</code> <code>int</code> <p>Logging level.</p> <code>INFO</code> <p>Constructing the logger raises <code>ImportError</code> when the <code>mlflow</code> extra is not installed.</p>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.log_scalar","title":"<code>log_scalar(name, value, step=None)</code>","text":"<p>Log one metric.</p>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.log_scalars","title":"<code>log_scalars(scalars, step=None)</code>","text":"<p>Log several metrics.</p>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.log_image","title":"<code>log_image(name, image, step=None)</code>","text":"<p>Log an image or a list of images as an artifact under <code>images</code>.</p>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.log_histogram","title":"<code>log_histogram(name, values, step=None)</code>","text":"<p>Log the summary statistics as metrics and the histogram as an artifact.</p>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.log_text","title":"<code>log_text(name, text, step=None)</code>","text":"<p>Log <code>text</code> as an artifact under <code>texts</code>.</p>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.log_hyperparams","title":"<code>log_hyperparams(params)</code>","text":"<p>Log the hyperparameters as run parameters.</p> <p>MLflow accepts strings, numbers and booleans; anything else is logged as its <code>str</code> representation.</p>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.log_artifact","title":"<code>log_artifact(local_path, artifact_path=None)</code>","text":"<p>Log one file as a run artifact.</p> <p>Parameters:</p> Name Type Description Default <code>local_path</code> <code>str | Path</code> <p>File to log.</p> required <code>artifact_path</code> <code>str | None</code> <p>Directory within the run's artifacts to place it in.</p> <code>None</code>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.log_artifacts","title":"<code>log_artifacts(local_dir, artifact_path=None)</code>","text":"<p>Log every file in a directory as run artifacts.</p> <p>Parameters:</p> Name Type Description Default <code>local_dir</code> <code>str | Path</code> <p>Directory whose files to log.</p> required <code>artifact_path</code> <code>str | None</code> <p>Directory within the run's artifacts to place them in.</p> <code>None</code>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.end_run","title":"<code>end_run()</code>","text":"<p>End the run; a second call is a no-op.</p>"},{"location":"api/tracking/#substrax.tracking.MLFlowLogger.close","title":"<code>close()</code>","text":"<p>End the run and close the logger.</p>"},{"location":"api/tracking/#substrax.tracking.WandbLogger","title":"<code>WandbLogger(name, project, *, entity=None, log_dir=None, config=None, tags=None, notes=None, level=logging.INFO, **init_options)</code>","text":"<p>               Bases: <code>Logger</code></p> <p>Logger that records metrics, media and artifacts in a Weights &amp; Biases run.</p> <p>Start the W&amp;B run.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the run and of the logger.</p> required <code>project</code> <code>str</code> <p>Name of the W&amp;B project.</p> required <code>entity</code> <code>str | None</code> <p>User or team that owns the project.</p> <code>None</code> <code>log_dir</code> <code>str | Path | None</code> <p>Local directory for W&amp;B's own files, in addition to the run.</p> <code>None</code> <code>config</code> <code>Mapping[str, Any] | None</code> <p>Hyperparameters to record on the run.</p> <code>None</code> <code>tags</code> <code>Sequence[str] | None</code> <p>Tags for the run.</p> <code>None</code> <code>notes</code> <code>str | None</code> <p>Notes about the run.</p> <code>None</code> <code>level</code> <code>int</code> <p>Logging level.</p> <code>INFO</code> <code>**init_options</code> <code>Any</code> <p>Further keyword arguments for <code>wandb.init</code>, such as <code>mode=\"offline\"</code> or <code>resume=\"allow\"</code>; the SDK owns their meaning.</p> <code>{}</code> <p>Constructing the logger raises <code>ImportError</code> when the <code>wandb</code> extra is not installed.</p>"},{"location":"api/tracking/#substrax.tracking.WandbLogger.log_scalars","title":"<code>log_scalars(scalars, step=None)</code>","text":"<p>Log several scalars to the run.</p>"},{"location":"api/tracking/#substrax.tracking.WandbLogger.log_image","title":"<code>log_image(name, image, step=None)</code>","text":"<p>Log an image or a list of images to the run.</p>"},{"location":"api/tracking/#substrax.tracking.WandbLogger.log_histogram","title":"<code>log_histogram(name, values, step=None)</code>","text":"<p>Log the histogram and its summary statistics (<code>&lt;name&gt;/min</code> and so on).</p>"},{"location":"api/tracking/#substrax.tracking.WandbLogger.log_text","title":"<code>log_text(name, text, step=None)</code>","text":"<p>Log <code>text</code> as a preformatted HTML block.</p>"},{"location":"api/tracking/#substrax.tracking.WandbLogger.log_hyperparams","title":"<code>log_hyperparams(params)</code>","text":"<p>Record the hyperparameters on the run's config.</p>"},{"location":"api/tracking/#substrax.tracking.WandbLogger.log_code","title":"<code>log_code(root=None)</code>","text":"<p>Save the code under <code>root</code> (default: the current directory) to the run.</p>"},{"location":"api/tracking/#substrax.tracking.WandbLogger.log_model","title":"<code>log_model(model_path, name=None, metadata=None)</code>","text":"<p>Upload a model file or directory as a W&amp;B artifact of type <code>model</code>.</p> <p>Parameters:</p> Name Type Description Default <code>model_path</code> <code>str | Path</code> <p>File or directory to upload.</p> required <code>name</code> <code>str | None</code> <p>Artifact name; defaults to <code>&lt;run name&gt;_model</code>.</p> <code>None</code> <code>metadata</code> <code>Mapping[str, Any] | None</code> <p>Metadata to record on the artifact.</p> <code>None</code>"},{"location":"api/tracking/#substrax.tracking.WandbLogger.finish","title":"<code>finish(exit_code=0)</code>","text":"<p>Finish the run; a second call is a no-op.</p> <p>Parameters:</p> Name Type Description Default <code>exit_code</code> <code>int</code> <p>Exit code to report to W&amp;B.</p> <code>0</code>"},{"location":"api/tracking/#substrax.tracking.WandbLogger.close","title":"<code>close()</code>","text":"<p>Finish the run and close the logger.</p>"},{"location":"api/tracking/#substrax.tracking.create_logger","title":"<code>create_logger(name, log_dir=None, *, log_to_file=True, level=logging.INFO)</code>","text":"<p>Create a console logger, or a file logger that also logs to the console.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the logger.</p> required <code>log_dir</code> <code>str | Path | None</code> <p>Directory to save logs to. <code>None</code> with <code>log_to_file</code> means <code>./logs/&lt;name&gt;</code>.</p> <code>None</code> <code>log_to_file</code> <code>bool</code> <p>Whether to write files as well as console lines.</p> <code>True</code> <code>level</code> <code>int</code> <p>Logging level.</p> <code>INFO</code> <p>Returns:</p> Name Type Description <code>A</code> <code>Logger</code> <p>class:<code>FileLogger</code> when <code>log_to_file</code> is set, else a :class:<code>ConsoleLogger</code>.</p>"}]}